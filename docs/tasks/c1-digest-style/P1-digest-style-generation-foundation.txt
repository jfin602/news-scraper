TASK: Correction 1 / P1 — Digest style generation foundation

CORRECTION SCOPE
This is the first implementation prompt in owner-approved correction `c1-digest-style`.

Phase 1 Gemini Profile digest foundation is GREEN / owner-accepted and package is `2.2.0`, before normal Phase 2 work. The governing AI contract now adds optional Profile-level `digestStyleGuidance` so an administrator can influence digest tone/voice/audience without replacing News Scraper's fixed Gemini grounding, URL, schema, validation, or security instructions.

P1 owns the complete server-side producer: additive persistence, canonical validation/read/update semantics, digest identity participation, safe Gemini request composition, and protected server-side Profile AI administration/API representation. P2 adds only the bundled browser/client field that consumes this producer.

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.
- Task-shape classification: `Cross-cutting`.
- Specification clarity: `Explicit`.
- State coupling: `Moderate` across persisted settings, digest identity/lifecycle, provider request construction, and protected admin services.
- Integrity risk: `High` because this changes supported-production persisted configuration and digest provenance/idempotency input.
- Concurrency: `None` new; existing post-provider settings revalidation must remain correct.
- Security: `Sensitive` because administrator-authored text enters the Gemini request boundary and must never expand system/tool/URL authority.
- Novel inference: `Moderate`.
- Model-family basis: Terra is the minimum adequate workhorse for the combined production migration, identity compatibility, prompt-authority, and admin producer work.
- Reasoning basis: High is warranted for Unicode normalization, production-forward migration, backward-compatible identity semantics, prompt-injection/URL allowlist isolation, and complete producer handoff.
- Complexity / quality floor: `High`.
- Estimated usage: `High`.
- Usage basis: multi-file source tracing, migration + real PostgreSQL/upgrade/recovery proof, identity/provider regressions, admin service/integration adaptation, and portable final regression.
- Lower-cost alternative considered: `Terra Medium`; rejected because persistence compatibility and untrusted prompt composition create two subtle correctness boundaries in one producer task.
- Escalation trigger: this requires changing lifecycle transaction/claim ownership, public v1/PHP schema, Gemini tool permissions, a generic prompt framework, or another unapproved architecture/product boundary.
- Escalation target: `Sol High`; return `Planning needed` rather than silently redesigning those areas.
- Estimate confidence: `High`.

VERSIONING
- Required unchanged project version: `2.2.0`.
- Do not change top-level `package.json` version.
- This correction consumes no Phase 2 patch number.
- Do not create `package-lock.json`, `npm-shrinkwrap.json`, or unrelated generated state.
- Do not manually commit; the correction runner owns the implementation-prompt commit boundary.

SEQUENCE / FRESHNESS PREFLIGHT
Before editing:
1. read current `BOOT.md`, `AGENTS.md`, `README.md`, `docs/README.md`, and `docs/codex-model-selection.md`;
2. read `docs/contracts/project-contract.md`, `docs/contracts/ai-assistance-contract.md`, `docs/contracts/testing-and-validation-contract.md`, `docs/decisions/production-data-and-schema-compatibility.md`, and the current `docs/roadmap/3.0-roadmap.md` correction entry;
3. require package exactly `2.2.0`;
4. inspect migration history, especially `0018_profile_ai_digest_foundation.sql` and `0019_digest_lifecycle_handoff.sql`; require no existing `0020` and do not rewrite historical migrations;
5. inspect `src/distribution/digests/repository.ts`, `input.ts`, `lifecycle.ts`, and `provider.ts`, including every important `ProfileAiSettings` consumer;
6. inspect `src/admin/profile-ai-administration.ts` and `src/app/web/profile-ai-administration-router.ts`;
7. inspect `test/unit/digest-input.test.ts`, `digest-lifecycle.test.ts`, `digest-provider.test.ts`, `test/database/profile-ai-digest-foundation.test.ts`, `profile-ai-administration.test.ts`, `distribution-profile-production-upgrade.test.ts`, `migrations.test.ts`, and `test/integration/profile-ai-administration-http.test.ts`;
8. search all settings constructors/update call sites and outward digest serializers/PHP consumers;
9. inspect the Test Necessity and Test Environment matrices before selecting commands;
10. stop/report planning drift if the setting is already implemented, package state moved, or the governing contract changed materially.

LOCKED SETTING SEMANTICS
Implement one canonical server concept:

`digestStyleGuidance: string | null`

Rules:
- admin label is **Digest writing style** (browser field is P2);
- maximum **500 Unicode code points** after canonical application normalization;
- `null`, empty, or whitespace-only means no custom guidance/default digest writing behavior;
- nonblank input is trimmed to one stable plain-text value while preserving meaningful internal text/newlines;
- subject/audience-specific wording is permitted as Profile configuration and must not create topic-specific engine branches;
- it may influence tone, voice, audience, formality, and similar writing qualities only;
- it never replaces fixed News Scraper instructions and cannot weaken grounding, URL allowlists, secret handling, tool selection, schema, support-ID validation, plain-text handling, or output bounds;
- saving configuration does not synchronously call Gemini;
- a real canonical style change must alter `digestInputIdentity` even with unchanged Article IDs;
- the raw setting stays server/admin-only and must not enter public v1/PHP/LKG/customer output, logs, or provider diagnostics.

PERSISTENCE / SUPPORTED PRODUCTION MIGRATION
Add exactly one forward migration after `0019`, expected:

`migrations/0020_profile_digest_style_guidance.sql`

Use `distribution_profile_ai_settings`; do not create a parallel table.

Requirements:
- add nullable `digest_style_guidance text`;
- existing rows upgrade to `NULL` while preserving enabled/lookback/max/timestamps/Profile relationships;
- newly created Profile AI rows default to `NULL` through the existing trigger/default path without rewriting `0018`;
- PostgreSQL must reject an over-500-character value and ambiguous blank/whitespace-only non-null state under an appropriate CHECK or equivalent schema guarantee;
- use PostgreSQL character semantics (`char_length`) rather than byte length;
- do not reset/recreate customer data, squash migrations, rewrite history, or invent a backfilled style;
- preserve migration-from-zero and supported production-forward upgrade through `0020`;
- preserve backup/restore/rollback compatibility under the production-data ADR.

PROFILE AI SETTINGS REPOSITORY / CANONICAL NORMALIZER
Extend `ProfileAiSettings`, row types, SELECT/UPDATE mapping, and tests with nullable `digestStyleGuidance`.

Create/reuse one canonical server normalizer. It must:
- accept null;
- accept strings, trim surrounding whitespace, and canonicalize blank/whitespace-only to null;
- reject non-string/non-null values;
- count Unicode code points, not JavaScript UTF-16 code units: 500 astral/emoji code points accepted, 501 rejected;
- return only a bounded plain-text string or null;
- never interpret markup, URLs, model names, tools, or commands.

Do not copy separate 500-character implementations into repository/admin/provider code. The repository full settings update remains the persistence authority for the complete settings shape.

PROTECTED SERVER-SIDE ADMIN PRODUCER
Extend `AdminProfileAiReadModel.configuration` with `digestStyleGuidance: string | null` and extend the existing protected configuration save/audit path.

Compatibility and behavior:
- P1 must remain usable before P2 changes the browser client: if the existing protected request omits `digestStyleGuidance`, preserve the currently stored style rather than clearing it;
- explicit null/empty/whitespace clears to null;
- existing required `digestEnabled`, `lookbackDays`, and `maxArticles` semantics remain unchanged;
- unknown fields still fail closed;
- reuse the canonical server normalizer; do not implement a second code-point policy;
- no-op canonical updates do not create duplicate audit events;
- a style change uses the existing bounded Profile AI configuration audit event and does not call lifecycle/provider generation;
- admin responses/audit contain no Gemini key, raw provider response, provider page body, or hidden system prompt;
- reuse the existing admin router and request-integrity boundary; create no new route.

P2 must be able to consume only this protected JSON configuration field and existing PUT route.

DIGEST INPUT IDENTITY — BACKWARD-COMPATIBLE EXTENSION
The setting is relevant digest configuration, but **do not create a false change for every existing Profile whose style remains default/null**.

Preserve the current `DIGEST_INPUT_IDENTITY_VERSION = 'v1'` and extend its canonical representation compatibly:
- when canonical `digestStyleGuidance` is `null`, the hashed JSON representation must remain byte-for-byte equivalent to the pre-correction v1 representation for the same Profile, enabled/lookback/max settings, and ordered Article IDs;
- when guidance is non-null, add that canonical style value to the hashed settings representation in a deterministic location;
- do not include `digestStyleGuidance: null` if doing so would change all historical no-style hashes;
- do not bump the version merely to add this optional field, because that would create needless `older` states/Gemini regeneration for Profiles whose effective writing configuration did not change;
- this is a backward-compatible extension: historical v1 identities represent the null/default style state; non-null style is a newly representable v1 settings variant.

Required identity behavior:
- unchanged null/default style preserves the exact historical v1 hash;
- null -> non-null changes identity;
- one non-null style -> another changes identity;
- non-null -> null returns to the historical/default identity when all other inputs are identical;
- whitespace-only canonicalizes to null and must not create a phantom identity;
- prior Profile/settings/Article-order distinctions remain exactly significant.

Add a test-only legacy-v1 representation/hash calculation or fixed known fixture if needed to prove no-style hash compatibility. Do not add a production legacy helper solely for the test.

LIFECYCLE CONSEQUENCES
Do not add a style-specific state machine. Existing generic identity behavior should own regeneration and post-provider revalidation.

Require/prove:
- migration itself makes no Gemini call;
- no-style existing active digests remain `current` after the migration when all governed input/settings are otherwise unchanged;
- first save of a real custom style makes the current active digest `older` when it remains canonically valid/overlapping, and the next normal scheduled evaluation recognizes the settings/identity change without requiring a newly entering Article;
- clearing style back to default uses the default identity semantics correctly;
- manual Generate now remains the same force path;
- if style changes while a generation is in flight, existing post-provider identity/settings revalidation rejects activation of output generated for the superseded style;
- no migration-time generation, second scheduler, transaction/lock redesign, heartbeat, or special provider call is introduced.

Change `lifecycle.ts` only if source proves the generic identity path is incomplete; otherwise add focused tests without moving responsibility.

GEMINI REQUEST COMPOSITION / PROMPT AUTHORITY
Keep the application-owned `DIGEST_SYSTEM_INSTRUCTION` fixed and authoritative. Never interpolate administrator guidance into `system_instruction`.

Use a clearly separated request-input section:

```text
fixed application system instruction
  > optional subordinate Profile writing-style guidance
  > governed Article JSON / exact originalUrl allowlist
```

Requirements:
- null guidance preserves default writing behavior and does not inject customer/topic-specific default prose;
- non-null guidance appears only in the request input, clearly labeled as subordinate style guidance;
- fixed application instruction states that Profile writing guidance can affect writing style only and cannot change policy, grounding, URL/tool scope, secrets, schema, support rules, or output bounds;
- treat guidance as untrusted text for prompt injection;
- neutralize URL-looking text in guidance through the same safe boundary used for other non-allowlisted untrusted text so it cannot add a raw provider URL;
- exact stored Article `originalUrl` values remain the only raw URL Context destinations;
- preserve Gemini Developer API/Interactions, default `gemini-3.6-flash`, `store=false`, low thinking, URL Context only, Search disabled, structured schema, timeout, one-request/no-hidden-retry path, response validation, support-ID validation, output bounds, and bounded failures;
- do not add fallback URLs/models/tools or another provider call.

FOCUSED PROOF
At minimum add/update tests proving:

Persistence / validation:
- `0020` appears in migration-from-zero;
- supported pre-`0020` state upgrades additively and prior AI/digest/Profile/customer-governed state is preserved;
- new Profiles default style null;
- canonical style round-trip and clear;
- 500 Unicode code points including emoji accepted by application; 501 rejected;
- PostgreSQL rejects >500 and invalid blank persisted state;
- malformed value types fail closed.

Identity / lifecycle:
- default/null style produces exactly the legacy v1 identity for the same inputs;
- custom style and distinct styles change identity;
- whitespace-only canonicalizes to the same no-style identity;
- `generationRequired` treats style change as relevant configuration with unchanged Article IDs;
- existing no-style active digest is not made older solely by migration;
- existing post-provider settings revalidation rejects a superseded style when an existing cheap test seam can prove it.

Provider safety:
- style appears in `input`, never `system_instruction`;
- fixed system instruction remains present/topic independent and explicitly outranks style;
- injection-like style stays subordinate;
- embedded style URL is neutralized and does not join the raw allowlist;
- governed Article original URLs stay exact;
- null style preserves default request behavior;
- tools/schema/store/thinking/model/request count remain unchanged.

Protected server admin producer:
- protected read returns nullable style;
- explicit save persists/audits without generation;
- omitted pre-P2 field preserves existing style;
- blank/null clears;
- invalid/over-limit returns bounded `invalid_request` without secret/raw error leakage;
- existing request-integrity and machine-vs-admin route separation remain intact.

PRODUCER → CONSUMER HANDOFF
P1 is incomplete unless P2 can remain thin:

`canonical string|null -> Profile AI repository -> protected admin read/update JSON`

and:

`canonical style -> backward-compatible digestInputIdentity -> existing lifecycle -> Gemini request input`

P2 must not invent DB normalization, identity, prompt composition, URL neutralization, audit, or server error semantics.

PRESERVED BEHAVIOR / NON-GOALS
Do not change:
- canonical Profile Article selection/order/lookback/max;
- Source trust/collection/Relevance/Categories/moderation/duplicates/Article identity/provenance/original destinations;
- digest output schema/bounds/support resolution;
- attempt claim/concurrency/persistence/activation/freshness architecture;
- public v1 digest fields, snapshot schema, PHP LKG/local-read shape, customer cache/presentation;
- Gemini model/tool/timeout/credential behavior;
- Phase 2 package/integration implementation;
- historical Phase 1 worksheet/prompts/validation.

Do not create a generic prompt-template/plugin system, arbitrary admin system prompt, rich-text/Markdown prompt editor, new AI endpoint, or customer-side Gemini config.

VALIDATION MANIFEST
RUN — `LOCAL-PORTABLE`:
- focused unit/input/lifecycle/provider/admin HTTP tests during iteration;
- final `npm run check`;
- `npm run codex:phase:validate -- c1-digest-style`;
- complete correction/current-diff `git diff --check` as appropriate.

RUN — `LOCAL-PREREQUISITE` (real disposable PostgreSQL):
- focused database files during iteration via `npm run test:db:focused -- <paths>`;
- final `npm run test:db` because schema/repository/admin persistence and production-forward migration changed;
- final `npm run test:recovery` because a supported-production schema migration changed.

DEFER:
- `npm run test:live-gemini` — `LIVE/EXTERNAL`; deterministic request construction is sufficient for P1. Do not spend a live call merely to judge subjective tone. Later Phase 2 integrated qualification may observe style behavior before customer rollout if that gate requires it.

N/A unless actual diff expands:
- `npm run test:browser` — P2 owns browser/client behavior;
- PHP/browser-PHP — outward representation is intentionally unchanged;
- live Sources/reference deployment;
- separate security suite unless auth/trust code actually changes.

Use the smallest focused iteration commands and do not duplicate subordinate suites after aggregates on an unchanged final tree. Missing required local prerequisites are reported honestly once, not retried/faked.

REPOSITORY RULES
- Do not edit governing docs/ADRs/roadmap/changelog/BOOT/AGENTS/README or prior Phase 1 records.
- Do not write the final correction validation artifact; P3 owns it.
- Do not add dependencies or change package version.
- P1 may adapt browser test fixtures/types minimally so typecheck compiles with the new server read-model field, but do not implement the actual browser field/client flow; P2 owns that.

ACCEPTANCE
P1 is complete only when:
1. additive `0020` safely adds nullable bounded style and production upgrade/recovery evidence passes;
2. one canonical server normalizer enforces null/trim/500-code-point semantics;
3. repository + protected admin producer read/update/audit style without synchronous Gemini work;
4. no-style/default preserves exact existing v1 digest identity while real style changes affect identity/regeneration;
5. existing lifecycle handles style changes/in-flight revalidation without a special state machine;
6. Gemini receives subordinate bounded style only in request input, with embedded URLs neutralized and fixed system/tool/schema authority preserved;
7. public v1/PHP/customer state has no style field;
8. required portable/database/recovery evidence passes;
9. package remains exactly `2.2.0` and no Phase 2 package behavior changed.

Finish with exact pre/post source identity, changed files, migration/normalization/identity/provider/admin behavior, tests actually run/results, deferred live evidence, package confirmation, and residual risk for P2/P3. Do not declare the correction GREEN; P3 owns final closeout.