TASK: Phase 20 / P1 — Phase 20 customer launch closeout

ROADMAP PHASE
Phase 20 — Customer launch validation.

MODEL / REASONING / USAGE
- Recommended configuration: `Sol Light`.
- Model-family basis: repository policy deliberately uses Sol for independent roadmap-closeout review; this task crosses final deployment, live Sources, browser/admin behavior, recovery, Cloudflare/origin protection, and the production-data baseline, so independent cross-system review is more valuable than a cheaper implementation-oriented pass.
- Reasoning basis: Light reasoning is sufficient because the governing contracts, Phase 19 operating procedures, and Phase 20 exit gate are explicit; deeper reasoning is reserved for a concrete defect or ambiguity discovered during validation.
- Complexity / quality floor: `High`.
- Estimated usage: `High`.
- Lower-cost alternative considered: `Terra High`; it is technically plausible for this explicit validation task but is not selected because current repository closeout policy intentionally uses `Sol Light` as the independent closeout baseline.
- Escalation trigger: unresolved production-baseline identity, difficult recovery/schema/transaction behavior, ambiguous Cloudflare/origin security, a cross-process defect, or another finding that cannot be confidently classified within existing governed behavior.
- Escalation target: `Terra Ultra` when deeper reasoning over explicit recovery/state interactions is dominant; use `Sol High` when the blocker requires stronger cross-system/security inference. Meaningful behavior-preserving structural refactors use the separate `Terra High` remediation handoff below.
- Estimate confidence: `High`.
- Efficiency rationale: Phase 20 adds no planned engine implementation. One closeout prompt reuses the existing admin, collection, browser, recovery, deployment, and operations surfaces, runs each required final evidence set once, and reserves expensive escalation for an observed hard problem.
- Plan delta: `Unchanged` from `/prompt-plan`.
- If this exact configuration is unavailable at execution time, re-read current `BOOT.md`, `docs/codex-model-selection.md`, and `scripts/codex-phase-core.mjs`; do not invent a configuration label.

VERSIONING
- This is Phase 20 prompt 1 and its assigned project version is `0.20.1`.
- On first execution require top-level `package.json` to be `0.20.0`, then advance only the top-level package version to `0.20.1` before establishing the launch candidate.
- On a rerun of this same P1 retain `0.20.1`; do not increment again.
- Any other package version is sequence drift: stop and report it.
- `package.json` remains the sole current-version authority.
- Do not create `package-lock.json` or `npm-shrinkwrap.json`.
- A later successful conversational `/closeout` separately establishes the Phase 21 baseline `0.21.0`; that transition is not part of P1.

CLOSEOUT-ONLY PHASE BOUNDARY
This is the sole final manual closeout prompt for Phase 20. The Phase 20 task stack intentionally contains zero implementation prompts. `npm run codex:phase -- p20` must parse a valid one-prompt phase, report zero implementation prompts, and leave P1 for manual execution.

Phase 20 configures, validates, and hands off the already-built Platform. Do not invent repository implementation work merely to make the phase look conventional. The normal repository write scope is limited to:
- the required `package.json` version transition to `0.20.1`;
- `docs/validation/phase-20-customer-launch-validation.md`;
- a concrete bounded defect fix plus focused regression only if closeout exposes an actual defect already inside governed MVP behavior.

Do not rewrite `config/publication.json` or `config/editorial.json` merely to mirror the live production database. Under the Source/Collection contract, bootstrap files are initialization input once admin is the normal management surface; live Publication/Source/endpoint/Category/Relevance/moderation state is governed database state.

New engine capability, deferred features, topic-specific shared-engine logic, native administrator identity, a second configuration authority, bulk historical Relevance reprocessing, destructive production-data behavior, or a meaningful structural refactor are not normal Phase 20 work. Stop and use the appropriate escalation/correction/documentation workflow rather than expanding this prompt.

TWO-STAGE MANUAL CLOSEOUT
P1 is a hard two-stage handoff:

- **Stage 1 — Codex launch-candidate certification:** establish version `0.20.1`, inspect the exact final product, run the final local/deterministic/PostgreSQL/recovery/browser/live-Source matrix, perform three independent closeout review passes, repair only bounded governed defects, create the provisional durable artifact, and commit the exact launch candidate.
- **Stage 2 — Operator-controlled customer launch:** the human operator deploys that exact candidate, curates/finalizes the real Publication through the existing Cloudflare-protected admin surfaces, validates approved live Sources and feed quality, reconfirms recovery policy/ownership and perimeter evidence, records launch metrics/ownership/limitations, and supplies observations for final artifact acceptance.

Stage 1 GREEN means only that the candidate is ready for the operator-controlled launch. It MUST NOT establish or claim the first supported production baseline.

If Stage 2 exposes a source, migration, package, runtime-contract, or test defect that changes executable behavior, the candidate is invalidated. Return to Stage 1, repair through the permitted bounded path or stop for correction/escalation, rerun all invalidated evidence, commit a new exact candidate, and restart affected Stage 2 evidence. Never combine executable evidence from different candidate trees.

Database/admin configuration changes made deliberately during Stage 2 are launch curation, not executable-source drift. Record the final governed live database configuration, moderation, and provenance state as part of the accepted baseline.

MANDATORY HANDOFF BEFORE WRITES
Before any P1 version/source/artifact write:
1. read current `BOOT.md`, `docs/codex-model-selection.md`, the complete Phase 20 roadmap entry, `docs/contracts/project-contract.md`, `docs/contracts/source-and-collection-contract.md`, `docs/contracts/public-feed-and-admin-contract.md`, and `docs/contracts/testing-and-validation-contract.md`;
2. read `docs/decisions/production-data-and-schema-compatibility.md`, `docs/decisions/cloudflare-access-admin-perimeter.md`, `docs/operations/security-reliability-and-operations.md`, `docs/operations/database-backup-and-restore.md`, and `docs/operations/deployment-and-incident-runbook.md`;
3. read the final Phase 19 validation artifact and distinguish reusable historical procedure evidence from evidence that Phase 20 must observe against the actual launch candidate/deployment;
4. inspect current `package.json`, `scripts/codex-phase-core.mjs`, `scripts/codex-phase.mjs`, `scripts/validate-codex-phase.mjs`, migration chain/status tooling, backup/restore tooling, reference-deployment validator, Web/Worker entrypoints, and current admin/public routes;
5. inspect the actual admin Source/endpoint, Publication, Editorial, Article/duplicate moderation, and Operations services/routes/UI plus their important tests; do not assume earlier task prose matches the current implementation;
6. inspect `config/publication.json` and `config/editorial.json` only as seed/bootstrap inputs, not as presumed production authority;
7. inspect `test/live-sources/` and identify exactly which approved Sources/endpoints it currently proves; do not overclaim that suite for additional launch Sources;
8. capture the pre-P1 `HEAD` as the Phase 20 closeout base for range validation and confirm the worktree is clean and `package-lock.json` absent;
9. run/inspect `npm run codex:phase:validate -- p20` and require the one-prompt stack to parse as P1 closeout at `0.20.1` with no implementation prompts;
10. stop and report planning/sequence drift if Phase 20 is not current, `0.20.0`/same-P1 rerun state is not coherent, the task grammar is invalid, a required launch capability is actually missing, or governing docs materially contradict this prompt.

PRESERVED LAWS
Throughout P1 preserve at minimum:
- one deployed installation hosts one singleton Publication/topic; shared engine remains topic independent;
- only explicitly administrator-approved Sources/endpoints may be contacted or counted as launch Sources;
- successful fetch/live behavior never expands approval/domain policy automatically;
- Source/endpoint approval, lifecycle, operational state, network safety, endpoint lock, run accounting, retry/failure isolation, Source RSS/Atom admission-filter semantics, Relevance/Categories, Source-scoped Article identity/idempotency, observations/provenance, and duplicate/manual-moderation laws remain unchanged;
- RSS/Atom admission filters are Source-owned, include-only, prospective, pre-normalization, and do not become HTML filters or a second Relevance engine;
- public feed eligibility remains singleton-public + approved/active Source + visible Article + ungrouped/Primary; filters/search/cursors cannot resurrect non-Primary/hidden rows;
- public headlines retain exact stored `original_url` as destination;
- Cloudflare Access/origin protection remains the external admin perimeter while the application retains request-integrity/resource-validation/content-safety responsibility;
- backup/restore/schema/rollback claims never exceed observed evidence;
- until final Phase 20 acceptance, the project remains pre-production and older unsupported pre-production databases remain disposable;
- after final Phase 20 acceptance, the exact accepted customer database/source/schema state becomes durable supported production state under the production-compatibility ADR.

DURABLE VALIDATION ARTIFACT
Create/update only from observed evidence:

`docs/validation/phase-20-customer-launch-validation.md`

After Stage 1, its prominent status must be:

`STAGE 1 GREEN — OPERATOR CUSTOMER-LAUNCH VALIDATION REQUIRED`

After every required Stage 2 item is actually GREEN and accepted, change it to:

`PHASE 20 GREEN — CUSTOMER LAUNCH ACCEPTED; FIRST SUPPORTED PRODUCTION BASELINE ESTABLISHED`

Never use the final status early.

The artifact must identify at minimum:
- Stage 1 and Stage 2 dates/times/environments;
- Phase 20 pre-P1 base SHA and exact committed launch-candidate SHA/tree;
- package version `0.20.1`;
- exact launched migration ledger/schema state and checksums/filenames sufficient to identify the schema baseline without secrets;
- Node/npm/PostgreSQL/PostgreSQL-client/Playwright/Chromium versions actually observed where relevant;
- exact Stage 1 commands/procedures and counts/results;
- exact final approved Source/endpoint inventory and non-secret configuration facts relevant to launch, including approval/lifecycle/operational state, endpoint type, Source priority, polling/default Category, and RSS/Atom admission phrases where configured;
- final Publication presentation/public/collection state and Category/Relevance inventory without inventing a second config authority;
- approved-live-Source observations and feed-quality sample counts per Source;
- duplicate-review/moderation disposition and remaining queue count;
- real public desktop/mobile/accessibility/theme/link/date observations;
- real authorized admin and Cloudflare/direct-origin observations;
- final Operations/health/queue/alert metric baseline using only facts the existing product actually exposes;
- the observed pre-launch backup identifier;
- monitoring/recovery/database/Cloudflare/origin ownership and escalation path;
- backup frequency/location/durability/encryption/access/retention policy, RPO/RTO, and total-host-loss posture;
- relevant log/audit/metadata retention/access ownership;
- operator training notes and launch checklist outcome;
- known limitations, retries, unavailable evidence, and accepted risk decisions;
- Pass 2 adversarial findings/disposition;
- Pass 3 structural findings and whether a Terra High remediation handoff occurred;
- explicit statement that bootstrap JSON is initialization input and the accepted production configuration is the governed database/admin state;
- explicit statement that the final accepted source SHA is the launched executable candidate, even if a later evidence-only commit finalizes this artifact;
- explicit identification of the governed live customer database configuration/moderation/provenance state observed during launch;
- intended next roadmap phase if GREEN: Phase 21 — Codebase simplification and maintainability hardening;
- statement that conversational `/closeout` remains required for the separate `0.21.0` baseline transition.

Do not record database URLs, credentials, Cloudflare cookies/tokens, private keys, internal secret-bearing origin URLs, or raw sensitive logs in the artifact.

STAGE 1 — VERSION AND CANDIDATE
On first execution:
1. verify `package.json` is exactly `0.20.0` and the worktree is clean;
2. change only the top-level version to `0.20.1` before candidate validation;
3. do not run `npm install` merely for the version edit;
4. if a concrete bounded defect is repaired during the passes, keep the repair minimal, add focused regression coverage when technically practical, and rerun every invalidated final-tree suite;
5. once all Stage 1 evidence and the provisional Stage 1 artifact are coherent, commit the exact candidate with a subject beginning `0.20.1` and record the resulting commit SHA/tree as the launch candidate;
6. after that candidate commit, do not alter executable source/migrations/package/runtime configuration while collecting Stage 2 evidence. Evidence-only artifact finalization may be committed later and must be distinguished from the launched executable SHA.

On a same-P1 rerun already at `0.20.1`, identify whether an earlier candidate was invalidated and establish one new coherent candidate rather than incrementing the version.

STAGE 1 — PASS 1: CONTRACT / EVIDENCE
Validate the exact final candidate against the complete Phase 20 exit gate.

A. FINAL PRODUCT / CONFIGURATION SURFACES
Confirm by source/test inspection that existing protected admin surfaces already own:
- singleton Publication configuration and presentation state;
- Source create/configuration/approval/lifecycle/operational state/priority/domain policy/RSS admission phrases;
- endpoint create/configuration/approval/lifecycle/operational state/polling/parser profile/default Category/check-now/health/recent runs;
- Categories and Relevance rules;
- Article visibility/display-title/Category moderation;
- duplicate review/dismiss/merge/split/Primary selection;
- operational snapshot/health/queue/alerts.

Confirm Web/API performs no Source collection inline and public `/`/`/api/feed` remain canonical.

B. PRODUCTION-LIFECYCLE BOUNDARY
Prove by contract/source/tool inspection that:
- Phase 19 established backup/restore, deployment/rollback, schema status/upgrade, and reference-deployment procedures;
- no Phase 20 acceptance has already been inferred from package `0.20.0`, previous Phase 19 deployment, or documentation-only commits;
- current migration history is coherent and migration-from-zero remains valid;
- the launched schema ledger can be identified exactly;
- after acceptance, supported production history/data cannot be destructively reset/squashed under the old pre-production rule.

C. LIVE-SOURCE TEST SCOPE
Inspect every file under `test/live-sources/` and record the exact named approved Sources/endpoints proven. Current historical suites may prove specific initial Sources, but they do not automatically prove every Source the operator later includes at launch.

D. PHASE 19 PROCEDURE REUSE
Treat Phase 19 Level 8/recovery evidence as evidence that the procedures and deployment topology were previously validated, not as a substitute for Phase 20 observations against the `0.20.1` launch candidate. Reconfirm the previously recorded local-only-backup/total-host-loss risk if it remains the actual policy.

STAGE 1 — FINAL NON-OVERLAPPING COMMAND MATRIX
Re-read current `package.json` immediately before execution and map to actual equivalent commands if scripts changed. Expected final matrix:

- `npm install --ignore-scripts`
- `npm run codex:phase:validate -- p20`
- `npm run check`
- `npm run test:db`
- `npm run test:security`
- `npm run test:recovery`
- `npm run test:browser`
- `npm run test:live-sources`
- `git diff --check <phase-20-closeout-base>...HEAD` after the candidate commit, plus a final working-tree coherence check

Do not separately rerun `test:unit`, `test:integration`, or `test:collection` after `npm run check`; they are contained by the current aggregate matrix. Do not repeat database/browser/security/recovery/live-Source suites solely for ceremony. Rerun only evidence invalidated by an intervening change or needed to diagnose a failure.

Required specialized suites must fail rather than silently skip for missing prerequisites or zero selected tests. Live-public-network failure must be reported truthfully; do not replace it with fixture evidence while claiming Level 7.

STAGE 1 — PASS 2: ADVERSARIAL LAUNCH REVIEW
After Pass 1 is green on the current candidate, independently derive realistic launch-failure hypotheses from the actual implementation and deployment contract rather than existing test names.

At minimum challenge:
- bootstrap JSON diverging from actual governed production database/admin state;
- a Source being counted as launch-ready without explicit administrator approval;
- Source/endpoint domain policy being widened merely to make a fetch pass;
- a live suite proving only a subset of the eventual launch Sources while the artifact overclaims all Sources;
- stale earlier successful Collection runs hiding a currently failing endpoint;
- conditional `304 not_modified` being misread as failure or as proof of fresh new content;
- prospective Source admission/Relevance edits being mistaken for retroactive cleanup of existing Articles;
- an existing bad row requiring ordinary Article moderation rather than a new bulk reprocessing mechanism;
- duplicate false positives suppressing legitimate related coverage;
- a wrong Primary keeping the wrong public `original_url` visible;
- a pending duplicate-review backlog being ignored because sampled feed rows look good;
- configured presentation timezone/date boundaries producing wrong visible calendar dates;
- browser suites passing locally while the deployed public/admin assets are stale or from a different SHA;
- authorized admin validation accidentally reaching a direct origin rather than the Cloudflare Access path;
- public root/API/readiness being healthy while Worker scheduling/collection is degraded;
- direct-origin bypass becoming reachable after deployment changes;
- local-only backup risk or no-guarantee total-host-loss RPO/RTO disappearing from production handoff wording;
- artifact text containing credentials, Access tokens/cookies, private keys, secret URLs, or raw logs;
- an evidence-only artifact commit being misidentified as the deployed executable candidate;
- package `0.20.1` or Stage 1 GREEN being mistaken for production-baseline acceptance before Stage 2 is complete.

Classify each plausible finding:
1. protected by implementation plus meaningful observed evidence;
2. governed behavior is clear but coverage/evidence is weak — add or collect the smallest missing proof;
3. concrete bounded Phase 20 defect — repair minimally, add focused regression where practical, and rerun invalidated evidence;
4. meaningful behavior-preserving structural refactor — stop and emit the Terra High remediation prompt below;
5. material ambiguity/hard defect — stop for model escalation/replanning/correction.

Do not invent launch policy merely to close a finding.

STAGE 1 — PASS 3: CODE-QUALITY / STRUCTURAL REVIEW
After Pass 1 and Pass 2 are green on the current candidate, independently inspect the final tree and important producers/consumers for launch-created or pre-existing structural risk that would invalidate safe launch.

Specifically look for:
- duplicate runtime configuration authority between bootstrap JSON, database/admin services, Worker, and public feed;
- topic-specific indie-author conditionals in shared engine/runtime code;
- admin browser logic inventing backend validation/state/health semantics;
- duplicate Source/Relevance/Category/duplicate/public-feed authorities;
- live-Source or deployment tests that claim broader coverage than they execute;
- runbook commands/routes that no longer exist;
- recovery/deployment scripts that can expose secrets or target unsafe databases;
- migration/status logic whose ledger interpretation differs from deployment/restore verification;
- launch-only adapters, wrappers, compatibility paths, or dependencies added without need;
- unbounded/N+1 admin/operations reads that are material at launch scale;
- stale documentation/task comments that would cause an operator to use the wrong admin/API/recovery path;
- redundant final test execution inconsistent with the validation-efficiency contract.

Small obvious bounded defects may be repaired inside Stage 1. Do not perform a general refactor merely because Phase 21 exists next.

TERRA HIGH STRUCTURAL REMEDIATION HANDOFF
If Pass 2 or Pass 3 finds a meaningful behavior-preserving structural refactor necessary before launch, stop before implementing it and output one self-contained transient `Terra High` remediation prompt.

The remediation prompt must:
- identify the concrete observed problem and why it blocks safe launch;
- define the smallest behavior-preserving file/module boundary;
- identify important producers/consumers and preserved contracts;
- preserve `0.20.1` on a same-P1 rerun;
- prohibit new product capability/topic logic/destructive database handling;
- require focused proof plus the smallest relevant broader regression set;
- require rerunning invalidated Stage 1 evidence afterward;
- require P1 to restart on the resulting tree before Stage 2 resumes.

Do not mark Stage 1 GREEN while this handoff is outstanding.

STAGE 2 — OPERATOR CUSTOMER-LAUNCH PROCEDURE
After Stage 1 is GREEN and the exact candidate is committed, stop claiming autonomous progress. Present the operator with bounded copy/paste or browser actions one logical block at a time, record only observations the operator actually supplies, and never impersonate Cloudflare/VPS/browser/database results.

Use the current deployment and backup runbooks as the command authority. Adapt hosting-specific commands only from the operator's actual environment; do not invent Docker/systemd/Kubernetes or other deployment machinery absent from the deployment.

1. EXACT CANDIDATE / PRE-LAUNCH BACKUP
- Deploy exactly the Stage 1 candidate SHA/tree and `0.20.1`.
- Record runtime/tool versions and verify the working tree/source identity.
- Confirm required environment-variable presence without printing secrets.
- Create a fresh managed pre-launch backup and record its safe identifier/duration.
- Run `npm run db:status`; require current or safely apply only reviewed pending repository migrations through `npm run db:migrate` according to the runbook.
- Stop on incompatible schema/history.

2. WEB / WORKER STARTUP AND BASE SMOKE
- Follow the governed Web-first/Worker-second startup and quiescence rules.
- Observe Web liveness/readiness, `/`, `/api/feed`, Worker ready/scheduler/recovery diagnostics, and database connectivity/current schema.
- Confirm public assets and API behavior correspond to the exact candidate, not a stale deployment.

3. FREEZE THE REAL LAUNCH CONFIGURATION THROUGH `/admin`
Through authorized Cloudflare Access, review and deliberately finalize the actual database-owned launch state:
- Publication name/description/logo/accent/timezone, `activeForCollection`, and `publicStatus`;
- every Source identity/display/site URL/priority/default Category/approved domains/RSS admission phrases/approval/lifecycle/operational state;
- every endpoint URL/type/poll interval/domain narrowing/default Category/profile where applicable/approval/lifecycle/operational state;
- Category vocabulary;
- Relevance rules, enabled state, action/predicate/Source scope/target Category;
- Article manual visibility/display/Category overrides relevant to launch;
- duplicate review/group/Primary state relevant to launch.

Treat repository bootstrap/editorial JSON as initialization/history, not a required mirror. Do not add a Source merely because it is technically collectable, present in reconnaissance, requested historically, or fetches successfully. Every launch Source must have explicit administrator/owner approval. Record that approval source in the artifact without recording secrets.

An empty Relevance-rule set is acceptable when the real launch review demonstrates that default-include behavior is intentionally appropriate. Do not create rules ceremonially.

4. FINAL APPROVED ENDPOINT COLLECTION / HEALTH
For every final Source/endpoint that is approved + active + enabled and intended for collection:
- use existing Check now and/or ordinary Worker scheduling through the governed path;
- observe recent runs and endpoint health;
- distinguish success, unchanged/`304 not_modified`, filtered-item outcomes, parser/profile failures, and transport/network failures truthfully;
- require no unexplained failing launch endpoint;
- confirm unrelated Sources continue when one Source is exercised.

5. LEVEL 7 LIVE-SOURCE / PUBLIC-FEED QUALITY SAMPLE
For each final launch Source, inspect at least 5 current feed-eligible rows, or every available current row when fewer than 5 exist. Record the sample count.

For sampled rows verify:
- Source label matches the owning approved Source;
- headline/display title is accurate and inert;
- visible calendar date is correct for configured Publication timezone or UTC fallback;
- headline destination is the exact stored publisher `original_url` and goes to the intended original publisher;
- effective Category/Relevance treatment is appropriate for the configured Publication;
- public eligibility is correct and hidden/non-Primary/ineligible rows do not appear.

Existing `test:live-sources` evidence may be cited only for the Sources/endpoints that suite actually exercises. Any additional launch Source requires its own real deployed collection/feed observation through the governed path.

6. EDITORIAL TUNING WITHOUT FALSE RETROACTIVITY
If Source RSS admission phrases or Relevance/Category configuration needs tuning, use the existing admin surfaces and collect new observations as needed. Treat those edits as prospective unless an existing explicit moderation control is used.

For already-persisted launch rows needing correction, use existing Article hide/restore, display-title override, Category override, or duplicate moderation as appropriate. Do not invent automatic bulk historical reprocessing inside Phase 20.

After material curation, rerun the affected Source/feed sample and record the final state.

7. DUPLICATE QUALITY / MODERATION GATE
Before acceptance:
- inspect every pending duplicate-review candidate currently present in the launch database;
- dismiss false/weak candidates where appropriate;
- merge/split/choose Primary only through existing moderation authority;
- verify grouped sampled Articles expose the intended Primary/original publisher destination;
- confirm legitimate related coverage remains separate;
- preserve all Article/observation provenance;
- record pending duplicate-review count at acceptance and require it to be zero unless an explicit owner-approved exception with rationale is recorded.

If the pending set is unexpectedly too large for responsible review, stop launch rather than silently sampling around unresolved duplicate risk.

8. REAL PUBLIC PRESENTATION / ACCESSIBILITY
Against the deployed public URL, observe at minimum:
- representative desktop viewport around `1280x900`;
- representative mobile viewport around `390x844`;
- no destructive horizontal overflow with real long Source/headline/category values;
- readable Date | Headline | Source hierarchy and stacked mobile behavior;
- system/light/dark theme behavior;
- keyboard access and visible focus for core public controls/links;
- understandable loading/error/empty behavior when practically observable without harming production;
- correct configured-timezone/UTC-fallback calendar dates;
- exact original publisher navigation.

Do not claim a formal automated accessibility audit beyond what is actually observed/tested. Existing Level 6 browser suites remain the deterministic regression evidence; this step is real-deployment confirmation with customer content.

9. REAL AUTHORIZED ADMIN / OPERATOR TRAINING
Through the normal Cloudflare Access path, confirm the operator can:
- open Operations;
- inspect Sources/endpoints, health, recent runs, and Check now;
- pause/disable and resume the appropriate narrow collection scope conceptually without bypassing lifecycle rules;
- edit Publication and Editorial configuration;
- search/inspect/hide/restore Articles;
- review/dismiss/merge/split/select duplicate Primary;
- recognize when to use the deployment/recovery/Cloudflare incident runbooks rather than direct DB edits.

Record concise operator training notes in the artifact. Do not expose or teach direct SQL as a normal management path.

10. LEVEL 8 REFERENCE DEPLOYMENT / ADMIN PERIMETER
- Run `npm run deployment:validate -- <private-reference-config.json>` with credential-free distinct public/direct-origin targets.
- Observe unauthenticated public `/admin` challenge/denial without following it into an authenticated session.
- In a normal authorized browser session, confirm `/admin`, Operations, Sources, Editorial, and Articles are reachable through Cloudflare Access.
- Externally confirm direct-origin/IP/alternate-host bypass remains denied/unreachable/inconclusive only as classified by the governed validator/runbook; an actually successful unprotected admin response is a launch blocker.
- Identify/reconfirm the real Tunnel/firewall/Authenticated-Origin-Pulls/equivalent origin control without recording secrets.

The shared validator does not authenticate an Access identity; never claim otherwise.

11. POST-LAUNCH OPERATIONAL METRIC BASELINE
After final curation/collection settles, capture only the bounded facts actually exposed by Operations and endpoint health, including where available:
- overall operational status and active alerts;
- final Source/endpoint counts and health distribution;
- ready/future/running/expired job counts;
- oldest-ready queue delay if present;
- current capacity/timing facts exposed by the canonical snapshot;
- endpoint-specific failures/delay needing ownership;
- pending duplicate-review count from the moderation surface.

This is a baseline observation, not a new analytics feature or SLA promise.

12. OWNERSHIP / RECOVERY / LIMITATIONS
Reconfirm or deliberately amend the Phase 19 deployment-specific record:
- monitoring/alert owner and escalation path;
- recovery incident commander;
- database owner;
- Cloudflare/Access/origin owner;
- backup schedule/location/durability/encryption/access/retention and last tested restore;
- RPO/RTO plus rationale/measurement method;
- Cloudflare/admin, application audit/change-history, infrastructure/IP-log, and bounded Source/Raw-item metadata retention/access ownership;
- actual origin-protection mechanism;
- adopted process-manager commands/log locations.

If backup remains local-only, explicitly preserve the accepted limitation that total VPS/storage loss has no guaranteed recovery point or recovery time for configuration/moderation/operator-created state. Do not hide this behind generic production-ready wording.

13. FINAL LAUNCH ACCEPTANCE SNAPSHOT
Before changing the artifact to final GREEN, reconfirm and record:
- exact deployed executable candidate SHA/tree and `0.20.1`;
- current schema status and complete migration ledger identity;
- final non-secret Publication/Source/endpoint/Category/Relevance configuration inventory;
- final approved live-Source/feed sample outcomes;
- duplicate-review disposition;
- public desktop/mobile/admin observations;
- Operations baseline;
- Cloudflare/origin results;
- observed pre-launch backup identity plus backup/recovery policy and ownership;
- production ownership/limitations;
- no required Stage 2 item left blank, inferred, or borrowed from a different candidate/deployment.

PRODUCTION-BASELINE ACCEPTANCE RULE
Only after all required Stage 1 and Stage 2 evidence is GREEN against one coherent launch candidate may the artifact state that Phase 20 is accepted and the first supported production baseline exists.

The accepted baseline identity is the combination of:
- the exact launched executable source SHA/tree and package version `0.20.1`;
- the exact launched current migration ledger/schema state;
- the governed live customer database/configuration/moderation/provenance state observed during launch;
- the recorded deployment ownership, operational policy, and accepted limitations under which that state was accepted.

From that acceptance forward, customer production data is durable supported state. Future schema/persisted/data-access work must preserve this baseline under `docs/decisions/production-data-and-schema-compatibility.md`; clean migration-from-zero alone is no longer sufficient production-upgrade proof.

An evidence-only commit that finalizes `docs/validation/phase-20-customer-launch-validation.md` after Stage 2 does not replace the recorded launched executable SHA/tree. Keep both identities explicit.

FINAL CLOSEOUT RESULT
If and only if all requirements are green:
1. finalize the durable artifact with the exact final GREEN status specified above;
2. commit only the evidence-only artifact update if the launched executable candidate itself did not change;
3. report the accepted launched executable SHA/tree, package `0.20.1`, schema ledger baseline, governed live customer database/configuration/moderation/provenance state, final evidence artifact commit SHA, and any explicitly accepted limitations;
4. report Phase 20 GREEN and identify Phase 21 as the next roadmap phase;
5. state that conversational `/closeout` is still required and must perform only the separate Phase 21 `0.21.0` version-only baseline transition after its normal structural handoff check.

If any required evidence is unavailable, failed, materially ambiguous, or tied to a different candidate, report Phase 20 BLOCKED/RED with the precise missing evidence. Do not soften a blocking failure into a known limitation merely to launch.

NON-GOALS
Do not:
- add new product features or deferred capabilities;
- add native admin identity/accounts/roles/sessions;
- add another Source discovery/approval mechanism;
- hard-code indie-author publication logic into shared engine behavior;
- convert bootstrap JSON into a competing production authority;
- bulk mutate historical Articles merely because editorial/filter configuration changed;
- weaken Source approval/domain/network safety, request integrity, idempotency, provenance, duplicate topology, or public eligibility;
- delete Articles/observations to make duplicate quality look cleaner;
- change schema/migrations unless a concrete launch-blocking defect requires an approved bounded fix path;
- treat old unsupported pre-production databases as supported upgrade inputs;
- perform a destructive production reset after final acceptance;
- claim Level 6/7/8, backup/restore, browser, live-Source, Cloudflare, direct-origin, or operator evidence that was not actually observed;
- run redundant subordinate tests already contained by the final matrix;
- perform Phase 21 cleanup/refactoring early;
- update roadmap/root phase-state documentation inside P1; conversational `/closeout` and the subsequent docs workflow own the phase transition/alignment.
