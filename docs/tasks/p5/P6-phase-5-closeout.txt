TASK: Phase 5 / P6 — Phase 5 closeout validation

ROADMAP PHASE
Phase 5 — RSS/Atom transport, parsing, and minimal Collection runs.

MINIMUM MODEL / EFFORT
- Minimum recommended rating: `Terra Max`.
- A stronger model/effort tier may be used.
- Do not lower this rating without explicit repository-owner direction.
- Rationale: closeout must reason across security-sensitive transport/redirect behavior, real PostgreSQL run provenance, deterministic fixture evidence, Worker process behavior, and approved live-Source evidence while ensuring no partial proof is overstated. It should add no new architecture.

VERSIONING
- This is Phase 5 prompt 6 and its assigned project version is `0.5.6`.
- On first execution confirm P5 left `package.json` at `0.5.5`, then advance only its top-level `version` to `0.5.6` before establishing the final source SHA to validate.
- If this same P6 prompt is rerun and `package.json` is already `0.5.6`, retain `0.5.6`; do not increment again.
- If another unexpected version exists, stop and report sequence drift.
- `package.json` is the sole authoritative current-version source.
- The repository intentionally does not use `package-lock.json`; do not create, require, regenerate, or validate one.
- The later conversational `/closeout` command, if Phase 5 is accepted, separately transitions to the next phase baseline and is NOT part of this P6 task.

DEPENDS ON
- P1 minimal Collection-run persistence.
- P2 Raw-item/RSS/Atom parser boundary.
- P3 validated-address one-hop HTTP transport.
- P4 safe redirect-aware feed fetcher.
- P5 canonical manual Worker endpoint collection service/command.
- Existing approved bootstrap configuration containing Author Media and The Creative Penn.

GOAL
Perform final-tree Phase 5 validation, fix only defects required to satisfy the already-approved Phase 5 contracts/roadmap if any are discovered, rerun the complete required matrix against the exact accepted tree, and create the durable Phase 5 validation artifact.

P6 is primarily a validation/closeout task, not a feature prompt. Do not add new product scope merely to make validation easier.

TWO-STAGE CLOSEOUT / EXTERNAL LIVE-EVIDENCE HANDOFF
Phase 5 closeout is explicitly allowed to span two environments:

1. Stage 1 — primary Codex/local environment, expected to be the repository owner's Windows development environment.
2. Stage 2 — repository-owner VPS environment, used for the required approved live-Source evidence when that evidence cannot or should not be executed from the primary Codex environment.

Do not force one environment to stand in for evidence that belongs to the other. Evidence claims remain scoped to the exact environment, command/procedure, source tree, and evidence level actually observed.

STAGE 1 — LOCAL / WINDOWS CANDIDATE VALIDATION
On the first P6 execution:
- advance to `0.5.6` as specified above;
- inspect/finalize the Phase 5 implementation tree;
- add the substantive explicit `test:live-sources` command/procedure if it does not already exist;
- run every required static, deterministic, fixture, process, and Level 4 PostgreSQL check that is available in the primary environment;
- fix only Phase 5 defects discovered by that validation;
- rerun the complete affected matrix after any implementation/test correction;
- establish the exact final `0.5.6` candidate implementation source SHA only after all executable source/test/version changes are complete;
- create or update the one canonical Phase 5 validation artifact even if VPS Level 7 evidence is still outstanding.

If all locally executable requirements pass but required VPS Level 7 evidence has not yet been supplied, the Stage 1 result is NOT Phase 5 green. Record the artifact state explicitly as:
- `PARTIAL — VPS LEVEL 7 EVIDENCE PENDING`, or equivalent unambiguous wording.

That state means the local candidate is ready for external live validation; it is not a failure of otherwise passing local evidence and it is not permission to run conversational `/closeout`.

The Stage 1 artifact must contain a dedicated `VPS handoff` section recording:
- exact candidate implementation source SHA;
- package version `0.5.6`;
- branch;
- local environment/tool versions;
- local commands/procedures actually executed and concise outcomes;
- evidence levels already satisfied;
- explicit statement that Level 7 has not yet been claimed;
- exact remaining live-source command/procedure expected on the VPS;
- required environment prerequisites for that command, without secrets;
- the two approved Source/endpoint identities that must be exercised;
- the bounded result fields that must be captured for final artifact completion;
- explicit instruction not to use the production application database as the disposable test database.

If the validation artifact is committed after the candidate implementation source SHA, record the candidate implementation SHA separately from the later docs-only artifact commit SHA. A later docs-only artifact commit does not redefine the candidate executable tree.

STAGE 2 — VPS LEVEL 7 VALIDATION
The repository owner may inspect the Stage 1 artifact, deploy the candidate tree to the VPS, and execute the required live procedure there.

Before accepting VPS evidence:
- verify the VPS deployment corresponds to the Stage 1 candidate implementation tree;
- if VPS `HEAD` includes only later docs-only validation-artifact commits on top of the candidate implementation SHA, record that relationship and confirm no executable product/test/migration/configuration drift exists between the candidate SHA and deployed tree;
- if executable source, tests, migrations, dependency metadata, package scripts, or committed Publication configuration changed after Stage 1 validation, the candidate SHA is stale and affected local validation must be rerun before closeout.

The VPS Level 7 procedure must use a safe disposable PostgreSQL administrative boundary through `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL`; it must not use the production `news_scraper` application database as the disposable test target.

After the repository owner supplies the VPS output/evidence, rerun this same P6 task at `0.5.6`. Do not consume another prompt/version number.

On the Stage 2/finalization rerun:
- inspect the existing canonical validation artifact first;
- verify supplied VPS evidence applies to the recorded candidate implementation tree;
- record the VPS environment/tool versions, commands/procedures, timestamp/timezone, bounded results, evidence level, and limitations;
- update the same canonical validation artifact; do not create a second Phase 5 validation artifact;
- if no executable source changes are needed, do not rerun unrelated local suites merely because the artifact changed;
- if live evidence exposes an implementation defect requiring executable changes, fix the smallest root cause under `0.5.6`, establish a new candidate implementation SHA, rerun all affected local/final-tree evidence, and require fresh VPS Level 7 evidence against that new candidate;
- only after all final acceptance criteria are satisfied may the artifact state become Phase 5 green/accepted.

Do not substitute local Windows live-network evidence for separately supplied VPS evidence when the repository owner has designated the VPS as the Level 7 closeout environment. Do not claim Level 8 reference-deployment validation unless that separate level was intentionally executed and recorded.

GOVERNING DOCUMENTS
Read current versions before validation and again if implementation changes materially during defect correction:
- `BOOT.md`
- `AGENTS.md`
- `docs/contracts/project-contract.md`
- `docs/contracts/domain-and-data-contract.md`
- `docs/contracts/source-and-collection-contract.md`
- `docs/contracts/testing-and-validation-contract.md`
- `docs/architecture/system-architecture.md`
- `docs/operations/security-reliability-and-operations.md`
- `docs/roadmap/mvp-roadmap.md`
- `docs/decisions/whitelist-and-structured-feed-first.md`
- `docs/validation/phase-4-collection-eligibility-network-safety.md`
- Phase 5 P1-P5 task files and final implementation/tests.

PHASE 5 EXIT GATE TO PROVE
The Phase 5 roadmap exit gate requires all of the following on the accepted final tree:
- at least two real approved active enabled feeds fetch/parse independently through Worker execution for live-Source evidence;
- every real fetch attempt has a persisted truthful Collection run;
- deterministic fixture tests cover representative success, malformed, conditional/no-change, redirect, bounds, and isolated-failure behavior;
- one broken feed does not prevent another run from completing;
- re-fetching unchanged content is transport-safe and deterministic at the Raw-item boundary.

The inherited testing contract also requires:
- focused tests for each implemented capability;
- broader regression coverage for touched Phase 1-4 behavior;
- Level 4 PostgreSQL proof for persistence/lock claims;
- Level 5 deterministic collection-fixture evidence;
- final-tree static/test/runtime evidence;
- no skipped/flaky/zero-selected required suite standing in for proof;
- durable validation artifact tied to the exact accepted implementation source SHA.

NO NEW FEATURE DESIGN
P6 should not redesign interfaces or add new capabilities if P1-P5 are contract-correct.

If validation exposes a defect:
- fix the smallest root cause under the same P6 `0.5.6` version;
- add/adjust focused regression coverage;
- rerun every affected broader suite plus the complete closeout matrix;
- establish the accepted implementation SHA only after the final executable code/test/version state is complete.

If validation exposes a material contract ambiguity or architecture decision that cannot be corrected as a clear defect, stop and report the blocker rather than inventing new Phase 5 scope during closeout.

EXPECTED PHASE 5 IMPLEMENTATION SHAPE
Before running proof, inspect the final tree and confirm it has one coherent implementation path:
- one minimal `collection_runs` persistence model/repository;
- one generic Raw-item model;
- one parser adapter and RSS/Atom parser;
- one validated-address one-hop HTTP transport;
- one redirect-aware fetcher reusing the Phase 4 safety gate every hop;
- one canonical endpoint collection service;
- one manual Worker-owned endpoint command;
- existing long-running Worker runtime preserved;
- no inline Web/API Source fetching.

Report unexpected parallel fetch/parser/run paths as a closeout concern even if tests pass.

CLEAN DEPENDENCY / ENVIRONMENT RECORD
Record at minimum for each environment used:
- implementation source SHA/deployed relationship;
- branch;
- Node version;
- npm version;
- PostgreSQL server/client version relevant to DB evidence;
- operating system/platform;
- resolved XML parser dependency version where relevant;
- relevant environment prerequisites used, without exposing credentials.

Because the repository intentionally has no package lock:
- use `npm install`, not `npm ci`;
- confirm `.npmrc` prevents lock generation;
- do not describe dependency installation as byte-for-byte reproducible;
- confirm no `package-lock.json` was created.

VERSION-FIRST CLOSEOUT SEQUENCE
P6 owns `0.5.6`.

Before candidate validation:
1. verify expected preceding version `0.5.5` or existing rerun version `0.5.6`;
2. set top-level `package.json` version to `0.5.6` if needed;
3. do not alter dependency ranges merely for the version bump;
4. include the versioned tree in every validation claim;
5. if later defect fixes alter executable source after an earlier test pass, rerun the applicable matrix and establish a new candidate SHA before collecting/reusing live evidence.

STATIC / STRUCTURAL REVIEW
Before or alongside execution, inspect final Phase 5 source and confirm:
- shared aggregation code contains no indie-author/Author Media/Creative Penn-specific business conditions;
- only configured approved Sources/endpoints can enter canonical collection execution;
- Web/API does not perform Source collection inline;
- every redirect passes Phase 4 scheme/domain/default-port/DNS/public-address validation before contact;
- actual transport uses only Phase-4-approved concrete address information and preserves hostname/TLS verification;
- no TLS certificate bypass or production safety test bypass exists;
- parsers emit Raw items and do not persist Articles;
- Raw-item data remains unnormalized/untrusted at Phase 5 boundary;
- Collection runs start only after eligibility + lock and before safety/fetch;
- ineligible/locked attempts do not create runs;
- expected safety/transport/parser attempts produce truthful terminal run accounting;
- 304/no-change behavior does not invoke parser on nonexistent content;
- no persistent endpoint ETag/Last-Modified scheduling/cache state has been front-loaded;
- no normalization/Relevance/Article/duplicate/scheduler/health/public-feed implementation exists;
- no uncontrolled full publisher response/body fixtures are committed;
- fixture data is minimal/sanitized/deterministic;
- logging/output does not expose credentials, database URLs, authorization headers, raw bodies, or unbounded stack/error content.

ORDINARY STATIC / DETERMINISTIC COMMANDS
Run against the exact candidate tree and record exact outcomes:
- `npm install`
- `npm run format:check`
- `npm run lint`
- `npm run typecheck`
- `npm run test:unit`
- `npm run test:integration`
- `npm run test:collection`
- `npm test`
- `npm run check`

If any explicit filtered/subsuite command selects zero tests, that is a failure; do not report green.

Run `git diff --check` against the accepted Phase 5 change range/appropriate base so committed whitespace errors are actually inspected. Do not substitute an empty clean-working-tree diff.

DATABASE / LEVEL 4 MATRIX
Run the full:
- `npm run test:db`

with a valid safe `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL` pointing only to a disposable-test-capable PostgreSQL administrative environment.

Required DB evidence includes:
- production migrations from zero apply in order through Phase 5 schema;
- rerun is idempotent/current;
- Collection-run FK/status/count/timestamp/error constraints;
- Collection-run start/finalize repository lifecycle;
- double-finalization protection;
- transaction/failure behavior as implemented;
- Phase 3 Publication/Source/endpoint configuration constraints/bootstrap idempotency/no-overwrite regressions;
- Phase 4 endpoint advisory-lock contention, unrelated endpoint independence, release/reacquisition;
- canonical collection execution creates no run for lock contention;
- canonical collection execution creates truthful terminal runs for actual attempted safety/transport/parser paths;
- failure paths do not leak the endpoint lock;
- disposable databases are cleaned up and cleanup is verified.

A mocked DB test does not satisfy these claims.

If the primary environment genuinely cannot supply the required Level 4 prerequisite, record that as a real blocking evidence gap; do not silently defer Level 4 merely because Stage 2 exists. The two-stage handoff is primarily for the designated VPS Level 7 evidence.

DETERMINISTIC LEVEL 5 COLLECTION MATRIX
`npm run test:collection` plus relevant ordinary integration tests must prove representative behavior without live publisher dependencies.

Required fixture coverage should include at least:

RSS/ATOM PARSING
- valid RSS with multiple items;
- valid Atom with multiple entries;
- zero-item valid feed;
- common namespace metadata supported by implementation;
- missing optional item fields;
- malformed XML;
- valid XML that is not supported RSS/Atom;
- hostile/DOCTYPE/entity-expansion or equivalent XML security case;
- parser/item/field bounds;
- repeated fixture parse determinism.

HTTP TRANSPORT
- validated-address binding/no unchecked second DNS;
- Host/TLS hostname semantics at the strongest deterministic evidence implemented;
- identifiable User-Agent/Accept headers;
- 2xx content;
- conditional request headers;
- ETag/Last-Modified response metadata;
- 304 no-change;
- supported XML content types;
- unsupported content type;
- HTTP failure/retry classification;
- connect timeout;
- total timeout;
- wire response-size below/at/above boundary;
- gzip/deflate and Brotli if supported;
- decompressed-size below/at/above boundary;
- malformed compression;
- unsupported encoding;
- network reset/failure cleanup.

REDIRECTS
- relative redirect;
- allowed absolute redirect;
- multiple allowed hops;
- rejected cross-domain redirect;
- unsupported scheme;
- non-default port;
- unsafe/mixed DNS answer on later hop;
- missing/blank/malformed Location;
- redirect loop if implemented;
- redirect limit below/at/above boundary;
- fresh DNS safety decision per hop including same-host redirect;
- no prior-hop address reuse;
- shared overall deadline across redirect chain;
- 304 after redirect.

CANONICAL COLLECTION ORCHESTRATION
- eligibility blocked -> no run/fetch;
- lock blocked -> no run/fetch;
- run creation failure -> no network work, lock release;
- safety block -> persisted failed run with transport/parser not_run;
- transport failure -> persisted failed run with parser not_run;
- 304 -> persisted successful not_modified run, no parser;
- content + parser success -> persisted successful run + correct Raw-item count;
- parser failure -> persisted failed run with transport succeeded/parser failed;
- run finalization failure surfaces instead of false success;
- unrelated/later endpoint can still execute after one failure.

Do not weaken production SSRF/whitelist policy to run these fixtures.

MANUAL WORKER RUNTIME / PROCESS VALIDATION
Exercise the actual Phase 5 manual endpoint command against a controlled environment before live Sources where possible.

Record:
- exact command syntax;
- argument validation behavior;
- missing endpoint behavior;
- database startup/failure behavior;
- structured success/failure output shape;
- clean resource shutdown;
- confirmation existing `start:worker` lifecycle/readiness tests remain green.

Web/API should require no Phase 5 runtime change; existing Web regressions must remain green under ordinary tests.

LIVE-SOURCE SUITE / COMMAND
P6 should add the first substantive explicit live-Source validation command if P1-P5 did not already add one:
- recommended `npm run test:live-sources`.

This command must remain OUTSIDE ordinary `npm test` and `npm run check` because live public network is not deterministic regression infrastructure.

It may be implemented as a dedicated Node test/procedure under e.g.:
- `test/live-sources/phase-5-approved-feeds.test.ts`
- plus only the smallest support required.

The command should load optional `.env` because it requires DB/network environment.

Update package-script contract tests accordingly:
- `test:live-sources` is environment-requiring;
- ordinary deterministic commands remain `.env`-free.

LIVE-SOURCE PREREQUISITES
The live suite/procedure must:
- require a safe usable `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL`;
- fail clearly if required DB/network prerequisites are unavailable; do not silently skip green;
- create a unique disposable PostgreSQL database rather than use development/production application data;
- run production migrations from zero;
- explicitly run the existing approved bootstrap configuration into the disposable DB;
- contact only endpoints already explicitly approved in repository-owner configuration;
- use the real canonical manual Worker collection service/path and real production Phase 4 safety/HTTP/parser implementation;
- clean up the disposable DB and verify cleanup.

Do not auto-approve newly discovered Sources during live testing.

APPROVED PHASE 5 LIVE SOURCES
The repository-owner-approved initial Source set is:

1. Author Media
- Publication slug: `indie-author-publishing-news`
- Source config key: `author_media`
- endpoint config key: `site_rss`
- approved URL from bootstrap: `https://www.authormedia.com/feed/`

2. The Creative Penn
- Publication slug: `indie-author-publishing-news`
- Source config key: `the_creative_penn`
- endpoint config key: `podcast_rss`
- approved URL from bootstrap: `https://www.thecreativepenn.com/feed/podcast/`

Do not substitute a different publisher/feed without explicit repository-owner approval.

Historical Phase 3 reconnaissance that these feeds existed does NOT satisfy Phase 5 Level 7 proof. P6 must observe the current live behavior through the implemented Worker collection path.

LEVEL 7 LIVE PROCEDURE
For each approved endpoint independently:
1. load configured aggregate from the disposable DB;
2. verify it is approved/active/enabled and Publication collection-active through the real service path;
3. execute the canonical endpoint collection operation using production resolver/safety/transport/redirect/parser;
4. assert/record that a Collection run was created for the real attempt;
5. if live fetch/parse succeeds, record terminal transport/parser status, HTTP status, redirect count, byte counts, Raw-item count, and parser dialect where available;
6. inspect only bounded metadata/sample fields needed to establish Raw-item plausibility—do not persist/print full publisher content;
7. confirm the second Source executes independently even if the first failed;
8. record actual observed failure honestly if a publisher is unavailable/changed; do not rewrite contracts or whitelist policy to force green.

The roadmap exit gate requires at least two real approved feeds to fetch/parse successfully. If one/both approved feeds are currently unavailable or changed incompatibly, Phase 5 closeout remains BLOCKED until the real issue is resolved through an appropriate follow-up decision/fix. Do not treat historical or mocked success as substitute evidence.

UNCHANGED / CONDITIONAL LIVE BEHAVIOR
The exit gate also requires re-fetching unchanged content to be transport-safe and deterministic at the Raw-item boundary.

Phase 5 intentionally does not persist endpoint cache validators yet, so live proof may perform a controlled two-step fetch in the validation procedure:
- first real fetch captures ETag/Last-Modified if the publisher supplies them and parses Raw items;
- second immediate fetch supplies available validators to the same fetcher/service test seam/procedure;
- if server returns 304, confirm no parser invocation/zero Raw items and truthful run state as applicable;
- if server ignores validators/does not supply them and returns content, parse again and confirm Raw-item output is deterministic for unchanged returned content where observable;
- do not invent persistent endpoint cache columns to satisfy this proof.

Because publisher content may change between calls, record limitations honestly. Deterministic fixture evidence remains the authoritative regression proof for conditional/no-change semantics; live evidence proves only what was observed at that time.

FAILURE-ISOLATION LIVE EVIDENCE
The roadmap requires one broken feed not to prevent another run from completing.

Deterministic tests must prove this reliably.

For live validation, do not intentionally break or attack a publisher. It is enough to execute the two approved endpoints as independent operations and establish that one operation's result/failure does not control whether the other is attempted. If both succeed, deterministic controlled failure tests remain the proof of the broken-feed case.

DO NOT RUN IN BROWSER
Phase 5 has no browser-dependent product behavior. `test:browser` / Level 6 is not required.

DO NOT CLAIM REFERENCE DEPLOYMENT
Phase 5 closeout is not Level 8 deployment proof unless the user separately requests it and the corresponding reference-deployment procedure is actually executed. Existing VPS health, PM2, nginx, Cloudflare, backup, or restore observations may be recorded separately for their true scope, but they do not substitute for or automatically elevate P6 Level 7 live-Source evidence to Level 8.

CLOSEOUT VALIDATION ARTIFACT
Maintain exactly one canonical artifact:
- `docs/validation/phase-5-rss-atom-transport-parsing-collection-runs.md`

Stage 1 MUST create/update this artifact once the candidate implementation SHA is established and local validation has been executed, even when VPS Level 7 evidence is still outstanding.

Stage 1 artifact state:
- mark it explicitly `PARTIAL — VPS LEVEL 7 EVIDENCE PENDING` when local required evidence is green but the designated VPS live procedure has not yet been supplied;
- do not state that Phase 5 is complete;
- do not state that the roadmap exit gate is satisfied;
- do not authorize `/closeout`.

Final Stage 2 artifact state:
- update the same file with verified VPS Level 7 evidence;
- change the final exit-gate determination to green only if every acceptance criterion is satisfied;
- do not create a second closeout artifact.

The artifact must identify, as applicable to each stage:
- validation date/time/timezone;
- branch;
- exact candidate/accepted Phase 5 implementation source SHA;
- any later docs-only artifact commit SHA and its relationship to the candidate SHA;
- package version `0.5.6`;
- Node/npm/PostgreSQL/XML-parser versions and operating environment for each evidence source;
- commands/procedures actually executed;
- concise result counts/statuses;
- evidence level for each claim;
- live Source names/endpoints actually observed once Level 7 is supplied;
- bounded observed live results and timing context;
- limitations/environmental caveats;
- explicit current Phase 5 roadmap exit-gate determination;
- explicit statement that the artifact records evidence and does not redefine contracts.

Do not copy huge terminal logs into the artifact. Preserve enough exact command/result information to prove what was run.

VPS HANDOFF OUTPUT REQUIRED FROM STAGE 1
The Stage 1 task result shown to the repository owner must prominently report:
- candidate implementation SHA;
- validation artifact path;
- artifact state (`PARTIAL — VPS LEVEL 7 EVIDENCE PENDING` when applicable);
- whether `test:live-sources` was added/changed and its exact invocation;
- required VPS environment variable/prerequisite names without secret values;
- explicit instruction that the repository owner should inspect the validation artifact before executing the VPS phase;
- explicit statement that conversational `/closeout` must not run yet.

FINAL NO-SCOPE-LEAK INSPECTION
Before declaring Phase 5 complete, inspect final tree and affirm that Phase 5 did NOT implement:
- Article normalization;
- relative Article URL acceptance/canonical identity URL handling;
- Source/domain Article-link post-normalization gate;
- Relevance evaluation;
- Article schema/persistence;
- Article observations;
- Article processing outcome/effect counters;
- duplicate detection/grouping;
- due-endpoint scheduler/durable jobs;
- retry/backoff/jitter scheduler;
- endpoint last-success/next-due/health/failure-counter state;
- persistent ETag/Last-Modified endpoint cache state;
- public feed/API/UI;
- admin collection route/UI;
- HTML collection/custom browser automation;
- topic-specific engine rules.

If later-phase code was introduced accidentally, remove it or report the blocker before closeout.

BROADER REGRESSION / PRESERVED BEHAVIOR
Explicitly confirm earlier behavior remains green on the final candidate tree:
- Web/API liveness/readiness;
- Worker readiness/lifecycle/clean shutdown;
- runtime/database config validation;
- database migration/checksum behavior;
- Publication/Source/endpoint structural constraints;
- bootstrap idempotency/no overwrite;
- Phase 4 eligibility decisions;
- static destination scheme/domain/default-port policy;
- DNS public-unicast/special-use rejection;
- IPv4-mapped IPv6 safety;
- redirect safety primitives;
- rebinding-resistant validated-address handoff;
- endpoint lock contention/release/reacquisition;
- no public-network dependency in ordinary deterministic tests.

LIKELY FILE SCOPE
Expected P6 changes when P1-P5 are correct:
- `package.json` — version + `test:live-sources` script if not already substantive;
- `test/live-sources/**` and minimal support for the explicit live suite/procedure;
- `test/unit/package-scripts.test.ts` for command classification;
- `docs/validation/phase-5-rss-atom-transport-parsing-collection-runs.md`;
- only defect-fix source/tests discovered by closeout validation.

Do not perform broad documentation cleanup in P6. Root/roadmap phase-state advancement happens later through `/closeout` then `/docs-review`/`/docs-apply` workflow.

REQUIRED EVIDENCE LEVELS
- Level 0/1: final architecture/file/scope/security inspection + static checks.
- Level 2/3: focused/unit/integration/process regression proof.
- Level 4: real disposable PostgreSQL migration/run/lock persistence evidence.
- Level 5: deterministic controlled HTTP/RSS/Atom/redirect/conditional/bounds/failure fixture evidence.
- Level 6: not applicable.
- Level 7: both approved live Sources through the production Worker collection execution path, supplied from the designated VPS closeout environment when using the two-stage workflow.
- Level 8: not required.

STAGE 1 ACCEPTANCE / HANDOFF CRITERIA
Stage 1 is ready to hand to the repository owner for VPS validation only when:
- `package.json` is exactly `0.5.6`;
- all executable P6 changes required for the candidate are complete;
- clean install/static/type/lint/format checks pass on the candidate tree;
- complete deterministic ordinary and `test:collection` matrices pass with no required zero-selected/skipped/flaky substitutes;
- full `test:db` passes against safe disposable PostgreSQL and cleanup is verified;
- deterministic fixtures cover the Phase 5 matrix;
- Phase 4 security/lock regressions remain green;
- manual Worker collection command/service behavior is validated;
- substantive `test:live-sources` or equivalent explicit procedure exists and is outside ordinary deterministic commands;
- no later-phase scope leak remains;
- no `package-lock.json` exists;
- candidate implementation SHA is established;
- the canonical validation artifact exists in `PARTIAL — VPS LEVEL 7 EVIDENCE PENDING` state with a complete VPS handoff section.

If those local requirements do not pass, do not label the result as merely VPS-pending; report the actual local blocker.

FINAL ACCEPTANCE CRITERIA
P6 / Phase 5 is complete only when:
- every Stage 1 handoff criterion remains valid for the accepted implementation tree;
- Author Media and The Creative Penn each successfully fetch/parse through the real canonical Worker execution path under Level 7 evidence;
- every observed live fetch attempt has truthful persisted Collection-run provenance;
- unchanged/refetch behavior is supported safely with deterministic fixture proof and live observations recorded without persistent Phase 10 cache state;
- failure isolation is proven deterministically and independent live attempts are observed;
- VPS evidence is tied to the same accepted executable tree, allowing only explicitly recorded docs-only artifact commits above the candidate SHA;
- the canonical durable Phase 5 validation artifact records local and VPS environments separately, commands/procedures, results, evidence levels, limitations, and the exact accepted implementation SHA;
- the artifact explicitly determines the Phase 5 roadmap exit gate is satisfied;
- no later executable drift invalidates the recorded evidence;
- no `package-lock.json` exists.

BLOCKED / PENDING CLOSEOUT CONDITIONS
Do not report Phase 5 green while any final requirement is missing.

Use `PARTIAL — VPS LEVEL 7 EVIDENCE PENDING` rather than a generic failure when all Stage 1 criteria pass and the only outstanding requirement is the repository-owner-designated VPS Level 7 procedure.

Report a genuine blocker rather than VPS-pending if, for example:
- one/both approved live feeds are executed but cannot successfully fetch/parse and no approved resolution exists;
- live attempt lacks persisted truthful run provenance;
- safety requires a test/production bypass;
- transport can independently re-resolve after safety validation;
- required deterministic collection suite is flaky/skipped/zero-selected;
- required Level 4 prerequisite is unavailable and cannot be executed;
- migration/run/lock constraints fail;
- executable tree changes after the last applicable validation and required evidence is not rerun;
- supplied VPS evidence cannot be tied to the candidate implementation tree;
- validation artifact cannot truthfully tie evidence to the accepted tree;
- later-phase behavior materially contaminates Phase 5 boundaries.

NON-GOALS / DEFERRED
Do not use P6 to implement:
- Phase 6 normalization;
- Phase 7 Relevance/Article persistence;
- Phase 8/9 public feed;
- Phase 10 scheduler/cache persistence/health;
- admin UI;
- HTML collection;
- duplicate behavior;
- production monitoring/reference-deployment infrastructure.

IMPLEMENTATION DISCIPLINE
Validation evidence is the product of this prompt. Do not optimize for a green report by weakening tests, safety, bounds, approval policy, or evidence terminology.

A successful Stage 1 handoff is intentionally not a final closeout. If the exact final tree cannot satisfy the Phase 5 exit gate at the required evidence levels, report the limitation/blocker explicitly and leave the phase open. Conversational `/closeout` may run only after the canonical Phase 5 validation artifact has been finalized green with the required VPS Level 7 evidence.