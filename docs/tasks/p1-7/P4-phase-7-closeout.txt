TASK: Phase 7 / P4 — Phase 7 closeout

- Recommended configuration: `Sol Light`.
- Task-shape classification: `Investigative`.
- Specification clarity: `Explicit`.
- State coupling: `High`.
- Integrity risk: `High`.
- Concurrency: `Cross-process`.
- Security: `Sensitive`.
- Novel inference: `Moderate`.
- Model-family basis: repository policy uses Sol's independent review/code-reading capability for roadmap closeout. Phase 7 is the terminal 2.0 gate and now includes a restricted Windows -> managed-VPS SSH validation protocol, real managed Web/Worker/PostgreSQL/live-Source/API/security/recovery proof, and genuinely external PHP/LKG/SSR/failure proof.
- Reasoning basis: Light is the closeout-policy baseline because contracts and task boundaries are explicit and this prompt requires three disciplined passes. Escalate only when an observed blocker needs deeper state/security/architecture reasoning; do not escalate merely because the evidence matrix is large.
- Complexity / quality floor: `Critical`.
- Estimated usage: `Very High`.
- Lower-cost alternative considered: none under current roadmap closeout policy.
- Escalation trigger: unresolved production-data risk, Source-trust drift, credential/admin leakage, pagination/revision inconsistency, LKG corruption, unsafe remote validator privilege, unrestricted SSH fallback, external-host secret leakage, visitor-path API coupling, disable/re-enable failure, recovery incompatibility, or a structural defect that cannot be confidently resolved inside bounded existing 2.0 behavior.
- Escalation target: `Terra High` / `Terra Ultra` for explicit deeper state/integration/refactor reasoning, or higher-effort Sol for ambiguous contract/architecture reconciliation, selected from the observed cause.
- Estimate confidence: `High`.
- Efficiency rationale: independently audit the exact committed P3 behavior candidate once, use only the restricted remote interfaces for live evidence, reject any privilege/evidence ambiguity, and produce the one durable artifact that gates the terminal version-only `2.0.0` transition.

The assigned project version is `1.7.4`.

## Goal

Perform the terminal independent Phase 7 closeout against the exact integrated P1-P3 behavior candidate, execute the complete applicable Level 0-8 evidence matrix, audit the restricted remote-validation boundary, and write the durable Phase 7 release-qualification artifact.

This is not a feature prompt and not the terminal conversational release transition.

The final result remains **HUMAN REVIEW REQUIRED**. Do not perform `/closeout`, do not change package version to `2.0.0`, and do not create `1.8.0` or `2.0.1`.

## Critical pre-closeout operator gate

The normal Phase runner stops before P4. Before P4 begins, the operator must have used that pause to prepare the exact committed P3 candidate.

Require all of the following before review:

1. local Git HEAD is the runner-created P3 commit and package version is `1.7.3`;
2. the managed News Scraper deployment is the exact same committed P3 behavior SHA, deployed through the normal operator deployment path;
3. the P1 remote-validator bundle installed on the managed VPS matches the expected committed P1/P3 protocol identity;
4. Windows alias `news-scraper-phase7` succeeds for `preflight`/`capabilities` through the restricted account only;
5. an operator-created short-lived arm is present for the exact P3 candidate and dedicated Phase 7 validation resources required by mutating tests;
6. the generic PHP integration corresponding to the exact P3 behavior tree is installed on a genuinely external PHP host;
7. if automated external validation is used, the independent restricted alias `news-scraper-phase7-php` is provisioned and points to that external host, not the managed VPS;
8. otherwise the operator has the P3 external procedure/evidence channel ready;
9. managed/external environments start from normal real endpoints/config, not a fault origin.

If the managed deployed SHA, external installed source identity, remote-validator protocol identity, or local P3 HEAD do not match, stop before behavioral review. Do not auto-deploy via unrestricted SSH.

## Mandatory remote access rules

Managed Codex access is exclusively:

```text
news-scraper-phase7
```

through the P1 named-operation forced-command protocol.

External Codex access, if available, is exclusively:

```text
news-scraper-phase7-php
```

through an independently restricted named-operation protocol.

Never use:

- raw managed/external IP/hostname SSH targets;
- `deploy`, `root`, ordinary hosting account shell, or another unrestricted identity;
- `ssh -F NUL` to bypass the configured aliases;
- PTY/interactive shell, `bash`, `sh`, arbitrary quoted remote command;
- `scp`, `sftp`, `rsync`, forwarding, ProxyCommand, agent forwarding;
- SSH config/known-host/key/agent mutation;
- `StrictHostKeyChecking=no`/`accept-new`;
- automatic retry loops after auth/host-key/connect failures.

Use the P1 client or equivalent:

```text
ssh -o BatchMode=yes -o ConnectTimeout=10 -o ConnectionAttempts=1 <approved-alias> <named-operation>
```

One failed transport attempt is enough. Stop with a bounded operator blocker rather than risking Fail2ban or weakening SSH.

Do not modify `/etc/ssh`, `authorized_keys`, `sudoers`, account membership, forced-command scripts, or arm state from the validator connection.

## Exact identity and allowed P4 changes

Establish from Git and remote evidence, not conversation text:

- exact pre-P1 Phase 7 base commit;
- P1/P2/P3 runner-created commits;
- local pre-closeout P3 HEAD;
- local package version;
- complete Phase 7 source/task diff;
- managed deployed behavior SHA/version;
- managed validator protocol/bundle identity;
- external installed behavior source/package identity;
- P2/P3 non-secret evidence handoffs and limitations.

The required Level 7/8 runtime evidence is tied to the **exact committed P3 behavior tree** that is deployed/installed before P4.

P4 must not silently change behavior-bearing production source and still call that evidence final. If any pass discovers a behavior-bearing defect requiring source change, STOP and produce the appropriate remediation/correction/refactor handoff. After a runner-created repair commit is deployed to both required environments, P4 must be rerun from the beginning.

P4 itself may create/update only the durable validation artifact, top-level `package.json` version, and narrowly non-runtime closeout/test evidence support that does not change the behavior being claimed. If a supposedly non-runtime change could alter managed/external execution, treat it as behavior-bearing and block.

This rule exists because the phase runner creates the commit after the agent returns; uncommitted P4 source cannot honestly be claimed as remotely executed.

## Read first

Read:

1. current `BOOT.md`, `AGENTS.md`, root `README.md`;
2. Project Contract;
3. product scope;
4. distribution/integration contract;
5. distribution API contract;
6. testing/validation contract;
7. Source/collection and Article lifecycle/deduplication contracts where live Source/eligibility/provenance claims are checked;
8. accepted headless-distribution and managed/self-hostable ADRs;
9. production-data/schema compatibility ADR;
10. backup/restore and deployment/incident runbooks;
11. security/reliability/operations;
12. Phase 7 and terminal release roadmap entries;
13. accepted Phase 20 baseline and relevant Phase 1-6 validation artifacts;
14. every prompt under `docs/tasks/p1-7/`;
15. complete Phase 7 diff from exact pre-P1 base through P3 HEAD;
16. P1 remote protocol/server-helper/shared-PHP/fault-origin code and tests;
17. P2 managed Profile/credential/v1/deployment/recovery/live-Source code/tests implicated by evidence;
18. P3 PHP sync/LKG/local-read/render/external qualification code/tests;
19. current package command graph/runner grammar.

Task wording and prior green output remain lower authority than contracts/current source/executed evidence.

# Pass 1 — Contract and evidence review

## A. Restricted managed transport

Verify source plus live `preflight`/`capabilities` evidence proves:

- Windows targets only `news-scraper-phase7`;
- account is the dedicated validator identity, not deploy/root;
- remote protocol/bundle identity matches expected P1 artifact;
- unknown operations are rejected;
- client has no direct-host/PTY/forwarding/file-transfer/general-shell fallback;
- SSH auth/host-key/connect failures fail once, not retry-loop;
- remote JSON is bounded/schema-validated/redacted;
- no secret, DB URL, Authorization, raw payload, environment dump, or backup content is returned;
- server dispatcher does not use remote input via `eval`, general `sh -c`/`bash -c`, arbitrary executable/path, arbitrary npm script, or arbitrary SQL;
- any sudo/deploy delegation is exact and narrow rather than broad shell authority;
- validator cannot edit SSH/sudoers/private config/arm state or elevate itself;
- no agent-controlled uncommitted code execution was used as a privilege bypass.

An unrestricted shell path, broad sudo, or remote-input command injection is a release blocker.

## B. Mutation arming boundary

For every managed state-changing qualification operation verify:

- arm was operator-created outside validator authority;
- unexpired at execution;
- exact P3 candidate SHA matched;
- exact validation Profile/Source/disposable credential identities matched;
- operation was explicitly allowed;
- ordinary customer resources were rejected/unaffected;
- before/after state was bounded;
- cleanup/restoration was proven;
- validator could not create/renew/broaden the arm.

Require at least one deterministic test showing mutation rejection for missing/expired/mismatched arm. Do not intentionally mutate production merely to prove rejection.

## C. P1 qualification/fault boundaries

Verify:

- shared-host-like public/private/scheduled-sync plane remains test/release tooling;
- visitor PHP remains independent of upstream URL/token;
- same-VPS plane is preflight only;
- qualification-only fault origin is not imported/mounted/reachable through production Web/API;
- no real bearer credential is required/stored by fault origin;
- malformed/revision/rate/dependency scenarios are deterministic and do not implement competing Profile/cursor/eligibility authority;
- missing prerequisites fail, not skip;
- output/cleanup is secret-safe/bounded.

## D. Real managed upstream proof

Against exact deployed P3 behavior SHA, require actual evidence for:

- accepted production migrations `0001`-`0014` unchanged/supported;
- post-launch migration ledger current;
- clean migration and accepted-production -> current disposable upgrade preservation;
- actual managed DB status observed without reset/reseed;
- current backup/recovery compatibility including fresh-target restore when required;
- Web/Worker healthy under existing topology;
- at least one real currently approved live Source/endpoint through canonical collection with no trust widening;
- dedicated validation Profile provisioned through protected control plane;
- >100 real eligible Articles and genuine multi-page v1 traversal;
- Profile key, stable revision/order/boundary continuity, exact originalUrl;
- invalid/revoked auth -> bounded `401`;
- machine credential lacks admin authority;
- disposable real rate-limit -> `429` + positive `Retry-After` without unrelated credential impact;
- legitimate Profile mutation -> old cursor real `409 snapshot_changed` then fresh recovery;
- real disable -> authenticated `409 profile_disabled` then re-enable/recovery;
- `/`, `/api/feed`, health, Cloudflare/admin perimeter, direct-origin, HTTPS/trusted-proxy behavior preserved;
- targeted telemetry safe and useful.

If any claim was only local/fixture/operator-description rather than observed in managed environment, downgrade it and block the corresponding gate.

Admin mutations may be operator-performed when deliberately not exposed to the validator. Require evidence that the actual protected lifecycle was used; never replace it with raw SQL or a hidden SSH admin authority.

## E. Genuine external PHP proof

Verify the customer host is materially separate from managed News Scraper host/process/filesystem.

If `news-scraper-phase7-php` is used, verify the same no-shell/no-forwarding/no-file-transfer/named-operation/redaction principles independently. If operator-run, verify evidence provenance and exact source identity rather than assuming commands ran.

Require normal path evidence:

- exact P3 generic PHP package installed/used without provider-specific product branch;
- public root excludes state/token/sync config/private logs/fault controls;
- real external scheduled/CLI sync uses real managed HTTPS v1 and traverses >1 page;
- complete validated candidate atomically activates;
- ordinary visitor uses LocalProfileReader/LocalReadResult + customer/fallback renderer, not cache internals;
- public page is server-rendered and reachable externally;
- JavaScript-disabled browser sees core content/links;
- exact stored `originalUrl` direct anchors, no tracking/redirect rewrite;
- visitor process lacks upstream URL/token and causes no server-side/browser-direct API calls;
- no fault controls/private state leak to customer output.

## F. Required final failure matrix

Require honest evidence on the exact behavior candidate for each state:

### upstream/API/sync failure
- failed sync preserves active valid LKG;
- usable/stale-usable visitor rendering continues locally;
- no emergency live visitor API fallback;
- later real sync recovers.

### malformed/incomplete candidate
- isolated fault origin challenges real external PHP synchronizer/store;
- partial candidate rejected;
- prior real LKG unchanged;
- no malformed body/secret leak;
- real sync later succeeds.

### snapshot revision change
Require both halves:
1. real managed legitimate revision change -> old cursor `409 snapshot_changed`;
2. external PHP handles that class with candidate discard/bounded restart/LKG preservation, using deterministic real coordination or isolated fault origin.

Do not let fault-origin evidence replace production API semantics.

### stale / cutoff / never-synced
- stale valid renders by default without hard cutoff;
- configured strict cutoff suppresses Articles without deleting recoverable state;
- fresh sync/restored config recovers;
- separate fresh state shows never-synced safe fallback;
- no live visitor request in any state.

### authoritative disable/re-enable
- real admin disable -> real `409 profile_disabled` -> external authoritative disabled state -> cached Articles suppressed;
- re-enable/restore -> successful real sync -> external rendering returns;
- other failures never infer disabled.

### invalid/revoked auth
- external sync auth failure preserves LKG under stale policy and does not mark disabled;
- valid replacement recovers;
- no credential leak.

### Retry-After
- managed real limiter proof exists;
- external PHP retry/sleep/exhaustion behavior is proven using short deterministic qualification sequence;
- active state preserved.

## G. Final restoration

Before green require:

- managed Web/Worker healthy;
- managed DB current;
- validation Profile active and exact intended config restored;
- ordinary customer Profiles/credentials/Sources unaffected;
- valid external sync credential active/private;
- disposable/revoked/rate-test credentials not used by ordinary integration;
- external base URL points to real managed instance;
- cadence/cutoff restored;
- fresh real multi-page sync completed;
- external page renders current real data;
- no fault-base URL/scenario flag/process/network/DNS outage remains;
- no private/generated runtime state tracked;
- arms either expired/removed by operator or no longer authorize unnecessary mutations.

## Evidence-level honesty

Classify evidence per testing contract:

- Levels 0-3: inspection/static/deterministic component;
- Level 4: real disposable PostgreSQL persistence/migration/recovery plus observed managed DB state where applicable;
- Level 5: deterministic collection/fault fixtures;
- Level 6: real browser;
- Level 7: approved live Source;
- Level 8: integrated managed deployment + genuinely external customer-style PHP integration under recorded conditions.

Remote SSH transport does not raise an evidence level by itself. A named operation proves only the real environment behavior it actually executed.

Do not call 2.0 production-ready without actual Level 7/8 evidence.

# Pass 2 — Adversarial error / edge-case review

Independently challenge at least:

- local client falls back to raw IP/deploy/root after alias failure;
- repeated auth attempts trigger Fail2ban and lock out operator access;
- host-key checking is weakened;
- validator can request arbitrary command/path/npm script/SQL;
- remote output parser accepts oversized/non-JSON/secret output;
- sudoers/helper grants shell/interpreter/general deploy authority;
- validator can edit its own forced command/private config/arm;
- expired/mismatched arm still mutates state;
- arm names an ordinary customer Profile/credential/Source;
- failure cleanup leaves Profile disabled, credential revoked, service stopped, recovery DB/live fault origin behind;
- uncommitted agent code was executed remotely despite the committed-candidate rule;
- same-VPS preflight presented as external proof;
- external alias accidentally points to managed host;
- public root contains state/token/config/logs/scenario controls;
- sync/visitor environments share bearer secret;
- browser looks local-only while server-side visitor PHP calls upstream;
- partial candidate activates after page 1;
- cursor mixes revisions or boundary item duplicates/drops;
- supposed pagination is actually one page;
- ordinary sync failure blanks valid stale LKG;
- auth/rate/service failure marks state disabled;
- real profile_disabled fails to suppress retained LKG;
- re-enable leaves external page permanently disabled/stale;
- stale cutoff off-by-one/future clock issue;
- malformed response leaks body/path/secret;
- rate test harms ordinary credential or floods production;
- production upgrade proof relies only on clean DB;
- backup exists but was never restored;
- restore targets source/non-empty production DB;
- accepted migrations rewritten;
- live Source test touches unapproved endpoint or widens domain trust;
- machine bearer/admin/perimeter bypass;
- trusted-proxy spoof/direct-origin exposure;
- telemetry leaks token/verifier/Authorization/DB URL/private payload;
- direct publisher URL rewritten/tracked;
- JS required for core content;
- external/operator evidence cannot be tied to exact P3 source identity;
- a required external prerequisite was silently treated as skip/pass;
- evidence predates a source change and was not invalidated/rerun.

Classify each as protected, release blocker requiring correction, Terra High structural remediation, or planning-needed product issue.

P4 should not implement behavior-bearing repairs in-place and still green the same run. Emit the appropriate remediation and require a new committed/deployed candidate + full rerun.

# Pass 3 — Code-quality / structural review

Inspect Phase 7 diff and important consumers for:

- duplicated release tooling vs reference deployment validator;
- SSH/client/dispatcher logic spread across unrelated modules;
- test-only remote/fault helpers leaking into production build/runtime;
- general command runner hidden behind a nominal allowlist;
- provider/HostGator-specific branches in generic PHP;
- adapter-side Profile/filter/order/cursor/revision semantics;
- visitor upstream fallback;
- duplicate PHP state/usability/escaping/link ownership;
- deployment scripts smuggling self-host packaging into 2.0;
- secret-bearing config/report abstractions;
- brittle timing/path/cleanup behavior;
- duplicated production migration/recovery logic;
- dead compatibility shims/speculative abstractions;
- privilege templates broader than required;
- inadequate tests for remote injection/arming/redaction/failure cleanup;
- documentation/task/evidence wording that overclaims provider/external/production support.

Small artifact wording cleanup is fine. Meaningful behavior-preserving structural refactor triggers the special handoff below.

## Terra High refactor handoff

If any pass finds a meaningful behavior-preserving structural refactor — responsibility movement across modules/layers, duplicated shared logic consolidation, abstraction replacement/collapse, shared state/data-flow reorganization, transaction-boundary change, remote privilege-boundary redesign, or another multi-file simplification whose safety requires producer/consumer tracing — STOP and output exactly one self-contained **Terra High refactor remediation prompt**.

The prompt must:

- recommend exactly `Terra High`;
- state observed quality problem and concrete evidence;
- define smallest behavior-preserving boundary/files/producers/consumers;
- preserve Source trust/network safety, idempotency/provenance, Profile/canonical eligibility, machine/admin separation, cursor/revision/rate behavior, sync/LKG atomicity, local usability, direct links, secret safety, production data, failure isolation, restricted SSH/arming/evidence identity where implicated;
- forbid unrelated cleanup/new product behavior/contracts/ADRs/roadmap changes;
- require focused tests plus invalidated broader suites;
- require a new runner-created remediation commit, operator deployment to required hosts, and a complete P4 rerun afterward;
- preserve assigned Phase 7 release numbering rather than inventing 1.8.x.

Do not implement the structural refactor inside this closeout run.

## Final automated matrix

Against the unchanged P3 behavior source tree / P4 closeout worktree as applicable, run:

```text
npm install
npm run check
npm run test:db
npm run test:security
npm run test:recovery
npm run test:browser
npm run test:live-sources
npm run codex:phase:validate -- p1-7
git diff --check <exact-pre-P1-base>...HEAD
```

Run P1/P2/P3 specialized deterministic remote/shared-PHP/external qualification commands not contained by these aggregates.

Then execute the complete managed/external runtime procedures above through restricted aliases/operator evidence only.

If any local behavior-bearing file changes after runtime evidence, stop; do not reuse the evidence.

## Durable validation artifact

Create/update exactly:

```text
docs/validation/phase-7-managed-integration-and-2.0-release-qualification.md
```

Record at minimum:

- exact pre-P1 SHA;
- P1/P2/P3 SHAs;
- pre-closeout P3 HEAD;
- version sequence `1.7.0 -> 1.7.4`;
- exact behavior candidate identity used by managed/external runtime evidence;
- final P4 candidate-tree identity described below;
- Node/npm/PHP/PostgreSQL/browser/OS/environment versions relevant to evidence;
- exact deterministic commands/results/evidence levels;
- managed validator protocol/bundle identity and safe operation list used;
- confirmation that no unrestricted SSH/direct-host/deploy/root fallback was used;
- arming/resource identities in non-secret form and restoration result;
- managed deployment/database/migration/backup/restore facts;
- approved live Source observation;
- Profile pagination/revision/auth/rate/disable evidence;
- reference/perimeter/telemetry evidence;
- P1 same-VPS preflight and explicit limitation;
- genuine external host classification and distinct-host proof;
- external installation identity, real sync/LKG/SSR/direct-link/local-only evidence;
- each failure case and real-managed vs fault-origin classification;
- final restoration state;
- closeout Pass 1/2/3 findings;
- any remediation/limitations/operator-provided evidence provenance.

Never include bearer plaintext, Authorization headers, DB URLs, SSH private/public keys, known-host material, cookies, private config contents, raw backups/payloads, or sensitive environment dumps.

## Candidate tree identity / runner-owned P4 commit

The phase runner owns the actual P4 implementation commit, so this agent cannot truthfully know that final commit SHA before returning.

Do not invent it.

After all allowed P4 changes are complete, compute the exact Git **candidate tree SHA** non-destructively using a temporary index or equivalent, including every intended P4 file.

Record in the artifact/final response:

- pre-closeout P3 HEAD;
- exact candidate tree SHA;
- exact P4 file list;
- required direct-successor commit subject/version `1.7.4`;
- statement that runtime Level 7/8 evidence applies to the recorded P3 behavior candidate and P4 changed no behavior-bearing runtime source.

If a reliable candidate tree identity cannot be produced, result is BLOCKED.

After the runner creates the `1.7.4` commit, human review must verify:

- its direct parent is the recorded P3 HEAD;
- its Git tree SHA equals the recorded candidate tree SHA;
- package version is `1.7.4`;
- no unexpected file entered the commit.

Only then may the terminal conversational `/closeout` bind the actual accepted `1.7.4` commit and perform the version-only `2.0.0` transition.

## Version/repository rules

- P4 may set only top-level `package.json` version to `1.7.4` as the version change.
- No `package-lock.json`.
- No private SSH/deployment/external config, credentials, LKG, arm state, backups/dumps, or secret-bearing evidence tracked.
- Do not edit governing contracts/ADRs/roadmap merely to make evidence fit.
- Do not rewrite accepted migrations.
- Do not manually commit; runner owns the P4 commit.
- Do not run `/closeout` or set `2.0.0`.

## Acceptance / final response

Return **GREEN — HUMAN REVIEW REQUIRED** only if:

1. exact P3 committed behavior candidate is deployed on managed host and installed on genuinely external PHP host;
2. restricted managed/external transport/operator evidence boundaries are verified and no unrestricted SSH fallback occurred;
3. managed mutation operations were correctly armed/scoped/restored;
4. complete required deterministic matrix is green;
5. actual Level 7 approved live Source evidence is present;
6. actual Level 8 managed + genuinely external PHP end-to-end evidence is present;
7. production data/migration/backup/recovery obligations are satisfied;
8. pagination/auth/rate/revision/disable/reference/perimeter/telemetry behavior is correct;
9. external LKG/SSR/direct-link/local-only/failure matrix is correct;
10. final managed/external state is restored and secret-safe;
11. Pass 2 finds no unresolved edge-case release blocker;
12. Pass 3 finds no unresolved structural issue requiring remediation;
13. no behavior-bearing source changed after final runtime evidence;
14. durable artifact is complete/honest;
15. package version is `1.7.4`;
16. candidate tree SHA/direct-parent handoff is available for human verification.

Otherwise return BLOCKED or the required remediation/planning handoff. Never downgrade missing real evidence to a skip/pass.

Finish with a concise closeout report containing pre-P1/P1/P2/P3 identities, managed/external behavior candidate identity, remote protocol/arming status, final command/procedure results, evidence levels, failure-matrix/restoration result, validation artifact path, candidate tree SHA/P4 file list, package version, explicit limitations, and the exact human checks required after the runner creates the `1.7.4` commit.