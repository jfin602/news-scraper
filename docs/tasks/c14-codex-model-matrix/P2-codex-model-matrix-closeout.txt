TASK: Correction 14 / P2 — Codex model matrix closeout

CORRECTION SCOPE
Final manual closeout for the non-versioned Codex model-matrix/tooling correction. Validate the exact P1 implementation, repair only bounded defects in the already-approved runner/model-policy change, and leave Phase 14/product behavior untouched.

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.
- Model-family basis: this closeout is bounded tooling verification over explicit runner/parser/test/documentation requirements and must remain executable by the pre-P1 parser metadata.
- Reasoning basis: High is sufficient for exact fail-closed runner verification and bounded defect triage.
- Complexity / quality floor: `Elevated`.
- Estimated usage: `Moderate`.
- Lower-cost alternative considered: none among the pre-correction executable runner labels; do not use a new Luna/Medium label in this stack's own metadata.
- Escalation trigger: a discovered defect requires redesign of runner execution/version/commit semantics rather than a bounded model-matrix/preflight correction.
- Escalation target: `Terra Ultra`.
- Estimate confidence: `High`.
- Efficiency rationale: validation is explicit and should stop on architectural drift instead of spending a stronger model to redesign unrelated runner behavior.

VERSIONING
- Required unchanged project version: `0.14.7`.
- This closeout MUST NOT advance the roadmap or change `package.json` version.
- Do not create `package-lock.json` or `npm-shrinkwrap.json`.

MANUAL CLOSEOUT BOUNDARY
This is the sole final manual closeout prompt for correction `c14-codex-model-matrix`. `npm run codex:phase -- c14-codex-model-matrix` must execute P1 only and stop before P2.

MANDATORY HANDOFF
Before any write:
1. read current `BOOT.md` and `docs/codex-model-selection.md`;
2. read P1 and inspect the actual P1 commit/source rather than assuming its final report is correct;
3. inspect `scripts/codex-phase-core.mjs`, `scripts/codex-phase.mjs`, `scripts/validate-codex-phase.mjs`, `test/unit/codex-phase-runner.test.ts`, and `test/unit/codex-phase-prompt-grammar.test.ts`;
4. inspect `AGENTS.md`, `docs/README.md`, and `docs/design/ui-workflow.md` for policy consistency;
5. inspect the current Phase 14 prompt folder and confirm its existing recommendation labels still parse under the new matrix;
6. confirm `package.json` is exactly `0.14.7` and stop on version drift.

CLOSEOUT GOAL
Prove the runner now supports the intended economical Luna/Terra/Sol matrix, preserves all previous runner safety semantics, rejects incompatible GPT-5.6 Codex CLI versions before execution artifacts/processes, and leaves documentation consistent with the executable matrix.

EXACT MODEL MATRIX EVIDENCE
Verify source/tests establish exactly:
- `Luna Low` → `gpt-5.6-luna` / `low`
- `Luna Medium` → `gpt-5.6-luna` / `medium`
- `Luna High` → `gpt-5.6-luna` / `high`
- `Terra Medium` → `gpt-5.6-terra` / `medium`
- `Terra High` → `gpt-5.6-terra` / `high`
- `Terra Ultra` → `gpt-5.6-terra` / `ultra`
- `Sol Light` → `gpt-5.6-sol` / `low`
- `Sol Medium` → `gpt-5.6-sol` / `medium`
- `Sol High` → `gpt-5.6-sol` / `high`
- `Sol Ultra` → `gpt-5.6-sol` / `ultra`

Confirm there is no `Luna Ultra`, `Sol Low` alias, `Terra Max`, or other speculative alias.

CLI PREFLIGHT EVIDENCE
Verify:
- GPT-5.6 execution requires Codex CLI >= `0.144.0`;
- normal version output is parsed numerically;
- `0.144.0` and newer pass;
- `0.143.9` and older fail;
- malformed/unparseable version output fails closed;
- failure is actionable and names required/observed version when available;
- incompatible version fails before `.codex-runs/<folder>/<stamp>` creation and before `runCodexProcess`/Codex execution;
- launcher resolution itself remains shell-free and unchanged except for consuming the returned version in the compatibility gate;
- no extra model/network availability probe was added.

ARGUMENT / PARSER EVIDENCE
Verify:
- a Luna Medium prompt builds arguments with exact model `gpt-5.6-luna` and reasoning `medium`;
- an existing Ultra prompt still emits the repository's established `ultra` value;
- every supported recommendation parses;
- unknown recommendation still fails closed;
- phase/correction grammar, closeout classification, version parsing, history resume, commit ownership, and parse-only validator behavior remain intact.

DOCUMENTATION EVIDENCE
Confirm:
- `BOOT.md` routes detailed model selection to `docs/codex-model-selection.md` and accurately describes the live runner matrix;
- `/prompt-ass` now makes a provisional family + reasoning decision from explicit task classifications;
- `/prompt-plan` is the final model gate and reports `Downgraded` / `Unchanged` / `Escalated` using observed source evidence;
- `/prompt-write` consumes the finalized plan decision, validates current `MODEL_CONFIGS`, and does not speculate upward because a prompt is long/important/test-heavy;
- `docs/codex-model-selection.md` no longer describes the matrix as pending;
- `AGENTS.md`, `docs/README.md`, and `docs/design/ui-workflow.md` are consistent with the minimum-cost-adequate policy;
- mutable OpenAI credit numbers are not hard-coded as durable selection policy.

BOUNDED DEFECT HANDLING
P2 may fix only defects directly preventing the approved correction from satisfying the above requirements, such as:
- one missing/incorrect mapping;
- one version-comparison boundary bug;
- one stale runner test expectation;
- one stale workflow-doc statement;
- one validator/parser regression caused by the matrix change.

For any bounded fix, add/adjust focused regression coverage where practical and rerun every broader suite invalidated by that fix.

STOP and report planning need if repair requires:
- runner commit/resume/version architecture redesign;
- replacing launcher discovery;
- changing task-stack grammar beyond model labels;
- adding runtime remote model/pricing discovery;
- changing Phase 14/product behavior;
- changing package version.

REQUIRED VALIDATION
Run on the exact final correction tree:
- `npm run format:check`
- `npm run lint`
- `npm run typecheck`
- focused `test/unit/codex-phase-runner.test.ts`
- focused `test/unit/codex-phase-prompt-grammar.test.ts`
- `npm run test:unit`
- `npm test`
- `npm run check`
- `npm run codex:phase:validate -- c14-codex-model-matrix`
- `npm run codex:phase:validate -- p14`
- `git diff --check`

Also run `codex --version` in the actual local execution environment when the CLI is available and record the observed version. If the installed CLI is below `0.144.0`, the repository implementation may still be correct but this local environment is not ready to execute GPT-5.6 prompts; report that operational blocker explicitly rather than weakening the preflight. If the CLI is unavailable, report that this environment-level compatibility observation was not made; do not substitute a fake invocation.

Do NOT launch a paid/model task merely to prove Luna availability during closeout. Unit/argument/parser evidence plus the official model slug/minimum-version contract is sufficient for this tooling correction; actual future task execution will fail visibly if account/model availability differs.

FINAL STATE CHECK
Confirm:
- `package.json` remains `0.14.7`;
- no npm lockfile exists;
- correction changes are limited to runner/tooling tests and workflow docs;
- current Phase 14 task folder remains parser-valid;
- no Phase 14 implementation prompt has been executed or version consumed by this correction.

FINAL REPORT
Report:
- closeout result `GREEN` or blocked;
- exact final source SHA/tree reviewed;
- final supported model matrix;
- CLI minimum-version behavior and actual local `codex --version` observation if available;
- commands run and results;
- any bounded fixes made during closeout;
- confirmation that package version remains `0.14.7`;
- next action if GREEN: revalidate unexecuted Phase 14 model ratings against the now-live Luna/Terra/Sol matrix before running Phase 14 implementation.
