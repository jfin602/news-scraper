TASK: Phase 21 / P10 — Remove residual compatibility and dead exports

ROADMAP PHASE
Phase 21 — Codebase simplification and maintainability hardening.

MODEL / REASONING / USAGE
- Recommended configuration: `Terra Medium`.
- Model-family basis: this is a bounded source-compatibility cleanup across parser type imports plus deletion of one unreferenced deduplication barrel.
- Reasoning basis: Medium reasoning is sufficient because the canonical replacements are already explicit and no runtime behavior or ownership boundary should change.
- Complexity / quality floor: `Elevated`.
- Estimated usage: `Low`.
- Lower-cost alternative considered: `Luna High`; it would normally be adequate, but Phase 21 currently uses the owner-set Terra-or-higher implementation floor.
- Escalation trigger: repository search reveals the deprecated alias or deduplication barrel is part of a real external/runtime contract rather than internal compatibility residue.
- Escalation target: `Terra High`; stop and report before preserving a compatibility bridge speculatively.
- Estimate confidence: `High`.
- Efficiency rationale: perform exact search-driven replacement/deletion and rely on existing parser/collector regressions instead of creating new abstractions.

VERSIONING
- This is Phase 21 prompt 10 and its assigned project version is `0.21.10`.
- On first execution require top-level `package.json` at `0.21.9`; update only that version to `0.21.10` as part of this implementation prompt.
- On rerun, `0.21.10` is acceptable; do not increment again.
- Any other package version is sequence drift and must stop execution.
- Do not create `package-lock.json` or `npm-shrinkwrap.json`.

GOAL
Remove two remaining pieces of pre-1.0 compatibility/dead-export surface without changing parser, collection, deduplication, persistence, or public behavior:
1. replace deprecated `FeedParser` type usage with canonical `CollectionParser` and delete the alias;
2. delete the unreferenced `src/deduplication/index.ts` barrel rather than keeping a compatibility export path with no consumer.

PART A — PARSER TYPE CANONICALIZATION
Current canonical parser contract in `src/collection/parsers/parser.ts` is `CollectionParser`.

The same file retains:
`export type FeedParser = CollectionParser;`
with a deprecated source-compatibility comment.

That alias is pre-1.0 internal compatibility debt. Replace active references with `CollectionParser` directly.

Expected consumers include:
- `src/collection/parsers/rss-atom-parser.ts`
- `src/collection/collect-endpoint.ts`
- `test/integration/collect-endpoint.test.ts`
- relevant collection/database tests that import or annotate `FeedParser`.

Use repository-wide search rather than assuming this list is exhaustive.

IMPLEMENTATION REQUIREMENTS — PARSER
- `RssAtomParser` should implement `CollectionParser` directly.
- `CollectEndpointDependencies.rssAtomParser` should be typed as `CollectionParser` while retaining the meaningful property name `rssAtomParser`.
- test parser fixtures/adapters should use `CollectionParser` where an interface type is needed.
- delete the deprecated `FeedParser` alias and its compatibility comment.
- do not rename `RssAtomParser`, `rssAtomParser`, RSS/Atom dialect types, parser adapter identities, parser result types, or endpoint types merely for naming symmetry.
- do not change parser input limits, diagnostics, XML behavior, HTML parser behavior, Raw-item shape, normalization, admission filtering, or run accounting.
- do not add another alias/re-export to preserve the removed name.

PART B — DEAD DEDUPLICATION BARREL
`src/deduplication/index.ts` only re-exports the specific deduplication modules. The Stage-0 reassessment found no active runtime/test consumer of that barrel; current code imports the owned modules directly.

Before deletion, search the repository for imports resolving through:
- `src/deduplication/index.ts`;
- `deduplication/index`;
- a directory-level `deduplication` import that Node/TypeScript resolution could map to the barrel.

If no real consumer exists, delete:
- `src/deduplication/index.ts`

Do not consolidate, rename, or move the actual deduplication implementation modules:
- evidence;
- grouping;
- moderation;
- primary;
- repository.

If a real consumer is found, change that internal consumer to its actual specific module when ownership is obvious. If the barrel is revealed to be an intentional documented external contract, stop and report instead of deleting it under this prompt.

STRUCTURAL PROOF
At completion, repository search must establish:
- zero active `FeedParser` references;
- `CollectionParser` is the one parser interface authority;
- no import depends on `src/deduplication/index.ts`;
- `src/deduplication/index.ts` no longer exists;
- no replacement compatibility alias/barrel was introduced.

PRESERVED INVARIANTS
- RSS/Atom and HTML parser runtime behavior unchanged;
- Raw-item normalization and Source RSS/Atom admission filtering unchanged;
- collector parser dispatch and run/parser diagnostics unchanged;
- duplicate detection/grouping/review/Primary behavior unchanged;
- Article persistence/idempotency/provenance unchanged;
- public/admin behavior unchanged;
- no schema/migration/dependency/product changes.

FOCUSED VALIDATION
During iteration run the directly affected parser/collector tests and any focused DB test whose imports were changed. Prefer the narrowest existing commands/files supported by the repository test runner.

FINAL VALIDATION
Run on the final unchanged tree:
- `npm run check`
- `git diff --check <P10-base-sha>...HEAD`

Do not rerun the entire DB/browser/security/recovery/live matrix solely for type-alias/barrel deletion when P9 already validated the collection security/database boundary and P11 will validate the final integrated tree. If implementation unexpectedly changes runtime semantics, stop and expand validation honestly.

NON-GOALS
- no parser redesign;
- no deduplication redesign;
- no file/module count target beyond the two evidence-backed residues;
- no generic barrel policy across the repository;
- no compatibility bridge replacement;
- no schema/migration/dependency/product change.

ACCEPTANCE
P10 is complete only when `FeedParser` is fully removed in favor of `CollectionParser`, the dead deduplication barrel is gone, runtime semantics remain unchanged, final static/regression validation is green, and top-level version is exactly `0.21.10`.

FINAL RESPONSE
Report:
- all active `FeedParser` consumers replaced;
- deduplication-barrel search result and deletion;
- files changed/deleted;
- focused/final commands actually run and observed results;
- any unexpected compatibility contract discovered;
- final package version and commit-ready status.
