TASK: Phase 21 / P1 — Retire pre-production compatibility test debt

ROADMAP PHASE
Phase 21 — Codebase simplification and maintainability hardening.

MODEL / REASONING / USAGE
- Recommended configuration: `Terra Medium`.
- Model-family basis: this is a bounded test-ownership cleanup, but it touches migration/status/recovery evidence immediately after the first supported production baseline, so Terra is the requested Phase 21 floor and provides stronger repository-wide judgment than a mechanical test deletion.
- Reasoning basis: Medium reasoning is sufficient because the supported compatibility boundary is explicit and the active test to retire is clearly pre-production; the main work is proving that current migration/status/recovery coverage remains complete.
- Complexity / quality floor: `Elevated`.
- Estimated usage: `Low`.
- Lower-cost alternative considered: `Luna High`; it would ordinarily be adequate for a bounded test cleanup, but the repository owner explicitly set Terra-or-higher as the Phase 21 implementation floor before the 1.0 release gate.
- Escalation trigger: source inspection shows the pre-launch procedure test uniquely protects a still-supported production upgrade/recovery invariant or exposes ambiguity in the accepted Phase 20 baseline.
- Escalation target: `Terra High`; stop for replanning before changing production compatibility policy or migration history.
- Estimate confidence: `High`.
- Efficiency rationale: remove one obsolete expensive real-PostgreSQL compatibility procedure first so later Phase 21 validation does not repeatedly pay for unsupported pre-production history.
- Plan delta: `Escalated` from the provisional assessment to the owner-requested Terra floor; task boundary is unchanged.

VERSIONING
- This is Phase 21 prompt 1 and its assigned project version is `0.21.1`.
- On first execution require top-level `package.json` version `0.21.0`, then update only that version to `0.21.1`.
- On rerun of this same P1 retain `0.21.1`; any other version is sequence drift and must stop execution.
- Do not create `package-lock.json` or `npm-shrinkwrap.json`.

MANDATORY HANDOFF
Before editing:
1. read current `BOOT.md`, `AGENTS.md`, the complete Phase 21 roadmap entry, `docs/codex-model-selection.md`, `docs/contracts/testing-and-validation-contract.md`, and `docs/decisions/production-data-and-schema-compatibility.md`;
2. read `docs/validation/phase-20-customer-launch-validation.md` and identify the exact accepted production schema baseline;
3. inspect `test/database/prelaunch-schema-upgrade.test.ts`, current migration/status database tests, `test/recovery/`, `scripts/database-status.ts`, migration discovery/status implementation, and backup/restore tooling;
4. inspect current `package.json` test scripts and `scripts/run-tests.mjs` so timing/coverage claims use the actual executable test matrix;
5. confirm the active pre-launch procedure reconstructs unsupported pre-Phase-20 state rather than a supported post-launch upgrade input;
6. stop and report `Planning needed` if removing it would eliminate unique required coverage for the accepted Phase 20 production baseline or if repository state materially differs from this plan.

GOAL
Remove active test/support code whose only purpose is to prove the old pre-production `0013 -> 0014` launch-upgrade exercise, while preserving current migration integrity, database-status, migration-from-zero, and current backup/restore protection.

CURRENT EVIDENCE BOUNDARY
Phase 20 accepted the deployed schema at migration `0014` as the first supported production baseline. Earlier pre-production states do not become supported upgrade inputs merely because they were exercised before launch.

Historical evidence under `docs/validation/` remains historical and MUST NOT be rewritten. The active regression suite should protect currently supported behavior rather than repeatedly reconstructing an unsupported `0.19.6`/migration-0013 database.

IMPLEMENTATION BOUNDARY
- Remove the pre-launch `0013 -> 0014` upgrade/restore procedure and helpers/fixtures that exist only for that unsupported state.
- Preserve any still-current database-status CLI regression from that file by moving/renaming it into a current-purpose database test when necessary.
- Prefer deleting obsolete setup code rather than leaving compatibility wrappers or renamed historical fixtures.
- Do not alter migrations `0001` through `0014`, their checksums/order, production schema, backup/restore semantics, or database-status behavior merely to simplify the tests.
- Do not create a new migration.

NORMAL FILE SCOPE
Expected changes are limited to:
- `package.json` version;
- `test/database/prelaunch-schema-upgrade.test.ts` removal or reduction;
- a narrowly named current database-status/migration test file if needed;
- directly test-only support that becomes unreachable solely because the obsolete procedure is removed.

If production source, migration SQL, recovery scripts, or compatibility policy appear to require changes, stop and replan rather than broadening P1.

MEASUREMENT
Before deleting the obsolete procedure, record a same-machine baseline for the full `npm run test:db` wall-clock duration and test count under the current P1 environment. Use the runner/test summary plus an external elapsed measurement when practical; do not add permanent benchmarking infrastructure merely for this timing.

After the final P1 tree is ready, run the same command under materially comparable conditions and record:
- before and after test counts;
- before and after elapsed time;
- environment facts needed to interpret the comparison;
- limitations/noise.

The final P1 response MUST include this measurement because the phase runner preserves that response in the P1 commit body for terminal Phase 21 closeout evidence. Do not claim a speedup if the measurement does not show one; removal is still justified by compatibility ownership even if wall-clock noise obscures improvement.

PRESERVE
Keep green/current protection for:
- migration discovery and ordered execution from zero;
- applied-migration checksum/history incompatibility detection;
- pending/current/incompatible database-status behavior;
- rollback on migration failure and migration serialization where already covered;
- current native backup/restore recovery behavior;
- accepted production migration history `0001` through `0014` remaining immutable and upgradeable for future post-baseline migrations.

FOCUSED DEVELOPMENT VALIDATION
During iteration run only the directly affected database-status/migration tests with `npm run test:db:focused -- <selected test files>` where real PostgreSQL is required. Use narrower non-DB tests only when they directly cover a changed test helper.

FINAL-TREE VALIDATION FOR THIS PROMPT
Run the smallest non-overlapping final set:
- `npm run check`;
- `npm run test:db`;
- `npm run test:recovery`;
- committed-range `git diff --check` for the P1 change.

Do not separately rerun unit/integration/collection suites after `npm run check`; they are contained by the current aggregate.

ACCEPTANCE
P1 is complete only when:
- no active test reconstructs pre-Phase-20 migration `0013` solely to prove the old `0013 -> 0014` launch procedure;
- current database-status/migration integrity behavior remains directly covered;
- current backup/restore coverage remains intact;
- no supported migration history or production data contract changed;
- before/after DB-suite measurement is truthfully reported;
- package version is `0.21.1` and the final tree passes the required evidence.

NON-GOALS
Do not optimize the broader database suite here, change database architecture, squash migrations, add schema changes, alter production compatibility, or perform unrelated test cleanup.

FINAL REPORT
Report:
- obsolete files/helpers removed and any current test relocated;
- why no supported compatibility was lost;
- focused and final validation actually run;
- before/after `test:db` test-count/timing measurement with limitations;
- package version confirmation `0.21.1`;
- any remaining concern that should return to Phase 21 planning rather than be hidden in P1.