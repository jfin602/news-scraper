TASK: Phase 21 / P9 — Retire obsolete pretransport boundary and test surface

ROADMAP PHASE
Phase 21 — Codebase simplification and maintainability hardening.

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.
- Model-family basis: this removes a historical security-sensitive test abstraction while preserving the canonical production eligibility, endpoint-lock, destination-safety, redirect, and transport path.
- Reasoning basis: High reasoning is justified because the task must distinguish obsolete Phase-4 composition from still-live production lock/eligibility primitives and migrate only uniquely valuable assertions into current owners.
- Complexity / quality floor: `High`.
- Estimated usage: `Moderate`.
- Lower-cost alternative considered: `Terra Medium`; rejected because deletion crosses integration/database/security evidence and an over-broad cleanup could weaken SSRF or endpoint-lock coverage while appearing mechanically correct.
- Escalation trigger: source inspection shows `reachValidatedOutboundBoundary` has a hidden production consumer, or preserving unique evidence requires redesigning transaction/lock/network-safety ownership rather than migrating tests.
- Escalation target: `Terra Ultra` for materially harder explicit concurrency/security-state reasoning; otherwise stop and report rather than redesigning the collection path.
- Estimate confidence: `High`.
- Efficiency rationale: delete the obsolete adapter and duplicate composite tests, reuse modern canonical suites, and add only missing assertions.

VERSIONING
- This is Phase 21 prompt 9 and its assigned project version is `0.21.9`.
- On first execution require top-level `package.json` at `0.21.8`; update only that version to `0.21.9` as part of this implementation prompt.
- On rerun, `0.21.9` is acceptable; do not increment again.
- Any other package version is sequence drift and must stop execution.
- Do not create `package-lock.json` or `npm-shrinkwrap.json`.

IMPORTANT PLANNING CORRECTION
The Stage-0 reassessment originally described both `reachValidatedOutboundBoundary` and `withEligibleEndpointExecution` as obsolete. Deep `/prompt-plan` source tracing disproved the latter.

`collectEndpoint()` still uses `withEligibleEndpointExecution()` as the production eligibility-before-lock boundary, and `endpoint-collection-service.ts` supplies the real `createEndpointExecutionLockRunner(database)`.

Therefore:
- KEEP `withEligibleEndpointExecution`;
- KEEP `createEndpointExecutionLockRunner`;
- KEEP `src/collection/execution.ts` as the canonical eligibility/endpoint-lock composition owner unless a tiny test-support import cleanup is needed;
- remove only the actually obsolete outbound adapter/test-era composition described below.

GOAL
Remove the dead `reachValidatedOutboundBoundary` production surface and retire historical pretransport tests that model an outdated synthetic composition, while preserving every meaningful approval/eligibility/endpoint-lock/SSRF/per-hop-safety assertion against the current production owners.

CANONICAL RUNTIME OWNERSHIP TO PRESERVE
The intended current flow remains:

`endpoint configuration eligibility -> endpoint execution lock -> collectEndpoint run/fetch -> HttpFetcher initial destination safety -> transport -> fresh redirect destination safety per hop -> downstream parse/normalize/persist`

Preserve the distinction between:
- eligibility/lock ownership in `src/collection/execution.ts`;
- collector orchestration in `src/collection/collect-endpoint.ts`;
- initial + redirect network safety in the HTTP fetcher/destination-safety layer;
- one-hop transport in the HTTP transport layer.

Do not collapse these responsibilities merely to make the call graph shorter.

PRIMARY REMOVAL CANDIDATES
Expected deletion:
- `src/collection/safety/outbound-boundary.ts`
- `test/integration/collection-pretransport.test.ts`
- `test/database/collection-pretransport.test.ts`

Potential focused test updates, only as needed to preserve unique evidence:
- `test/integration/collect-endpoint.test.ts`
- `test/database/collection-execution.test.ts`
- `test/collection/http-fetcher.test.ts`
- another existing canonical destination-safety/eligibility test if it is the clearer owner.

DISCOVERY GATE BEFORE DELETION
Before editing:
1. search the current production/test tree for every import/call of `reachValidatedOutboundBoundary` and confirm no real runtime consumer remains;
2. search every import/call of `withEligibleEndpointExecution` and `createEndpointExecutionLockRunner` and confirm the canonical collector/service usage remains;
3. enumerate each assertion in the two pretransport suites and map it to existing modern proof or a genuinely missing assertion;
4. do not delete a unique security/concurrency guarantee simply because a newer suite has a similar name.

CURRENT EVIDENCE THAT SHOULD BE REUSED
The modern HTTP-fetcher suite already owns per-hop network behavior such as:
- unsafe initial destination blocking before transport;
- validated address handoff;
- fresh validation on redirects;
- malformed/unapproved/credential-bearing/non-default-port/unsupported-scheme redirect rejection;
- redirect DNS/unsafe-address rejection;
- same-host redirect DNS re-resolution rather than address reuse;
- redirect limits/loops/deadlines/terminal failures.

The canonical database collection suite already owns real PostgreSQL behavior including:
- same-endpoint lock contention preventing fetch;
- independent endpoints proceeding separately;
- run creation/accounting;
- terminal failure and later recovery behavior.

The canonical collector integration suite already owns the orchestration order including eligibility, lock, run start, fetch, parse, finalization, and release.

Do not copy old tests wholesale into new filenames.

UNIQUE ASSERTIONS TO PRESERVE IF NOT ALREADY COVERED
At minimum verify the following and add the smallest canonical regression only when current coverage is insufficient:

1. Lock infrastructure exception
- an exception from the lock runner must not start a Collection run or fetch;
- it must propagate as an infrastructure failure rather than being converted into an eligibility decision.

2. Lock release after unexpected fetch-boundary failure
- when an injected fetcher throws unexpectedly, the collector's bounded terminal handling must finish/release the endpoint lock;
- a later actor must be able to acquire the same endpoint lock and collect;
- preserve truthful run/error behavior expected by the current collector rather than the old outbound-callback semantics.

3. Initial DNS-empty/no-address safety
- if this exact initial-destination case is not already covered by current destination-safety/http-fetcher tests, prove it there;
- transport must not start.

4. Eligibility state coverage
- preserve the existing dedicated eligibility unit/integration coverage; do not keep the old composite pretransport suite solely to duplicate those state cases.

5. Lock contention before network work
- preserve canonical proof that contention stops fetch/network work before transport. Prefer the current collector/database owner rather than a synthetic `reachValidatedOutboundBoundary` callback.

IMPLEMENTATION REQUIREMENTS
- Delete `reachValidatedOutboundBoundary` and its associated exported result type(s) when no active consumer remains.
- Remove obsolete imports/types/helpers used only by that adapter.
- Delete the old pretransport test files after unique evidence has been migrated or proven redundant.
- Keep `validateInitialDestination` / `validateRedirectDestination` / current destination-safety primitives used by the HTTP fetcher.
- Keep endpoint lock runner and lock implementation behavior.
- Keep controlled no-network testing through the current fetcher/transport seams; do not create a new general-purpose outbound callback abstraction.
- Do not add compatibility re-exports or a replacement wrapper under another name.

PRESERVED INVARIANTS
- collection eligibility must run before endpoint lock acquisition;
- endpoint contention must prevent Collection-run/fetch work for the blocked contender;
- endpoint locks must release after success and failure;
- every initial request and redirect hop must pass current approval/domain/DNS/address/port/SSRF safety before target contact;
- redirect validation must use fresh destination safety and never trust/reuse a prior safe answer as authority for a new hop;
- network safety blocks remain bounded collection outcomes where the canonical collector defines them;
- source failure isolation, run accounting, retry classification, provenance, Article pipeline behavior, and Worker/service composition remain unchanged;
- no schema/migration/product behavior changes.

FOCUSED VALIDATION
During implementation run the narrowest directly affected tests first, including the modern HTTP-fetcher/destination-safety and collector/lock tests that receive migrated assertions.

FINAL VALIDATION
Run on the final unchanged tree:
- `npm run check`
- `npm run test:db`
- `npm run test:security`
- `git diff --check <P9-base-sha>...HEAD`

Do not add browser/live-Source validation to this prompt unless the implementation unexpectedly crosses those boundaries. P11 will perform the full final-tree specialized matrix including approved live Sources.

NON-GOALS
- do not delete or inline `withEligibleEndpointExecution`;
- do not delete or inline `createEndpointExecutionLockRunner`;
- do not merge eligibility/locking with network safety;
- do not redesign the HTTP fetcher/transport architecture;
- do not weaken SSRF/domain/DNS/redirect protections;
- do not add new network capabilities;
- do not change schema, migrations, or production-data representation.

ACCEPTANCE
P9 is complete only when the obsolete outbound-boundary module and historical composite pretransport suites are gone, every uniquely valuable security/lock assertion is either proven by an existing canonical suite or migrated to its current owner, no modern production lock/safety boundary is weakened, the relevant final suites are green, and top-level version is exactly `0.21.9`.

FINAL RESPONSE
Report:
- deleted files and any focused test files changed;
- the assertion-mapping result for the old pretransport tests;
- which unique assertions were migrated and where;
- confirmation that `withEligibleEndpointExecution` and `createEndpointExecutionLockRunner` remain canonical production behavior;
- focused/final commands actually run and observed results;
- any security/concurrency limitation or unresolved evidence gap;
- final package version and commit-ready status.
