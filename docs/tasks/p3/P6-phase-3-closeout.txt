TASK: Phase 3 / P6 — Final-tree configuration validation and Phase 3 closeout evidence

ROADMAP PHASE
Phase 3 — Publication and Source configuration core.

VERSIONING
- This is Phase 3 prompt 6 and its assigned project version is `0.3.6`.
- On first execution confirm P5 left `package.json` at `0.3.5`, then advance it to `0.3.6` and synchronize npm-generated lockfile package metadata BEFORE establishing the final validated source SHA.
- If this same P6 prompt is rerun after failed/partial closeout and `package.json` is already `0.3.6`, retain `0.3.6`; do not increment again.
- `package.json` remains the sole authoritative project-version source.
- Do not duplicate the current version in README, BOOT, source constants, or manually maintained docs.

DEPENDS ON
- P1 relational Publication/Source/endpoint/domain-policy schema implemented and reviewed.
- P2 pure configuration normalization/validation implemented and reviewed.
- P3 persistence/read boundary implemented and reviewed.
- P4 generic transactional create-if-absent bootstrap engine/CLI implemented and reviewed.
- P5 owner-approved initial Publication configuration + `db:bootstrap` command implemented and reviewed.
- All Phase 3 P1-P5 runtime/source/test/config changes committed.
- Safe local PostgreSQL prerequisites exist for Level 4 evidence: a dedicated `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL` capable of disposable database lifecycle; application DB commands used in destructive validation must target disposable databases explicitly.

GOAL
Advance the final Phase 3 prompt version, execute the complete Phase 3 static/deterministic/real-PostgreSQL/operator-command validation matrix against one exact committed implementation tree, prove the Phase 3 roadmap/testing-contract exit gates, and create the durable Phase 3 validation artifact.

This is a closeout/evidence task, not a feature-development task. Apart from the required `0.3.6` package/lock transition and the successful evidence artifact, do not change runtime behavior.

GOVERNING DOCUMENTS
Read current versions before execution:
- BOOT.md
- AGENTS.md
- docs/contracts/project-contract.md
- docs/contracts/mvp-scope-and-users.md
- docs/contracts/domain-and-data-contract.md
- docs/contracts/source-and-collection-contract.md
- docs/contracts/testing-and-validation-contract.md
- docs/architecture/system-architecture.md
- docs/roadmap/mvp-roadmap.md
- docs/decisions/whitelist-and-structured-feed-first.md
- docs/validation/phase-2-database-foundation.md
- docs/validation/phase-3-source-reconnaissance.md
- docs/validation/phase-3-authors-publish-targeted-reconnaissance.md
- docs/validation/phase-3-source-reconnaissance-decisions.md
- current P1-P5 implementation/config/tests/package scripts

PRESERVED BEHAVIOR / RULES
- Do not advance BOOT/README/AGENTS/roadmap current phase to Phase 4 in this task.
- Do not claim behavior as verified unless it is actually executed at the required evidence level.
- Required database persistence/idempotency/rollback/concurrency claims require real disposable PostgreSQL.
- Required suites may not silently skip because PostgreSQL is unavailable and may not pass with zero selected tests.
- Ordinary deterministic validation remains independent of live public publishers.
- No browser validation is required because Phase 3 introduces no browser behavior.
- No approved-live-Source collection validation is required because Phase 3 does not perform outbound collection.
- Reconnaissance artifacts prove only point-in-time Source-selection observations, not implemented collection.
- Web/API and Worker startup remain non-migrating and non-bootstrapping.
- Bootstrap remains explicit, create-if-absent, idempotent, transactional, and non-overwriting.
- Shared engine/configuration code remains topic independent; publisher/topic-specific data belongs to the committed Publication bootstrap file.
- Never print real database URLs, credentials, `.env` contents, passwords, or secret query parameters in the evidence artifact.

CRITICAL CLOSEOUT RULE
If validation reveals an implementation defect, contract violation, false-green/skipped/hanging test, migration defect, configuration-validation defect, persistence scoping defect, bootstrap overwrite/idempotency/concurrency defect, CLI/env defect, leaked credential, cleanup failure, or other substantive problem:
1. stop Phase 3 closeout;
2. report the exact failed invariant/command/procedure;
3. do not hide or repair runtime/source/test/config behavior inside P6 unless the repository owner explicitly authorizes a separate corrective implementation task;
4. return the defect to the owning P1-P5 boundary;
5. keep assigned version `0.3.6` on a rerun of this same P6 prompt;
6. after the correction is committed, rerun P6 validation from the beginning against the new final source tree.

Do not create an accepted Phase 3 closeout artifact for a failing tree.

SOURCE MODIFICATION SCOPE
Before final-tree validation this task may modify only:
- `package.json` for the required version transition;
- `package-lock.json` as npm's synchronized mechanical mirror.

After the versioned implementation tree is committed and ALL required validation succeeds, create only:
- `docs/validation/phase-3-publication-source-configuration.md`

Do not modify runtime source, migrations, tests, scripts, committed bootstrap config, README, BOOT, AGENTS, contracts, ADRs, roadmap, earlier validation artifacts, or task prompts during successful P6 closeout.

VERSION TRANSITION / VALIDATED SOURCE SHA
Before final validation:
1. inspect `package.json` and lock root package metadata;
2. if version is `0.3.5`, change only package/lock root version metadata to `0.3.6` using normal safe project tooling without creating a git tag;
3. if already `0.3.6` because this P6 prompt is being rerun, do not increment;
4. if another unexpected version exists, stop and report sequence drift;
5. commit the P6 version transition;
6. require a clean working tree;
7. record `VALIDATED_SOURCE_SHA` as the resulting committed implementation/version tree BEFORE creating the evidence artifact;
8. record branch and confirm version exactly `0.3.6` at that SHA.

PHASE 3 BASE / COMMITTED-RANGE VALIDATION
Determine from repository history the explicit accepted Phase 3 `0.3.0` baseline created by `/closeout` before Phase 3 implementation. Do not blindly use a stale hard-coded SHA.

Record:
- `PHASE3_BASE_SHA`;
- `VALIDATED_SOURCE_SHA`.

Documentation/reconnaissance/task-writing commits after the `.0` baseline are allowed history and do not themselves invalidate implementation evidence.

Run committed-range whitespace validation equivalent to:
- `git diff --check PHASE3_BASE_SHA...VALIDATED_SOURCE_SHA`

Do not substitute a clean-working-tree-only diff that might inspect no committed Phase 3 changes.

ENVIRONMENT / TOOL EVIDENCE
Record actual output/results for:
- OS/platform;
- `node --version`;
- `npm --version`;
- PostgreSQL server version actually used for Level 4 evidence.

Confirm Node satisfies package engines.

LOCAL ENVIRONMENT SAFETY PRECHECK
- confirm root `.env` remains ignored;
- confirm `.env` is not tracked;
- do not display its contents;
- identify only relevant variable names: `NEWS_SCRAPER_DATABASE_URL` and `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL`.

If `.env` is tracked or real credentials appear in committed source/output, stop closeout.

REPRODUCIBLE INSTALL
Run:
- `npm ci`

It must succeed from the committed lockfile rather than relying on an existing `node_modules` state.

STATIC / ORDINARY DETERMINISTIC MATRIX
Run each explicitly against `VALIDATED_SOURCE_SHA` and record result/counts where available:
- `npm run format:check`
- `npm run lint`
- `npm run typecheck`
- `npm test`
- `npm run test:unit`
- `npm run test:integration`
- `npm run check`
- committed-range `git diff --check PHASE3_BASE_SHA...VALIDATED_SOURCE_SHA`

Confirm ordinary deterministic commands do not require PostgreSQL and do not depend on the public internet.

REQUIRED DATABASE SUITE
With a valid dedicated test-admin prerequisite, run:
- `npm run test:db`

It must complete, return control to the shell, clean disposable databases, and include all Phase 2 + Phase 3 real-PostgreSQL suites.

Record the actual test count/result and state explicitly that Level 4 claims were exercised against disposable PostgreSQL.

REQUIRED PHASE 3 DATABASE EVIDENCE
Ensure executed tests prove and the artifact records at least:

Migration/schema:
- production Phase 3 migration applies from zero;
- rerun/current behavior is repeat-safe;
- expected tables/constraints exist;
- existing migration history/rollback/readiness regressions remain green.

Database constraints:
- Publication slug uniqueness;
- Publication-scoped Source config-key uniqueness;
- Source-scoped endpoint config-key uniqueness;
- endpoint URL uniqueness scoped only within Source;
- FK ownership;
- separate state vocabularies;
- `rss_atom` endpoint type;
- polling lower/upper bounds;
- domain-rule uniqueness/basic persisted shape.

Pure configuration semantics:
- slug/config-key validation;
- hostname normalization;
- exact-host/subdomain matching;
- narrowing subset behavior;
- approved endpoint host containment;
- polling bounds.

Persistence:
- Publication/Source/endpoint/domain rules round-trip;
- stable-key ownership/scoping;
- aggregate endpoint configuration read;
- transaction rollback leaves no partial configuration.

Generic bootstrap:
- initial create;
- repeat idempotency/cardinality stability;
- concurrent equivalent invocation convergence;
- no-overwrite for Publication/Source/endpoint fields and domain policies;
- edited endpoint URL does not cause obsolete seed URL recreation;
- persisted Source policy is not widened to satisfy later seed data;
- failed required insertion rolls back newly created tree state;
- generic CLI path operates against disposable PostgreSQL.

Actual approved bootstrap:
- exactly one initial Publication;
- exactly two Sources;
- exactly two `rss_atom` endpoints;
- Author Media exact approved configuration;
- The Creative Penn exact approved configuration;
- exact-host policies and `21600` polling;
- both Source and endpoint state triples are approved/active/enabled;
- Publication is collection-active and public status remains private;
- Jane Friedman, Authors Publish, Sub Club, and Upstream Reviews are absent;
- rerunning actual bootstrap preserves cardinality;
- simulated operator edits survive actual bootstrap rerun.

PACKAGE / ENV COMMAND BOUNDARY
Confirm final package scripts preserve:
- optional `.env` loading for `test:db`, `db:migrate`, `db:bootstrap`, `start:web`, `start:worker`;
- ordinary `test`, `test:unit`, `test:integration`, and `check` do not auto-load `.env`;
- no application-level `dotenv` dependency;
- package/lock root version synchronization.

EXPLICIT DATABASE-SUITE PREREQUISITE NEGATIVE
Physically invoke `npm run test:db` with an explicit synthetic unusable/missing `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL` override that takes precedence over any valid local `.env` value.

Expected:
- nonzero exit;
- clear bounded prerequisite failure;
- no skipped-green result;
- no credential/environment dump.

Restore/remove the synthetic override before valid DB procedures.

ZERO-SELECTION REGRESSION
Physically invoke the repository test wrapper with a deliberately nonexistent required test pattern.

Expected:
- zero matching files detected;
- nonzero exit;
- clear no-tests-matched output.

OPERATOR COMMAND SMOKE — DISPOSABLE DB
Prove the real Phase 3 operator workflow using a fresh disposable PostgreSQL database, never the persistent development database.

Use the existing safe disposable test-admin boundary/helper or an equivalent controlled local procedure.

Required procedure:
1. create a unique `news_scraper_test_*` database using the dedicated test-admin boundary;
2. verify it exists;
3. explicitly set child/process `NEWS_SCRAPER_DATABASE_URL` to that disposable DB so it overrides any `.env` application URL;
4. run `npm run db:migrate` from zero;
5. verify schema status current;
6. run `npm run db:bootstrap`;
7. inspect persisted Publication/Source/endpoint/domain state and confirm exact approved initial configuration;
8. run `npm run db:bootstrap` a second time;
9. confirm Publication/Source/endpoint/domain cardinality does not increase;
10. close application/test connections;
11. drop the disposable DB through the safe helper/admin boundary;
12. verify cleanup in PostgreSQL catalog.

Record database NAME if useful, never its credential-bearing URL.

The smoke path must not contact Author Media or The Creative Penn; Phase 3 bootstrap is database configuration only.

RUNTIME READINESS REGRESSION
Because P1 introduced a production migration and Phase 3 final tree changes schema status, ensure the complete DB suite and/or an explicit focused run proves existing Web/Worker readiness behavior still works:
- migrated/current disposable DB: Web liveness 200, readiness 200, Worker reaches ready and shuts down cleanly;
- reachable but uninitialized/not-current DB: Web remains live but readiness is 503; Worker does not report ready and exits through established startup-failure behavior;
- startup does not apply migrations;
- startup does not bootstrap configuration.

Do not invent a Worker HTTP probe.

TOPIC-INDEPENDENCE / BOUNDARY INSPECTION
Perform a final structural review of the accepted source tree.

Confirm:
- real publisher names/URLs and `Indie Author Publishing News` occur only in Publication-owned config, its direct verification tests, reconnaissance/decision/task/evidence docs, or other clearly configuration-specific surfaces;
- generic `src/publications/**`, `src/sources/**`, database, Web, Worker, and shared engine logic contain no Author Media/The Creative Penn-specific branch;
- no Jane Friedman/Authors Publish/Sub Club/Upstream Reviews initial bootstrap data exists;
- no Source discovery/auto-approval exists;
- no bootstrap call exists in normal Web/Worker startup;
- no automatic migration exists in normal Web/Worker startup;
- no outbound HTTP/DNS/network-safety execution was added in Phase 3;
- no Categories/Relevance/branding/Source priority/parser/cache/health/due/attempt/success/failure-counter fields were front-loaded;
- no Article or Collection-run persistence was introduced.

This is Level 1 structural evidence. Do not describe it as runtime proof.

NO LIVE-SOURCE / BROWSER REQUIREMENT
Do NOT add or run public publisher fetching simply to close Phase 3.

- Phase 3 has no fetcher/parser.
- Existing reconnaissance already records Source-selection evidence separately.
- No browser UI exists in this phase.

Record Level 6/7 as not applicable rather than pretending they were exercised.

DURABLE VALIDATION ARTIFACT
After ALL required validation passes against `VALIDATED_SOURCE_SHA`, create:
- `docs/validation/phase-3-publication-source-configuration.md`

The artifact must record at minimum:
- validation date/time/timezone;
- branch;
- Phase 3 base SHA;
- `VALIDATED_SOURCE_SHA`;
- package version `0.3.6`;
- OS/platform;
- Node/npm/PostgreSQL versions;
- `npm ci` result;
- every static/test/DB/negative/operator command actually executed and its result;
- useful test counts where available;
- evidence level for each class of claim;
- migration-from-zero/rerun evidence;
- configuration validation evidence;
- persistence/transaction evidence;
- generic bootstrap idempotency/no-overwrite/concurrency/rollback evidence;
- actual approved bootstrap cardinality and exact Source/endpoint identities/states/domain rules/polling;
- Web/Worker readiness regression evidence;
- topic-independence/startup/no-network structural findings;
- cleanup verification;
- limitations;
- explicit Phase 3 roadmap exit-gate conclusion.

Never include passwords, full DB connection URLs, `.env` contents, or other secrets.

ARTIFACT / SHA SEQUENCING
The validation artifact itself is created after validating `VALIDATED_SOURCE_SHA`, so its commit will be later than the accepted implementation source SHA.

The artifact must clearly identify the exact implementation source SHA it validates. Do not falsely claim that the evidence-artifact commit itself was the source tree executed unless the entire matrix was rerun after adding only that artifact and the distinction is documented.

PHASE 3 EXIT-GATE CHECKLIST
Close Phase 3 only if all are demonstrated:
- domain migration applies reproducibly from zero;
- generic Publication and at least two approved `rss_atom` endpoints can be configured without topic-specific engine logic;
- DB constraints and focused configuration validation reject invalid Phase 3-owned state/domain relationships;
- repeated bootstrap does not increase logical cardinality;
- operator-modified existing configuration remains unchanged after ordinary bootstrap rerun;
- bootstrap/database failure paths preserve coherent state and rollback transactionally where required;
- initial indie-author configuration exists only as Publication-owned bootstrap data;
- focused and broader regression matrix includes required real PostgreSQL evidence;
- exact owner-approved initial Source set is Author Media + The Creative Penn only.

Do not mark Phase 3 complete if any item is only assumed or source-inspected when runtime/DB evidence is required.

LIKELY FILE SCOPE
Before validation:
- package.json
- package-lock.json

After successful validation only:
- docs/validation/phase-3-publication-source-configuration.md

No other successful P6 changes.

REQUIRED EVIDENCE LEVELS
- Level 1: version/static/diff/topic-independence/startup/no-network structure.
- Level 2: pure configuration normalization/validation.
- Level 3: CLI/process composition where applicable.
- Level 4: migration, constraints, persistence, transaction rollback, bootstrap idempotency/no-overwrite/concurrency, actual bootstrap, runtime database readiness, and cleanup against real disposable PostgreSQL.
- Level 5: not applicable; no collection fixtures implemented yet.
- Level 6: not applicable; no browser behavior.
- Level 7: not applicable to implementation closeout; recon evidence is separate and Phase 3 does not fetch live Sources.

ACCEPTANCE CRITERIA
- package/lock version metadata are synchronized at `0.3.6` before validation.
- all required deterministic and DB suites pass against one exact committed final implementation tree.
- operator `db:migrate -> db:bootstrap -> db:bootstrap` path is physically exercised against disposable PostgreSQL and proves exact state + idempotency.
- failure/negative paths are observed rather than inferred.
- no startup migration/bootstrap, public-network collection, or later-phase data/behavior has leaked into Phase 3.
- durable Phase 3 validation artifact accurately records actual commands/results/evidence levels and exact accepted source SHA.
- every Phase 3 roadmap + inherited testing-contract exit gate is satisfied.

NON-GOALS
- Fixing defects discovered during closeout without separate authorization.
- Advancing repository current-phase docs to Phase 4.
- Network safety/fetch/parser/Collection runs.
- Live Source collection.
- Browser validation.
- Categories/Relevance/branding/admin UI.
- Scheduler/health.
- Article persistence.

DELIVERY / REVIEW NOTES
P6 is evidence, not implementation. A failing closeout is a useful result: stop, report the owning defect, correct it in the appropriate P1-P5 boundary, then rerun P6 at `0.3.6`. Do not weaken tests or alter evidence language to manufacture a green phase.