TASK: Phase 3 / P5 — Owner-approved initial Publication bootstrap configuration

ROADMAP PHASE
Phase 3 — Publication and Source configuration core.

VERSIONING
- This is Phase 3 prompt 5 and its assigned project version is `0.3.5`.
- On first execution confirm P4 left `package.json` at `0.3.4`, then advance it to `0.3.5` and synchronize npm-generated lockfile package metadata.
- If this same P5 prompt is rerun and `package.json` is already `0.3.5`, retain `0.3.5`; do not increment again.
- `package.json` remains the sole authoritative project-version source.
- Do not duplicate the version in docs or source constants.

DEPENDS ON
- P1 Phase 3 relational schema implemented and reviewed.
- P2 pure configuration normalization/validation implemented and reviewed.
- P3 persistence/read boundary implemented and reviewed.
- P4 generic transactional create-if-absent bootstrap engine and CLI implemented and reviewed.
- Repository-owner Source approval recorded in `docs/validation/phase-3-source-reconnaissance-decisions.md`.

GOAL
Add the actual first Publication bootstrap document and bind the generic P4 bootstrap command to it. The data must contain exactly the two Sources explicitly approved by the repository owner: Author Media and The Creative Penn.

P5 should be behavior-light. Do not add Source-specific engine branches, custom parsers, HTML scraping, Relevance rules, Categories, branding, or additional customer candidates merely because reconnaissance found them technically collectable.

GOVERNING DOCUMENTS / EVIDENCE
Read current versions before implementation:
- BOOT.md
- AGENTS.md
- docs/contracts/project-contract.md
- docs/contracts/mvp-scope-and-users.md
- docs/contracts/domain-and-data-contract.md
- docs/contracts/source-and-collection-contract.md
- docs/contracts/testing-and-validation-contract.md
- docs/architecture/system-architecture.md
- docs/roadmap/mvp-roadmap.md
- docs/decisions/whitelist-and-structured-feed-first.md
- docs/validation/phase-3-source-reconnaissance.md
- docs/validation/phase-3-authors-publish-targeted-reconnaissance.md
- docs/validation/phase-3-source-reconnaissance-decisions.md
- P1-P4 implementation/tests

SOURCE-APPROVAL AUTHORITY
The repository owner has explicitly approved this initial Phase 3 bootstrap set:
1. Author Media
2. The Creative Penn

The following customer candidates are explicitly withheld from the initial bootstrap:
- Jane Friedman
- Authors Publish
- Sub Club
- Upstream Reviews

Do not infer any additional approval from reconnaissance, technical feed availability, future customer value, or platform conventions.

INITIAL PUBLICATION
Create one Publication-owned configuration file, recommended:
- `config/publications/indie-author-publishing-news.json`

Use exactly this Publication identity/state unless a higher-authority current contract has changed before execution:
- name: `Indie Author Publishing News`
- slug: `indie-author-publishing-news`
- active_for_collection: `true`
- public_status: `private`

Rationale:
- collection must be able to proceed in later critical-path phases;
- a public feed does not exist yet, so Phase 3 must not claim public exposure;
- name/slug are Publication configuration, not engine identity.

Do not add:
- Categories;
- Relevance rules;
- Source priority;
- branding/logo/accent/descriptive copy;
- timezone/final feed settings;
- parser/cache/health/scheduling data.

APPROVED SOURCE 1 — AUTHOR MEDIA
Persist/configure the Source boundary as the publisher, not as the narrower customer-provided subsection.

Exact approved configuration:
- Source name/display name: `Author Media`
- Source config_key: `author_media`
- Source site URL: `https://www.authormedia.com/`
- Source approval state: `approved`
- Source lifecycle state: `active`
- Source operational state: `enabled`
- Source approved hostname rule: exact `www.authormedia.com`
- Source include_subdomains: `false`

Endpoint:
- endpoint config_key: `site_rss`
- endpoint URL: `https://www.authormedia.com/feed/`
- endpoint type: `rss_atom`
- endpoint approval state: `approved`
- endpoint lifecycle state: `active`
- endpoint operational state: `enabled`
- poll_interval_seconds: `21600`
- endpoint narrowing: exact `www.authormedia.com`
- endpoint include_subdomains: `false`

Accepted caveat:
- the selected first-party feed is broader than the Author Update subsection and contains additional Author Media material;
- later Publication-owned Relevance rules may address breadth;
- Phase 3 must not embed Author Update keywords/filters into shared code.

Do NOT configure the Buzzsprout podcast host. It was not selected as the approved endpoint and would widen the Source boundary unnecessarily.

APPROVED SOURCE 2 — THE CREATIVE PENN
Persist/configure the publisher as the Source and its podcast feed as the endpoint.

Exact approved configuration:
- Source name/display name: `The Creative Penn`
- Source config_key: `the_creative_penn`
- Source site URL: `https://www.thecreativepenn.com/`
- Source approval state: `approved`
- Source lifecycle state: `active`
- Source operational state: `enabled`
- Source approved hostname rule: exact `www.thecreativepenn.com`
- Source include_subdomains: `false`

Endpoint:
- endpoint config_key: `podcast_rss`
- endpoint URL: `https://www.thecreativepenn.com/feed/podcast/`
- endpoint type: `rss_atom`
- endpoint approval state: `approved`
- endpoint lifecycle state: `active`
- endpoint operational state: `enabled`
- poll_interval_seconds: `21600`
- endpoint narrowing: exact `www.thecreativepenn.com`
- endpoint include_subdomains: `false`

Accepted caveat:
- the feed is primarily author-business/interview programming rather than a pure publishing-industry news desk;
- later Publication-owned Relevance may refine editorial inclusion;
- no Source-specific engine condition is allowed.

EXPLICITLY WITHHELD CONFIGURATION
The committed bootstrap document MUST NOT contain records for:
- Jane Friedman / janefriedman.com / Jane Friedman's Substack;
- Authors Publish;
- Sub Club;
- Upstream Reviews.

Important Authors Publish context:
- targeted recon verified `https://authorspublish.com/category/publishing-news/feed/` as technically valid RSS;
- it is withheld because the feed is stale and still materially mixed with opportunity content;
- technical collectability is not operator approval.

ROOT OPERATOR COMMAND
Add the first real convenience command:
- `npm run db:bootstrap`

Recommended exact form:
- `node --env-file-if-exists=.env scripts/bootstrap-database.ts config/publications/indie-author-publishing-news.json`

Requirements:
- uses the generic P4 CLI; do not create a second bootstrap implementation;
- optional root `.env` loading follows the established database command pattern;
- explicit shell/process `NEWS_SCRAPER_DATABASE_URL` overrides `.env` under Node's env-file semantics;
- no implicit bootstrap in `start:web` or `start:worker`;
- no automatic migration inside bootstrap; the application schema must already be current;
- no `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL` use in production/bootstrap source.

PACKAGE COMMAND CONTRACT TEST
Update `test/unit/package-scripts.test.ts` so `db:bootstrap` is treated as an environment-requiring command that uses optional `.env` loading, alongside:
- `test:db`;
- `db:migrate`;
- `start:web`;
- `start:worker`.

Preserve:
- ordinary `test`, `test:unit`, `test:integration`, and `check` do not auto-load `.env`;
- no `dotenv` dependency;
- package/lock root version synchronization test.

CONFIGURATION FILE OWNERSHIP
The JSON file is Publication-owned bootstrap data. It may contain the indie-author Publication name and approved publisher identities because those are configuration facts.

No matching strings/URLs/keywords should be added to generic shared aggregation/configuration/bootstrap logic except where a generic test explicitly reads the committed config file.

Do not create Source-specific TypeScript constants as a second source of truth. The bootstrap JSON should own this initialization data.

FOCUSED REAL-POSTGRESQL TESTS
Create a substantive suite, recommended:
- `test/database/initial-publication-bootstrap.test.ts`

Use the ACTUAL committed bootstrap JSON through the generic P4 path and a production-migrated disposable PostgreSQL database.

Prove at Level 4:

Exact cardinality:
- exactly 1 Publication created for this config;
- exactly 2 Sources under it;
- exactly 2 endpoints total;
- expected Source and endpoint domain-rule cardinality;
- no withheld Source records.

Publication:
- slug exactly `indie-author-publishing-news`;
- name exactly `Indie Author Publishing News`;
- `active_for_collection = true`;
- `public_status = private`.

Author Media:
- Source config key/name/site URL exact;
- approved/active/enabled state exact;
- exact `www.authormedia.com`, subdomains false Source rule;
- `site_rss` endpoint URL/type/poll/states exact;
- exact endpoint narrowing rule.

The Creative Penn:
- Source config key/name/site URL exact;
- approved/active/enabled state exact;
- exact `www.thecreativepenn.com`, subdomains false Source rule;
- `podcast_rss` endpoint URL/type/poll/states exact;
- exact endpoint narrowing rule.

Idempotency:
- run the actual config twice;
- Publication/Source/endpoint/domain cardinality remains unchanged;
- no duplicate rows.

No-overwrite using actual config:
- after first run, mutate representative operator-managed existing values through controlled test SQL, for example Source operational state and endpoint URL/polling/narrowing;
- rerun actual bootstrap;
- verify modified values remain unchanged;
- verify original seed endpoint URL is not recreated under a new endpoint row.

Withheld set:
- query by expected withheld names/known candidate keys/domains as appropriate and prove they are absent from the initial Publication.

ROOT COMMAND PROCESS TEST
Exercise the actual root command against a disposable migrated DB, not only imported functions.

Procedure inside a DB test or controlled process test:
- explicitly set `NEWS_SCRAPER_DATABASE_URL` for the child process to the disposable DB;
- invoke `npm run db:bootstrap` from repository root;
- verify nonzero/zero result as applicable and inspect DB state;
- invoke it a second time and prove idempotency;
- ensure child output contains no credential-bearing URL or password.

Do not let a developer `.env` redirect this test to a persistent development DB. Explicit environment override must target the disposable DB.

NO LIVE-SOURCE VALIDATION IN P5
Do not fetch Author Media or The Creative Penn during implementation tests.

The reconnaissance artifacts record point-in-time source-selection evidence. Phase 3 implementation performs no outbound HTTP. Ordinary deterministic regression must not depend on public publishers.

No `test:live-sources` command is required by P5.

LIKELY FILE SCOPE
Expected:
- package.json
- package-lock.json
- config/publications/indie-author-publishing-news.json
- test/unit/package-scripts.test.ts
- test/database/initial-publication-bootstrap.test.ts

P4 generic bootstrap code should not need Source-specific modification. If it does, ensure the change is a genuinely generic defect fix with regression coverage rather than a publisher special case.

BROADER REGRESSION VALIDATION
Execute against the final P5 tree:
- npm run format:check
- npm run lint
- npm run typecheck
- npm test
- npm run test:unit
- npm run test:integration
- npm run check
- npm run test:db with a valid dedicated PostgreSQL admin prerequisite
- actual `npm run db:bootstrap` against a migrated disposable PostgreSQL database with explicit `NEWS_SCRAPER_DATABASE_URL`
- committed-change whitespace/diff validation over the P5 range

Do not run `db:bootstrap` against an operator's persistent development DB as part of destructive/no-overwrite tests. Use disposable DBs for those procedures.

REQUIRED EVIDENCE LEVELS
- Level 1: exact committed config, package script, version, topic/config separation.
- Level 2: package-command environment contract.
- Level 3: actual command-process composition where process invocation is tested.
- Level 4: exact real bootstrap data, states/domain rules/cardinality/idempotency/no-overwrite against disposable PostgreSQL.

ACCEPTANCE CRITERIA
- package/lock version metadata are synchronized at `0.3.5`.
- one committed initial Publication bootstrap document exists with no later-phase fields.
- exactly Author Media and The Creative Penn are configured as approved/active/enabled Sources/endpoints.
- Source identities/site URLs reflect publisher boundaries; endpoint identities reflect selected feeds.
- exact host rules and `21600` polling are preserved.
- Jane Friedman, Authors Publish, Sub Club, and Upstream Reviews are absent.
- `npm run db:bootstrap` invokes the generic P4 bootstrap explicitly and loads optional `.env` without affecting ordinary test commands.
- actual bootstrap rerun does not increase cardinality and does not overwrite simulated operator changes.
- no live Source requests, custom Source adapters, topic conditionals, Categories, Relevance, branding, or later fields are introduced.

NON-GOALS
- More Sources than the two owner-approved candidates.
- Categories/Relevance or initial Category data.
- Final branding/feed/timezone settings.
- Source priority.
- RSS/Atom fetch/parser behavior.
- DNS/address/port/redirect runtime safety.
- Scheduler/health/cache/timing.
- Article/Collection-run persistence.
- Admin CRUD/UI.
- Paywall/email/browser automation.

DELIVERY / REVIEW NOTES
P5 is deliberately small: the generic machinery should already exist. Review the diff for topic leakage. Publisher names/URLs belong in the JSON bootstrap data and its verification test, not conditional logic in shared modules. Report exact validation commands actually executed and do not describe public feeds as runtime-verified by Phase 3 merely because reconnaissance previously observed them.