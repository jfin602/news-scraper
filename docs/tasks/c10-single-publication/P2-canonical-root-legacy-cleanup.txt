TASK: Correction 10 / P2 — Canonical root surface and legacy-tree cleanup

ROADMAP PHASE
Phase 10 — Automated polling, durable jobs, and endpoint health.

STACK TYPE
Non-versioned Phase 10 entry correction. This prompt completes the singleton correction implementation after P1; it is not ordinary Phase 10 scheduler/job work.

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.
- Complexity / quality floor: `Elevated`.
- Estimated usage: `Moderate`.
- Alternative considered: `Terra Ultra`.
- Estimate confidence: `High`.
- Efficiency rationale: P1 owns the destructive relational and transactional rewrite. P2 is deliberately narrower: finish canonical configuration paths, remove the obsolete slug-addressed page surface, delete remaining legacy-only selectors/artifacts, and prove the final public experience in Chromium. Terra High is sufficient for this bounded cleanup and browser-regression boundary without spending Sol-level usage.
- If this exact configuration is no longer available in the current Codex picker at execution time, revalidate against `BOOT.md` rather than silently substituting an invented model/effort name.

VERSIONING
- Required unchanged project version: `0.10.0`.
- Confirm P1 left `package.json` exactly `0.10.0` and preserve that exact version throughout P2.
- This correction does not advance the roadmap version.
- `package.json` remains the sole authoritative current-version source.
- The repository intentionally does not use npm package locks; do not create, require, regenerate, or validate `package-lock.json` or `npm-shrinkwrap.json`.

DEPENDS ON
- P1 complete and coherent.
- P1 canonical singleton schema/persistence/provenance.
- P1 selector-free Worker and canonical `GET /api/feed`.
- P1 public client already fetching `/api/feed` without Publication slug parsing.
- Current public-feed/browser contracts requiring canonical root `/`.

GOAL
Finish the singleton correction by making the active repository read like a one-Publication-per-installation system rather than a compatibility-preserving refactor of the old tenant-shaped implementation.

P2 must move the checked-in bootstrap configuration to a singular canonical path, make `GET /` the only supported public page route, remove the old slug-addressed page without redirects/aliases, update current executable tests to the canonical paths, delete remaining legacy-only selector/config/test artifacts, and provide Level 6 browser evidence for the root public experience.

P2 is primarily deletion, path canonicalization, route cleanup, and browser regression work. It MUST NOT reopen the P1 schema design or add ordinary Phase 10 scheduler/job functionality.

GOVERNING DOCUMENTS
Read current versions before implementation:
- `BOOT.md`
- `AGENTS.md`
- `docs/contracts/project-contract.md`
- `docs/contracts/domain-and-data-contract.md`
- `docs/contracts/public-feed-and-admin-contract.md`
- `docs/contracts/source-and-collection-contract.md`
- `docs/contracts/testing-and-validation-contract.md`
- `docs/architecture/system-architecture.md`
- `docs/operations/security-reliability-and-operations.md`
- `docs/roadmap/mvp-roadmap.md`
- `docs/decisions/single-publication-simplified-data-model.md`
- P1 task file and final P1 implementation/tests.

INSPECT / TRACE BEFORE EDITING
Re-read the final P1 tree before changing anything. Trace:
- final migration directory and singleton Publication module;
- final checked-in bootstrap configuration path/content;
- `package.json` scripts;
- `scripts/bootstrap-database.ts`;
- final `src/app/web/create-app.ts`;
- `src/app/web/main.ts`;
- `src/app/web/public-feed-page.ts`;
- `src/app/web/public/public-feed.js`;
- `src/app/web/public/public-feed.css`;
- `src/public-feed/repository.ts`;
- `test/integration/public-feed-http.test.ts`;
- `test/integration/public-feed-page-http.test.ts`;
- `test/database/public-feed-http.test.ts`;
- `test/database/public-feed-repository.test.ts`;
- `test/browser/public-feed-page.test.ts`;
- current `test/live-sources/**`;
- unit/package-script tests;
- all current source/config/test references to Publication IDs/slugs, plural config path, and slug-addressed public routes.

Do not assume P1 used every planned function/file name. Follow the actual final P1 implementation while preserving the governing contracts.

CANONICAL BOOTSTRAP CONFIG PATH
Move the checked-in installation configuration from the obsolete plural/topic-selection-shaped path:

`config/publications/indie-author-publishing-news.json`

to:

`config/publication.json`

Delete the old file/path. Do not leave a copy, symlink, redirecting loader, or fallback search path for compatibility.

The new config remains topic-specific edge configuration for this deployment. Shared engine behavior must remain topic independent.

Preserve the final P1 singleton document shape and current approved Source/endpoint configuration. Do not change Source approval, domain boundaries, endpoint URLs, operational state, or polling values merely because the file is moving.

PACKAGE / BOOTSTRAP COMMAND
Update `package.json` so the ordinary `db:bootstrap` script targets `config/publication.json`.

Preserve all unrelated scripts and keep version `0.10.0`.

Keep `scripts/bootstrap-database.ts` capable of explicit file-driven bootstrap if P1 retained that generic operator mechanism. Do not hard-code the initial topic into production source.

Update package-script tests to assert the final current command/environment behavior rather than historical paths.

CANONICAL PUBLIC PAGE
Replace the obsolete page route:

`GET /publications/:publicationSlug`

with the canonical customer-visible route:

`GET /`

Delete the old page route completely.

Do not:
- redirect the old slug route to `/`;
- retain an alias;
- add a wildcard Publication route;
- parse a slug and ignore it;
- add a reader topic selector.

The deployment itself identifies the one Publication/topic.

P1 should already have made the client fetch `GET /api/feed`. Confirm that remains true and that no page-path-derived Publication identity is required anywhere in the client.

PUBLIC PAGE BEHAVIOR TO PRESERVE
P2 is not a redesign. Preserve the accepted basic public-feed UI behavior:
- page shell remains topic independent;
- Publication name comes from canonical feed data, not a shared-code hard-coded topic string;
- loading state;
- populated state;
- public empty state;
- generic unavailable/not-found state for absent/private singleton settings;
- generic dependency/error state;
- no backend/private detail leakage;
- headline links use exact stored Article `original_url`;
- `canonical_identity_url` is not substituted as public destination;
- untrusted title/Source text remains inert text;
- effective feed dates use UTC calendar rendering until a later Publication timezone setting exists;
- desktop supports `Date | Headline | Source`;
- mobile uses the existing sane stacked presentation;
- current keyboard/focus behavior remains;
- no feed-caused horizontal overflow;
- Web/API never collects Sources inline.

Do not pull Phase 12/13 search, pagination, theming, or polish into this correction.

HTTP / INTEGRATION TESTS
Update current HTTP integration tests to prove:
- `GET /` serves the public page shell;
- direct page delivery does not query/fetch Articles itself beyond the existing client/read-model architecture;
- `GET /api/feed` remains canonical and unchanged from P1 semantics;
- old slug-addressed API route remains absent;
- old slug-addressed page route is unsupported;
- same-origin static JS/CSS delivery still works;
- CSP/security headers remain correct;
- health/readiness/process behavior remains unaffected.

Do not add redirect assertions for old paths; there should be no compatibility redirect.

BROWSER VALIDATION — LEVEL 6
Update `test/browser/public-feed-page.test.ts` so the canonical navigation target is `/`.

The final browser suite must prove at representative desktop/mobile viewports:
1. direct navigation to `/` succeeds;
2. refresh at `/` succeeds and the page remains canonical;
3. loading state remains visible until the feed request resolves;
4. populated feed renders expected date/headline/Source structure;
5. public empty state renders explicitly;
6. absent/private singleton settings produce the generic unavailable state without leakage;
7. backend dependency failure produces the generic error state without leakage;
8. exact stored external publisher URL is used for headline href;
9. untrusted title/Source strings are rendered inertly rather than executed/interpreted as markup;
10. UTC calendar-date rendering remains stable under a non-UTC browser timezone;
11. keyboard/focus behavior remains usable;
12. desktop three-column behavior remains legible;
13. representative mobile stacked layout remains usable;
14. feed presentation does not create horizontal overflow.

Use the existing Playwright/Chromium test infrastructure. Do not add a second browser framework.

CURRENT LIVE-SOURCE TEST SOURCE
`test/live-sources/**` remains current executable repository code even when filenames describe historical phase milestones.

Update those tests where needed so current code paths use:
- `config/publication.json`;
- two-key Worker invocation;
- selector-free `readPublicFeed(database)` or final equivalent;
- canonical root `/` for browser navigation;
- no Publication UUID/slug assertions.

Do not rewrite historical durable `docs/validation/**` artifacts. They must continue to describe what was actually observed at their recorded SHAs.

Do not require executing the live-source suite for P2 acceptance; ordinary deterministic correction validation remains independent of public publisher availability.

LEGACY-TREE CLEANUP SWEEP
After the concrete route/config updates, perform a deliberate structural sweep over the ACTIVE implementation/config/migration/test tree for legacy-only tenancy/selectors.

Search for patterns including, as applicable:
- `publication_id`;
- `publicationId`;
- `publicationSlug`;
- `/api/publications/`;
- `/publications/:`;
- `config/publications/`;
- `findPublicationBySlug`;
- Source/endpoint lookup functions taking Publication slug/id solely as scope;
- old plural-module forwarding wrappers if P1 renamed `src/publications/`;
- old migration filenames;
- tests/fixtures whose only purpose is unsupported pre-production Publication tenancy or old-schema upgrade compatibility.

Exclude intentionally historical records from deletion/search conclusions:
- `docs/tasks/p1/**` through `docs/tasks/p9/**`;
- historical `docs/validation/**`;
- superseded ADR history.

Do not globally erase the Publication concept. Valid current uses may include:
- singleton `PublicationConfiguration`;
- persisted singleton Publication/settings read model;
- Publication name;
- `activeForCollection`;
- `publicStatus`;
- stable collection reason `publication_inactive`;
- documentation describing the singleton editorial configuration.

The target is removal of tenant identity/scoping/selectors, not removal of the domain term itself.

DELETE RATHER THAN WRAP
If P2 finds a helper, type, route, fixture, test, config path, or compatibility module whose only purpose is old Publication tenancy/slug behavior, delete it rather than preserving it behind a deprecated wrapper.

Do not create:
- deprecated aliases;
- forwarding imports;
- ignored old parameters;
- dual config paths;
- old/new route pairs;
- migration compatibility fixtures for disposable old databases.

Git history and historical docs are the record of the old implementation.

EXPECTED PRIMARY WRITE SCOPE
P2 may change/delete/create files primarily in:
- `config/**` for canonical singleton config path;
- `package.json` scripts only, never version;
- `src/app/web/**` for root route/static/client behavior;
- current public-feed integration/browser tests;
- current package-script/unit tests;
- current `test/live-sources/**` path/invocation updates;
- small current test support only if required by browser/root-route validation;
- residual legacy-only source/types/tests/config paths discovered by the structural sweep.

P2 should not normally need to change P1's final schema or Article identity implementation. If a genuine P1 defect blocks P2, repair it minimally and rerun the newly applicable P1-focused regressions; do not redesign the data model.

FORBIDDEN / DEFERRED
Do not:
- add scheduler/job tables or code;
- implement retries/backoff/endpoint health automation;
- add Search/Category/Source filters or pagination;
- add final theme/branding polish;
- add duplicate grouping/review;
- add admin UI/native auth;
- add HTML adapters;
- restore Publication IDs/slugs/selectors;
- add old page/API redirects or compatibility aliases;
- change package version;
- modify historical task prompts/validation artifacts;
- alter governing docs unless a real contradiction is discovered. Report such a contradiction rather than silently changing contracts.

SECURITY / SAFETY PRESERVATION
P2 must preserve:
- X-Content-Type-Options and existing Web security headers;
- CSP/static-resource behavior;
- no HTML interpretation of feed text;
- no backend detail leakage in unavailable/error states;
- exact external `original_url` behavior;
- Web/API process isolation from collection;
- current readiness/liveness contracts.

P2 must not weaken collection/network safety merely because live-source tests are being updated to current paths.

EVIDENCE LEVELS
P2 requires Level 6 Chromium evidence for the canonical root page.

P2 should also rerun Level 4 database regression because the final tree still depends on the P1 canonical schema and P2 modifies current config/tests sitting on top of it.

FOCUSED / BROADER VALIDATION
Against the final P2 tree run and record at minimum:
- `npm run format:check`
- `npm run lint`
- `npm run typecheck`
- `npm run test:unit`
- `npm run test:integration`
- `npm run test:collection`
- `npm run test:db`
- `npm test`
- `npm run check`
- `npm run test:browser`

Use repository-standard `npm install` only when dependency installation is needed. Do not use `npm ci` and do not create a lockfile.

If Playwright-managed Chromium or the safe disposable PostgreSQL test prerequisite is unavailable, report that clearly and do not claim the corresponding evidence level passed.

ACCEPTANCE CRITERIA
P2 is complete only when all are true:
1. package version remains `0.10.0`;
2. the checked-in installation config is `config/publication.json` and the old plural path is gone;
3. ordinary bootstrap command uses the canonical config path;
4. `GET /` is the supported public page;
5. the old slug-addressed page route is absent without redirect/alias compatibility;
6. `GET /api/feed` remains the only basic public-feed API surface;
7. browser/client code contains no path-derived Publication selector;
8. public loading/populated/empty/unavailable/error/external-link/UTC behavior is preserved;
9. desktop/mobile/browser regression evidence passes at Level 6 when prerequisites are available;
10. current live-source test source uses canonical config/Worker/feed/root APIs without stale Publication identities;
11. active implementation/config/migration/test structural search finds no legacy-only Publication tenancy/slug selector or unsupported upgrade artifact that lacks an independent current purpose;
12. historical prompts/validation/ADR records remain untouched;
13. no ordinary Phase 10 feature work was pulled forward.

NON-GOALS
- Reworking P1's canonical schema unless a proven defect blocks acceptance.
- Data migration from old pre-production databases.
- Live publisher success as a deterministic P2 gate.
- Scheduler/jobs/retries/health.
- Search/filter/pagination/theme polish.
- Admin UI/native accounts.
- Duplicate grouping/review.
- HTML collection.

FINAL REPORT
Report:
- files created/changed/deleted;
- config path move;
- public route changes;
- legacy-only artifacts removed;
- structural searches performed and meaningful remaining Publication references;
- browser/database/test commands actually executed and observed results;
- unavailable prerequisites/limitations;
- confirmation that `package.json` remains `0.10.0`;
- whether the tree is ready for the manual P3 correction closeout.