TASK: Phase 2 / P5 — Final-tree PostgreSQL validation and Phase 2 closeout evidence

ROADMAP PHASE
Phase 2 — Database foundation.

VERSIONING
- This is Phase 2 prompt 5 and its assigned project version is `0.2.5`.
- On first execution confirm P4 left `package.json` at `0.2.4`, then advance it to `0.2.5` and synchronize npm-generated lockfile package metadata before establishing the final validated source SHA.
- If this same P5 prompt is rerun after failed/partial closeout and `package.json` is already `0.2.5`, retain `0.2.5`; do not increment again.
- `package.json` remains the sole authoritative project-version source.
- Do not duplicate the current version in README, BOOT, source constants, or other documentation.

DEPENDS ON
- P1-disposable-postgres-test-foundation.txt implemented and reviewed.
- P2-shared-database-runtime-transactions.txt implemented and reviewed.
- P3-sql-migration-workflow.txt implemented and reviewed.
- P4-web-worker-database-readiness.txt implemented and reviewed.
- All Phase 2 runtime/source/test changes from P1-P4 committed.

GOAL
Advance the final Phase 2 prompt version, execute the complete Phase 2 local/static/regression/PostgreSQL/runtime validation matrix against one exact committed implementation tree, prove the Phase 2 roadmap exit gate, and create the durable Phase 2 validation artifact required by the testing contract.

This is a closeout/evidence task, not a feature-development task. Apart from its required `0.2.5` version/lock metadata transition, successful closeout must not change runtime behavior.

GOVERNING DOCUMENTS
Read current versions before execution:
- BOOT.md
- AGENTS.md
- docs/contracts/project-contract.md
- docs/contracts/testing-and-validation-contract.md
- docs/architecture/system-architecture.md
- docs/operations/security-reliability-and-operations.md
- docs/roadmap/mvp-roadmap.md
- current package scripts/config/source/tests produced by P1-P4
- docs/validation/phase-1-application-foundation.md as the evidence-record format precedent

PRESERVED BEHAVIOR / RULES
- Do not advance BOOT/README/AGENTS/roadmap current phase to Phase 3 in this task.
- Do not claim database/runtime behavior as verified unless it was actually executed and observed.
- Do not use hosted CI/status checks as substitute evidence.
- Do not silently skip missing PostgreSQL prerequisites.
- Required selected suites must not pass with zero tests.
- Real persistence/transaction/migration/readiness claims require Level 4 real PostgreSQL evidence.
- No browser, live-Source, or reference-deployment evidence is required for Phase 2.
- Validation evidence records observations; it does not redefine contracts.
- Do not use development/production application data as disposable test storage.
- Never include credential-bearing DB URLs, passwords, or real secret values in the validation artifact.

CRITICAL CLOSEOUT RULE
If validation reveals an implementation defect, contract violation, flaky/hanging test, false-green suite, migration/readiness error, unsafe output, leaked credential, cleanup failure, or other failure:
1. stop Phase 2 closeout;
2. report the exact failed invariant/command/procedure;
3. do not hide/fix runtime behavior inside this closeout task unless the current user explicitly authorizes a fix/new implementation task;
4. return the defect to the owning P1/P2/P3/P4 boundary;
5. retain assigned version `0.2.5` when rerunning this same prompt;
6. after the fix is committed, rerun the P5 procedure from the beginning against the new final implementation tree.

Do not create an accepted Phase 2 validation artifact for a failing tree.

SOURCE MODIFICATION SCOPE
Before final-tree validation, this task may modify only:
- package.json for the required version transition;
- package-lock.json as npm's synchronized mechanical mirror.

After the validated implementation/version tree is committed and all validation succeeds, create only:
- `docs/validation/phase-2-database-foundation.md`

During successful closeout do not modify runtime source, tests, migrations, scripts, dependencies, README, BOOT, AGENTS, roadmap, or unrelated docs.

VERSION TRANSITION / PRE-VALIDATION COMMIT
Before establishing the validated source SHA:
1. inspect package.json;
2. if version is `0.2.4`, set it to `0.2.5` and synchronize package-lock using normal npm tooling without creating a git tag;
3. if already `0.2.5` because this same P5 prompt is being rerun, do not increment;
4. if another unexpected version is present, stop and report version-sequence drift;
5. confirm package.json and root package lock metadata are synchronized;
6. commit the P5 version-metadata transition so the tree to be validated has a stable SHA.

PRECONDITION / TREE IDENTITY
After the P5 version transition is committed and before validation:
- require a clean working tree;
- record `VALIDATED_SOURCE_SHA=$(git rev-parse HEAD)` or platform equivalent;
- record branch;
- record OS/platform information;
- record Node version;
- record npm version;
- record PostgreSQL server version actually used for Level 4 evidence;
- confirm package.json version at `VALIDATED_SOURCE_SHA` is exactly `0.2.5`.

The validated source SHA is the committed P1-P4 implementation plus P5 version metadata, before the evidence artifact itself is created.

PHASE 2 IMPLEMENTATION BASE FOR DIFF VALIDATION
The testing contract requires committed-range whitespace/diff validation.

Determine the exact Phase 2 implementation base from repository history. The repository owner created an explicit `0.2.0` Phase 2 baseline before this prompt stack; use the final merged/accepted baseline commit immediately before P1 implementation as `PHASE2_BASE_SHA` rather than assuming a stale hard-coded SHA.

Record:
- `PHASE2_BASE_SHA`
- `VALIDATED_SOURCE_SHA`

Run committed-range whitespace validation equivalent to:
- `git diff --check PHASE2_BASE_SHA...VALIDATED_SOURCE_SHA`

Do not substitute a plain clean-working-tree `git diff --check` that may inspect no committed Phase 2 changes.

ENVIRONMENT / TOOL EVIDENCE
Record actual output for at least:
- `node --version`
- `npm --version`
- PostgreSQL server version for the dedicated test environment
- OS/platform identification appropriate to the environment

Confirm Node satisfies the package engines baseline.

REPRODUCIBLE INSTALL VALIDATION
Run:
- `npm ci`

This must succeed from the committed lockfile. Do not rely on a pre-existing node_modules state.

STATIC / ORDINARY DETERMINISTIC VALIDATION
Run each explicitly and record result:
- `npm run format:check`
- `npm run lint`
- `npm run typecheck`
- `npm test`
- `npm run test:unit`
- `npm run test:integration`
- `npm run check`
- committed-range `git diff --check PHASE2_BASE_SHA...VALIDATED_SOURCE_SHA`

Record useful test counts where available. `npm test` must remain the ordinary deterministic suite and must not require PostgreSQL merely because DB tests exist.

REQUIRED DATABASE SUITE
With a valid dedicated test-admin PostgreSQL prerequisite, run:
- `npm run test:db`

This suite must exercise the complete Phase 2 real PostgreSQL behavior introduced by P1-P4, including disposable DB lifecycle, shared database runtime, transactions, migrations, and process readiness.

Record test counts/results and explicitly state that Level 4 evidence used real disposable PostgreSQL.

MISSING DB PREREQUISITE NEGATIVE
Physically invoke `npm run test:db` with `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL` absent/unset.

Expected:
- nonzero exit;
- clear explanation that the required database-test admin prerequisite is unavailable;
- no skipped-green result;
- no credential/environment dump.

Record command/procedure and observed result. A green/zero test/skip result is a Phase 2 closeout failure.

ZERO-SELECTION NEGATIVE
Physically invoke the repository test wrapper with a deliberately nonexistent required pattern.

Expected:
- zero matching files detected;
- nonzero exit;
- clear no-tests-matched output.

This confirms Phase 1's required false-green protection remains intact after test command restructuring in Phase 2.

PHYSICAL DISPOSABLE DATABASE / MIGRATION PROCEDURE
In addition to automated `test:db`, perform or surface a concise physical procedure against real PostgreSQL that proves the roadmap exit gate end-to-end.

Using only the dedicated test-admin connection:
1. create a unique disposable `news_scraper_test_*` database using the repository test helper or same validated safe mechanism;
2. verify the created database exists;
3. point ordinary `NEWS_SCRAPER_DATABASE_URL` at that disposable DB without exposing credentials in recorded output;
4. run the explicit migration command from zero;
5. query/inspect migration status and confirm current;
6. rerun migrations and confirm repeat-safe/no duplicate migration history;
7. if practical, surface a representative committed transaction and rolled-back transaction through the production DB boundary (automated Level 4 tests may satisfy this if their terminal output/result is unambiguous);
8. close all application/test connections;
9. drop the disposable database through the safe test helper;
10. query the admin catalog and confirm the database no longer exists.

Record generated database name only if it contains no secret; never record the full connection URL.

MIGRATION FAILURE / COMPATIBILITY EVIDENCE
Ensure executed DB tests prove and the artifact records at least:
- migration from untouched database;
- rerun/current behavior;
- failed migration rollback/not-recorded behavior;
- changed applied migration/checksum incompatibility;
- pending/uninitialized/incompatible status behavior;
- verified cleanup after the test database lifecycle.

Do not manufacture a failure manually against shared development data.

PHYSICAL WEB/API RUNTIME VALIDATION — READY DATABASE
Run the real Web/API entrypoint/package script against a migrated/current disposable PostgreSQL database.

Use safe local host and port 0 if supported.

Observe and record:
1. startup command/procedure with secrets redacted;
2. `web.listening` actual listener evidence;
3. actual host/port;
4. `GET /health/live` status/body;
5. `GET /health/ready` status/body;
6. graceful SIGTERM/SIGINT action appropriate to platform;
7. `web.stopped` or equivalent clean shutdown evidence;
8. successful process exit.

Expected:
- live = HTTP 200;
- ready = HTTP 200 with migrated/current DB.

PHYSICAL WEB/API RUNTIME VALIDATION — UNAVAILABLE DB
Start the real Web entrypoint with a syntactically valid but intentionally unavailable/nonexistent PostgreSQL application target using synthetic credentials/values.

Expected:
- Web listener still starts;
- `/health/live` remains HTTP 200;
- `/health/ready` is HTTP 503;
- process can shut down cleanly;
- output/body does not expose the synthetic password/raw URL.

If the exact unavailable target procedure differs by platform, record it accurately. Do not point at a real production database or intentionally disrupt the shared PostgreSQL server.

PHYSICAL WEB SCHEMA-NOT-CURRENT VALIDATION
Against a reachable disposable DB that is intentionally uninitialized or otherwise not current:
- Web listens;
- live = 200;
- ready = 503;
- readiness check does not auto-create/apply migrations.

This is direct proof that readiness includes schema compatibility and is non-mutating.

PHYSICAL WORKER RUNTIME VALIDATION — READY DATABASE
Run the real Worker entrypoint against a migrated/current disposable PostgreSQL database while Web is not running.

Observe and record:
1. startup command/procedure with secrets redacted;
2. `worker.ready`;
3. confirmation it remains running after readiness;
4. SIGTERM/SIGINT shutdown action;
5. `worker.stopped` or equivalent clean stop;
6. successful exit.

Confirm Web host/port config is absent/not required.

PHYSICAL WORKER DATABASE FAILURE VALIDATION
Run the real Worker entrypoint against:
- a syntactically valid unavailable/nonexistent DB target; and
- a reachable disposable DB whose schema is uninitialized/not current, if this is not already covered by an equivalent executed DB-process test.

Expected:
- no `worker.ready`;
- safe `worker.start_failed` or equivalent established failure event;
- nonzero exit;
- no auto-migration;
- no leaked synthetic credentials/raw DB errors.

PROCESS / ARCHITECTURE STRUCTURAL CHECKS
Inspect final source and record concise results for:
- no production `src/` consumption of `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL`;
- no domain Publication/Source/Article/Collection-run/Category/duplicate schema pulled into Phase 2;
- no indie-author/publishing-topic condition/data in shared database/process source;
- Web and Worker both use the canonical shared `src/database` boundary rather than separate pools;
- no Web→Worker or Worker→Web process dependency;
- no automatic application startup migration;
- Worker still has no HTTP health server;
- package.json and lockfile root package metadata both equal `0.2.5`.

EVIDENCE LEVELS TO RECORD
Phase 2 closeout should claim only actual evidence:
- Level 1: format/lint/type/static/diff/version/module-structure checks;
- Level 2: deterministic configuration/migration discovery/lifecycle unit tests;
- Level 3: Web HTTP/component/process and Worker process integration behavior where controlled dependencies are used;
- Level 4: real disposable PostgreSQL creation/cleanup, queries, transactions, migrations, schema compatibility, and DB-backed Web/Worker readiness.

Explicitly state Phase 2 does not establish:
- Level 5 collection-fixture evidence;
- Level 6 browser evidence;
- Level 7 approved live-Source evidence;
- Level 8 reference-deployment evidence.

VALIDATION ARTIFACT
Only after all required validation succeeds, create:
- `docs/validation/phase-2-database-foundation.md`

Include concisely:
- title / Phase 2 identification;
- validation date/time;
- branch;
- authoritative package version `0.2.5` observed at validated source SHA;
- `PHASE2_BASE_SHA`;
- `VALIDATED_SOURCE_SHA`;
- explanation that validated SHA is the pre-evidence committed implementation/version tree;
- OS/platform;
- Node/npm versions;
- PostgreSQL version;
- required environment prerequisite names used, without values/secrets;
- each static/test command and PASS summary/counts;
- committed-range diff result;
- missing-admin DB-suite negative result;
- zero-selection negative result;
- disposable DB creation/migrate-from-zero/rerun/cleanup observations;
- transaction commit/rollback evidence summary;
- migration failure/checksum/status evidence summary;
- Web ready-DB physical observations;
- Web unavailable/uninitialized DB observations;
- Worker ready-DB physical observations;
- Worker failure/uninitialized DB observations;
- process independence observation;
- structural Phase 2 boundary checks;
- evidence levels established;
- explicit limitations/non-evidence levels;
- Phase 2 exit-gate mapping.

Do not paste full connection strings or enormous terminal logs. Preserve exact commands/procedures and concise observed results sufficient to prove execution.

SELF-REFERENCE / SHA TRUTHFULNESS
Follow the Phase 1 evidence convention:
- `VALIDATED_SOURCE_SHA` is the clean committed implementation/version tree actually tested before creating the artifact;
- the later documentation-only commit containing the artifact is an evidence-record commit and must not be represented as though its own SHA received the runtime/DB validation.

After creating the artifact, run structural/static checks appropriate to the artifact itself. Any subsequent runtime/source/test/migration/package change invalidates the Phase 2 implementation evidence and requires applicable revalidation.

PHASE 2 EXIT-GATE CHECK
Explicitly map observed results to every Phase 2 roadmap criterion:
- clean disposable PostgreSQL database can be created and migrated reproducibly from zero;
- Web/API and Worker connect through the shared database boundary;
- database tests exercise real PostgreSQL and verify cleanup;
- migration failures, database failures, schema incompatibility, and missing explicit DB-test prerequisites surface rather than silently skipping;
- shared transaction boundary has real commit/rollback evidence;
- Web liveness remains available when DB is down while readiness truthfully fails;
- Worker does not become ready when DB/schema readiness fails;
- migrations are explicit and not automatically applied by Web/Worker startup;
- final deterministic/static/DB/runtime validation passes on the exact accepted implementation tree;
- durable validation artifact records source identity, commands, environment, evidence levels, results, and limitations.

NO ROADMAP ADVANCEMENT
Successful P5 means Phase 2 implementation evidence is available for review/acceptance. Do not edit BOOT.md, README phase state, AGENTS.md, or roadmap to declare Phase 3 current unless a later explicit documentation workflow instructs that transition.

NON-GOALS
- Feature implementation or bug fixing during successful closeout.
- Publication/Source schema or bootstrap.
- Collection/live publisher validation.
- Browser testing.
- Deployment/reference-environment validation.
- Hosted CI.
- Phase 3 implementation.
- Automatic phase-state documentation changes.

DELIVERY / REVIEW NOTES
Report every executed command/procedure and distinguish deterministic tests, component/process observations, and real PostgreSQL evidence. If the required PostgreSQL environment cannot be used safely, report the limitation and leave Phase 2 unclosed rather than manufacturing a passing artifact.