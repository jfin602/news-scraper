TASK: Phase 2 / P6 — Final-tree PostgreSQL validation and Phase 2 closeout evidence

ROADMAP PHASE
Phase 2 — Database foundation.

VERSIONING
- This is Phase 2 prompt 6 and its assigned project version is `0.2.6`.
- On first execution confirm P5 left `package.json` at `0.2.5`, then advance it to `0.2.6` and synchronize npm-generated lockfile package metadata before establishing the final validated source SHA.
- If this same P6 prompt is rerun after failed/partial closeout and `package.json` is already `0.2.6`, retain `0.2.6`; do not increment again.
- `package.json` remains the sole authoritative project-version source.
- Do not duplicate the current version in README, BOOT, source constants, or other manually maintained documentation.

DEPENDS ON
- P1 disposable PostgreSQL test foundation implemented and reviewed.
- P2 shared database runtime/transaction boundary implemented and reviewed.
- P3 explicit SQL migration workflow implemented and reviewed.
- P4 Web/Worker database readiness integration implemented and reviewed.
- P5 Windows database-test lifecycle reliability + optional local `.env` command loading implemented and reviewed.
- All Phase 2 runtime/source/test changes from P1-P5 committed.
- A safe local PostgreSQL environment exists for the required Level 4 validation, including a persistent application/development database and a dedicated test-admin connection capable of creating disposable `news_scraper_test_*` databases.

GOAL
Advance the final Phase 2 prompt version, execute the complete Phase 2 local/static/regression/PostgreSQL/runtime validation matrix against one exact committed implementation tree, prove every Phase 2 roadmap/testing-contract exit criterion, and create the durable Phase 2 validation artifact.

This is a closeout/evidence task, not a feature-development task. Apart from its required `0.2.6` package/lock metadata transition, successful closeout must not change runtime behavior.

GOVERNING DOCUMENTS
Read current versions before execution:
- BOOT.md
- AGENTS.md
- docs/contracts/project-contract.md
- docs/contracts/testing-and-validation-contract.md
- docs/architecture/system-architecture.md
- docs/operations/security-reliability-and-operations.md
- docs/roadmap/mvp-roadmap.md
- README.md
- current package scripts/config/source/tests produced by P1-P5
- docs/validation/phase-1-application-foundation.md as the evidence-record format precedent

PRESERVED BEHAVIOR / RULES
- Do not advance BOOT/README/AGENTS/roadmap current phase to Phase 3 in this task.
- Do not claim database/runtime behavior as verified unless it was actually executed and observed.
- Do not use hosted CI/status checks as substitute evidence.
- Do not silently skip missing/unusable PostgreSQL prerequisites.
- Required selected suites must not pass with zero tests.
- Real persistence/transaction/migration/readiness claims require Level 4 real PostgreSQL evidence.
- No browser, approved-live-Source, or reference-deployment evidence is required for Phase 2.
- Validation evidence records observations; it does not redefine contracts.
- Do not use development/production application data as disposable test storage.
- Never include credential-bearing DB URLs, passwords, `.env` contents, or other real secret values in the validation artifact.
- Git-tracked migrations/migration infrastructure remain authoritative for schema structure/evolution; PostgreSQL remains authoritative for persisted runtime/applied database state.
- Web/API and Worker startup remain non-migrating.
- The persistent local development DB must not be targeted by disposable cleanup/failure procedures.

CRITICAL CLOSEOUT RULE
If validation reveals an implementation defect, contract violation, flaky/hanging test, false-green suite, migration/readiness error, unsafe output, leaked credential, cleanup failure, process that does not terminate, env-precedence defect, or other failure:
1. stop Phase 2 closeout;
2. report the exact failed invariant/command/procedure;
3. do not hide/fix runtime/test/package behavior inside this closeout task unless the current user explicitly authorizes a separate corrective implementation task;
4. return the defect to the owning P1/P2/P3/P4/P5 boundary;
5. retain assigned version `0.2.6` when rerunning this same P6 prompt;
6. after the fix is committed, rerun the P6 validation procedure from the beginning against the new final implementation tree.

Do not create an accepted Phase 2 validation artifact for a failing tree.

SOURCE MODIFICATION SCOPE
Before final-tree validation, this task may modify only:
- `package.json` for the required version transition;
- `package-lock.json` as npm's synchronized mechanical mirror.

After the validated implementation/version tree is committed and all required validation succeeds, create only:
- `docs/validation/phase-2-database-foundation.md`

During successful closeout do not modify runtime source, tests, migrations, scripts, dependencies, README, BOOT, AGENTS, roadmap, contracts, ADRs, or unrelated docs.

VERSION TRANSITION / PRE-VALIDATION COMMIT
Before establishing the validated source SHA:
1. inspect `package.json`;
2. if version is `0.2.5`, set it to `0.2.6` and synchronize `package-lock.json` using normal npm tooling without creating a git tag;
3. if already `0.2.6` because this same P6 prompt is being rerun, do not increment;
4. if another unexpected version is present, stop and report version-sequence drift;
5. confirm package.json and root package-lock package metadata are synchronized;
6. commit the P6 version-metadata transition so the implementation tree to be validated has a stable SHA.

PRECONDITION / TREE IDENTITY
After the P6 version transition is committed and before validation:
- require a clean working tree;
- record `VALIDATED_SOURCE_SHA=$(git rev-parse HEAD)` or platform equivalent;
- record branch;
- record OS/platform information;
- record Node version;
- record npm version;
- record PostgreSQL server version actually used for Level 4 evidence;
- confirm package.json version at `VALIDATED_SOURCE_SHA` is exactly `0.2.6`.

The validated source SHA is the committed P1-P5 implementation plus P6 version metadata, before the evidence artifact itself is created.

PHASE 2 IMPLEMENTATION BASE FOR DIFF VALIDATION
The testing contract requires committed-range whitespace/diff validation.

Determine the exact Phase 2 implementation base from repository history. The repository owner created an explicit `0.2.0` Phase 2 baseline before P1; use the final accepted baseline commit immediately before P1 implementation as `PHASE2_BASE_SHA` rather than assuming a stale hard-coded SHA.

Record:
- `PHASE2_BASE_SHA`
- `VALIDATED_SOURCE_SHA`

Run committed-range whitespace validation equivalent to:
- `git diff --check PHASE2_BASE_SHA...VALIDATED_SOURCE_SHA`

Do not substitute a clean-working-tree-only `git diff --check` that may inspect none of the committed Phase 2 implementation.

ENVIRONMENT / TOOL EVIDENCE
Record actual output for at least:
- `node --version`
- `npm --version`
- PostgreSQL server version for the dedicated test environment
- OS/platform identification appropriate to Windows

Confirm Node satisfies the package engines baseline and supports the P5 optional env-file command behavior used by repository scripts.

LOCAL ENVIRONMENT SAFETY PRECHECK
The developer's real root `.env` is operator-owned secret configuration and MUST NOT be printed/read into the artifact.

Before validation:
- confirm `.env` is ignored using `git check-ignore .env` or equivalent;
- confirm `.env` is not tracked using `git ls-files .env` or equivalent;
- do not display its contents;
- identify only the prerequisite variable names used: `NEWS_SCRAPER_DATABASE_URL` and `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL`.

If `.env` is accidentally tracked, stop closeout and report a secret/configuration-boundary defect.

REPRODUCIBLE INSTALL VALIDATION
Run:
- `npm ci`

This must succeed from the committed lockfile. Do not rely on a pre-existing `node_modules` state.

STATIC / ORDINARY DETERMINISTIC VALIDATION
Run each explicitly and record result:
- `npm run format:check`
- `npm run lint`
- `npm run typecheck`
- `npm test`
- `npm run test:unit`
- `npm run test:integration`
- `npm run check`
- committed-range `git diff --check PHASE2_BASE_SHA...VALIDATED_SOURCE_SHA`

Record useful test counts where available. `npm test` must remain the ordinary deterministic suite and must not require PostgreSQL or automatically consume the project-local `.env` merely because DB tests exist.

P5 LOCAL ENV-FILE COMMAND BOUNDARY CHECK
Inspect/execute enough to confirm the final tree preserves P5 behavior:
- `test:db`, `db:migrate`, `start:web`, and `start:worker` use optional root `.env` loading when it exists;
- explicit process/shell environment values override `.env`;
- ordinary `test`, `test:unit`, `test:integration`, and `check` do not automatically load `.env`;
- no dotenv dependency/application-level env-loader path was added;
- `.env` remains outside Git.

Do not record real values.

REQUIRED DATABASE SUITE — VALID PREREQUISITE
With a valid dedicated test-admin PostgreSQL prerequisite available through the intended local environment workflow, run:
- `npm run test:db`

This suite must exercise the complete Phase 2 real PostgreSQL behavior introduced by P1-P5, including:
- production DB runtime boundary;
- transaction behavior;
- disposable database lifecycle/isolation/cleanup;
- migrations and compatibility states;
- real Web/Worker PostgreSQL readiness processes;
- Windows child-process termination regression from P5.

The command must finish, report selected tests, clean disposable databases, and return control to the shell without manual intervention.

Record test counts/results and explicitly state that Level 4 evidence used real disposable PostgreSQL.

WINDOWS PROCESS-READINESS REGRESSION EVIDENCE
Because P5 corrected a Windows-specific hang, record explicit evidence that the final accepted tree no longer has it.

At minimum either the complete `npm run test:db` output or a focused additional run must demonstrate:
- `test/database/process-readiness.test.ts` executes its current-DB Web/Worker case;
- executes its schema-not-current case;
- child lifecycle events are observed as expected;
- Web/Worker children actually terminate;
- the test process exits;
- the command returns to PowerShell;
- no forced/manual kill is required for the passing path.

If focused execution is clearer, use the repository test wrapper with optional env-file loading equivalent to:
- `node --env-file-if-exists=.env scripts/run-tests.mjs "test/database/process-readiness.test.ts"`

Do not claim the old hang fixed merely because earlier DB tests printed green lines; observe process-readiness completion and shell return.

UNUSABLE/MISSING DB PREREQUISITE NEGATIVE WITH .ENV PRESENT
P5 intentionally allows `npm run test:db` to load a valid test-admin URL from `.env`, so merely unsetting the parent variable may no longer produce a missing-prerequisite case.

Physically invoke `npm run test:db` while explicitly overriding `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL` with a synthetic unusable value that takes precedence over `.env` (prefer a blank-after-trim value such as a single space if reliable in the current shell; otherwise use a clearly invalid synthetic PostgreSQL URL/value).

Expected:
- nonzero exit;
- clear explanation that the required database-test admin prerequisite is absent/invalid/unusable;
- no fallback to the valid `.env` value;
- no skipped-green result;
- no credential/environment dump.

This procedure simultaneously proves the specialized-suite prerequisite failure rule and explicit-environment-over-`.env` precedence.

Restore/remove the synthetic override before valid DB validation resumes. Do not edit the real `.env` to manufacture this negative.

ZERO-SELECTION NEGATIVE
Physically invoke the repository test wrapper with a deliberately nonexistent required pattern.

Expected:
- zero matching files detected;
- nonzero exit;
- clear no-tests-matched output.

This confirms Phase 1's required false-green protection remains intact after Phase 2 command/test restructuring.

PERSISTENT DEVELOPMENT DATABASE / EXPLICIT MIGRATION COMMAND
Prove the intended development workflow without using the persistent database as disposable test storage.

In a controlled shell where `NEWS_SCRAPER_DATABASE_URL` is not separately exported, invoke:
- `npm run db:migrate`

Allow the optional ignored `.env` to provide the persistent development application URL.

Expected:
- command connects through `NEWS_SCRAPER_DATABASE_URL`;
- migration workflow runs explicitly and reports applied/current state safely;
- rerunning reports current/repeat-safe state;
- no credential-bearing URL is printed;
- no Web/Worker startup is used to apply migrations.

Do not perform destructive/failure experiments against the persistent development DB.

PHYSICAL DISPOSABLE DATABASE / MIGRATION PROCEDURE
In addition to automated `test:db`, perform or surface a concise physical procedure against real PostgreSQL that proves the roadmap exit gate end-to-end.

Using only the dedicated test-admin connection:
1. create a unique disposable `news_scraper_test_*` database using the repository test helper or same validated safe mechanism;
2. verify the created database exists;
3. explicitly override ordinary `NEWS_SCRAPER_DATABASE_URL` for the relevant child command/process to point at that disposable DB; do not rely on the persistent development URL from `.env`;
4. run the explicit migration command from zero;
5. query/inspect migration status and confirm current;
6. rerun migrations and confirm repeat-safe/no duplicate migration history;
7. surface a representative committed transaction and rolled-back transaction through the production DB boundary (automated Level 4 tests may satisfy this if result evidence is unambiguous);
8. close all application/test connections;
9. drop the disposable database through the safe test helper;
10. query the admin catalog and confirm the database no longer exists.

Record generated disposable database name only if it contains no secret. Never record the full connection URL.

IMPORTANT P5 PRECEDENCE SAFETY
Whenever P6 targets a specially created disposable application DB, the explicit `NEWS_SCRAPER_DATABASE_URL` override MUST take precedence over any persistent URL in `.env`.

Before destructive/failure-oriented runtime procedures, confirm structurally/procedurally that the process is using the intended disposable/synthetic target without printing credentials. Never let `.env` silently redirect a disposable validation step to the persistent development database.

MIGRATION FAILURE / COMPATIBILITY EVIDENCE
Ensure executed DB tests prove and the artifact records at least:
- migration from untouched database;
- rerun/current behavior;
- failed migration rollback/not-recorded behavior;
- changed applied migration/checksum incompatibility;
- pending/uninitialized/incompatible status behavior;
- verified cleanup after the test database lifecycle.

Do not manufacture migration failure manually against shared/persistent development data.

PHYSICAL WEB/API RUNTIME VALIDATION — READY DATABASE
Run the real Web/API entrypoint/package script against a migrated/current disposable PostgreSQL database using an explicit application-URL environment override.

Use safe local host and port 0 if supported.

Observe and record:
1. startup command/procedure with secrets redacted;
2. `web.listening` actual listener evidence;
3. actual host/port;
4. `GET /health/live` status/body;
5. `GET /health/ready` status/body;
6. graceful SIGTERM/SIGINT action appropriate to Windows/platform;
7. `web.stopped` or equivalent clean shutdown evidence;
8. actual successful process exit and shell return.

Expected:
- live = HTTP 200;
- ready = HTTP 200 with migrated/current DB.

PHYSICAL WEB/API RUNTIME VALIDATION — UNAVAILABLE DB
Start the real Web entrypoint/package script with an explicit syntactically valid but intentionally unavailable/nonexistent PostgreSQL application target using synthetic credentials/values that override `.env`.

Expected:
- Web listener still starts;
- `/health/live` remains HTTP 200;
- `/health/ready` is HTTP 503;
- process can shut down cleanly and exit;
- output/body does not expose the synthetic password/raw URL.

Do not point this negative at the persistent development database and do not intentionally disrupt the shared PostgreSQL server.

PHYSICAL WEB SCHEMA-NOT-CURRENT VALIDATION
Against a reachable disposable DB intentionally left uninitialized or otherwise not current, with explicit application-URL override:
- Web listens;
- live = 200;
- ready = 503;
- readiness check does not auto-create/apply migrations;
- process shuts down and exits cleanly.

This proves readiness includes schema compatibility and remains non-mutating.

PHYSICAL WORKER RUNTIME VALIDATION — READY DATABASE
Run the real Worker entrypoint/package script against a migrated/current disposable PostgreSQL database while Web is not running, using an explicit application-URL override.

Observe and record:
1. startup command/procedure with secrets redacted;
2. `worker.ready`;
3. confirmation it remains running after readiness;
4. SIGTERM/SIGINT shutdown action appropriate to Windows/platform;
5. `worker.stopped` or equivalent clean stop;
6. actual successful process exit and shell return.

Confirm Web host/port config is absent/not required.

PHYSICAL WORKER DATABASE FAILURE VALIDATION
Run the real Worker entrypoint with explicit synthetic/disposable application-URL overrides against:
- a syntactically valid unavailable/nonexistent DB target; and
- a reachable disposable DB whose schema is uninitialized/not current, if this is not already covered by equivalent executed DB-process evidence.

Expected:
- no `worker.ready`;
- safe `worker.start_failed` or equivalent established failure event;
- nonzero exit;
- actual process termination;
- no auto-migration;
- no leaked synthetic credentials/raw DB errors.

Do not target the persistent development DB for these negatives.

PROCESS / ARCHITECTURE STRUCTURAL CHECKS
Inspect final source and record concise results for:
- no production `src/` consumption of `NEWS_SCRAPER_TEST_DATABASE_ADMIN_URL`;
- no production source reading/parsing `.env` through a second dotenv/application-level loader; optional env loading remains the intended command boundary;
- no domain Publication/Source/Article/Collection-run/Category/duplicate schema pulled into Phase 2;
- no indie-author/publishing-topic condition/data in shared database/process source;
- Web and Worker both use the canonical shared `src/database` boundary rather than separate pools;
- no Web→Worker or Worker→Web process dependency;
- no automatic application startup migration;
- Worker still has no HTTP health server;
- test-only Windows IPC/process helpers remain outside production `src/`;
- package.json and lockfile root package metadata both equal `0.2.6`.

DISPOSABLE DATABASE CLEANUP AUDIT
At the end of Level 4 procedures, query/inspect the test-admin PostgreSQL catalog safely and confirm no disposable `news_scraper_test_*` database created by the validation remains.

If cleanup fails or an unexpected disposable database from the current run remains, Phase 2 closeout fails until the owning defect is corrected and validation reruns.

Do not drop arbitrary similarly named databases without proving they belong to the current safe disposable test mechanism.

EVIDENCE LEVELS TO RECORD
Phase 2 closeout should claim only actual evidence:
- Level 1: format/lint/type/static/diff/version/module/env-command/ignore checks;
- Level 2: deterministic configuration, migration discovery/status, package-command, and lifecycle helper unit behavior;
- Level 3: Web HTTP/component/process and Worker process integration behavior where controlled dependencies are used;
- Level 4: real disposable PostgreSQL creation/cleanup, queries, transactions, migrations, schema compatibility, DB-backed Web/Worker readiness, and Windows DB-process lifecycle completion.

Explicitly state Phase 2 does not establish:
- Level 5 deterministic collection-fixture evidence;
- Level 6 browser evidence;
- Level 7 approved live-Source evidence;
- Level 8 reference-deployment evidence.

VALIDATION ARTIFACT
Only after all required validation succeeds, create:
- `docs/validation/phase-2-database-foundation.md`

Include concisely:
- title / Phase 2 identification;
- validation date/time;
- branch;
- authoritative package version `0.2.6` observed at validated source SHA;
- `PHASE2_BASE_SHA`;
- `VALIDATED_SOURCE_SHA`;
- explanation that validated SHA is the pre-evidence committed implementation/version tree;
- OS/platform;
- Node/npm versions;
- PostgreSQL version;
- prerequisite environment variable NAMES used, without values/secrets;
- confirmation `.env` was ignored/untracked without recording contents;
- each static/test command and PASS summary/counts;
- committed-range diff result;
- valid `test:db` Level 4 result;
- Windows process-readiness completion/shell-return observation;
- unusable/missing-admin DB-suite negative result with explicit-env-over-`.env` precedence;
- zero-selection negative result;
- persistent development `db:migrate` explicit/current observation without destructive testing;
- disposable DB creation/migrate-from-zero/rerun/cleanup observations;
- transaction commit/rollback evidence summary;
- migration failure/checksum/status evidence summary;
- Web ready-DB physical observations;
- Web unavailable/uninitialized DB observations;
- Worker ready-DB physical observations;
- Worker failure/uninitialized DB observations;
- process independence observation;
- structural Phase 2 boundary checks;
- evidence levels established;
- explicit limitations/non-evidence levels;
- Phase 2 exit-gate mapping.

Do not paste full connection strings, `.env` contents, passwords, or enormous terminal logs. Preserve exact safe commands/procedures and concise observed results sufficient to prove execution.

SELF-REFERENCE / SHA TRUTHFULNESS
Follow the Phase 1 evidence convention:
- `VALIDATED_SOURCE_SHA` is the clean committed implementation/version tree actually tested before creating the artifact;
- the later documentation-only commit containing the artifact is an evidence-record commit and must not be represented as though its own SHA received the runtime/DB validation.

After creating the artifact, run structural/static checks appropriate to the artifact itself. Any subsequent runtime/source/test/migration/package change invalidates the Phase 2 implementation evidence and requires applicable revalidation.

PHASE 2 EXIT-GATE CHECK
Explicitly map observed results to every Phase 2 roadmap + inherited testing-contract criterion:
- PostgreSQL connection/configuration works through the shared boundary;
- clean disposable PostgreSQL databases can be created and migrated reproducibly from zero;
- development DB migration workflow is explicit and repeat-safe;
- Web/API and Worker connect through the shared database boundary;
- database tests exercise real PostgreSQL and verify cleanup;
- migration failures, database failures, schema incompatibility, and missing/unusable explicit DB-test prerequisites surface rather than silently skipping;
- shared transaction boundary has real commit/rollback evidence;
- Web liveness remains available when DB is down while readiness truthfully fails;
- Worker does not become ready when DB/schema readiness fails;
- migrations are explicit and not automatically applied by Web/Worker startup;
- Windows DB-backed process tests terminate reliably and bounded process waits prevent indefinite hangs;
- local optional `.env` command behavior works without committing secrets and explicit environment overrides remain authoritative;
- ordinary deterministic suites remain independent of automatic `.env` loading;
- final deterministic/static/DB/runtime validation passes on the exact accepted implementation tree;
- durable validation artifact records source identity, commands, environment, evidence levels, results, and limitations.

NO ROADMAP ADVANCEMENT
Successful P6 means Phase 2 implementation evidence is available for review/acceptance. Do not edit BOOT.md, README phase state, AGENTS.md, or roadmap to declare Phase 3 current unless a later explicit documentation workflow instructs that transition.

NON-GOALS
- Feature implementation or bug fixing during successful closeout.
- Publication/Source schema or bootstrap.
- Collection/live publisher validation.
- Browser testing.
- VPS/reference-deployment validation.
- Hosted CI.
- Secret-manager implementation.
- Phase 3 implementation.
- Automatic phase-state documentation changes.

DELIVERY / REVIEW NOTES
Report every executed command/procedure and distinguish deterministic tests, component/process observations, and real PostgreSQL evidence. On Windows, explicitly report whether the previously problematic DB process-readiness suite completed and returned to the shell. Use explicit disposable/synthetic application-URL overrides for failure/isolated runtime procedures so the persistent development database in `.env` is never accidentally targeted. If the required PostgreSQL environment cannot be used safely, report the limitation and leave Phase 2 unclosed rather than manufacturing a passing artifact.