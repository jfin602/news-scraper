TASK: Correction 1 / P2 — Test validation topology closeout

CORRECTION CLOSEOUT SCOPE
This is the sole final closeout prompt for correction `c1-test-fix`. P1 should have made the ordinary local Node/check path portable, separated PHP-backed browser evidence from ordinary browser evidence, preserved fail-closed PHP suites, and added focused regression protection for command containment.

This closeout independently reviews the P1 implementation and completes the correction using the ordinary local validation gate. Under the current owner-approved testing contract, VPS-required PHP/PHP-browser execution is **not** a default correction-closeout requirement; it remains deferred to the later explicitly designated full-system/project release qualification gate for the final integrated release candidate.

The earlier P1 prompt text that described VPS evidence as required before this correction closeout GREEN is superseded by the current `docs/contracts/testing-and-validation-contract.md` Qualification-gate policy. Preserve P1 as historical execution input; do not reinterpret its earlier VPS handoff wording as current governing authority.

This closeout does not add product behavior, change package version, activate the 3.0 roadmap, resolve unrelated issues, run live/provider/deployment proof, or redesign test infrastructure.

MODEL / REASONING / USAGE
- Recommended configuration: `Sol Light`.
- Model-family basis: repository policy uses Sol Light for independent correction closeout so final source/evidence review gets stronger base code-reading capability than the implementation pass without paying for unnecessary deep implementation reasoning.
- Reasoning basis: Light is appropriate because the correction is a narrow package/test-topology change; this prompt must perform the required three review passes and stop if source inspection exposes a deeper testing-architecture problem.
- Complexity / quality floor: `High`.
- Estimated usage: `Moderate`.
- Lower-cost alternative considered: `Terra High`; not selected because `docs/codex-model-selection.md` deliberately sets `Sol Light` as the normal independent correction-closeout floor.
- Escalation trigger: coverage was silently dropped, portable/specialized ownership is still ambiguous, command containment requires shared-runner redesign, or another defect cannot be resolved within this review-only closeout boundary.
- Escalation target: `Terra High` for a behavior-preserving structural remediation handoff; genuinely ambiguous contract/architecture work requires fresh planning.
- Estimate confidence: `High`.
- Efficiency rationale: inspect one bounded P1 diff, run one non-overlapping portable regression set locally, preserve specialized fail-closed suites by source/contract review, and defer VPS runtime proof to the final integrated qualification gate instead of repeating it per correction.

VERSIONING
- Required unchanged project version: `2.0.0`.
- This closeout MUST NOT change `package.json` version.
- Do not create `package-lock.json` or `npm-shrinkwrap.json`.
- This correction consumes no 3.0 roadmap patch number and does not activate `2.1.0`.
- The final result remains HUMAN REVIEW REQUIRED even when all current-gate evidence is green.

WRITE BOUNDARY
This closeout is review-only for executable/test topology.

It MAY write only the durable validation artifact after the P1 executable/test tree is genuinely green under this closeout's local validation manifest. It MUST NOT repair `package.json`, scripts, tests, production code, migrations, runtime config, contracts, roadmap state, or known issues in-place.

If a defect is found, stop and issue the required remediation/replanning handoff. After repair, rerun the applicable local evidence before rerunning this closeout. Do not request VPS evidence merely because a remediation occurred; VPS evidence remains owned by the later full-system/project release qualification gate unless an explicit owner decision assigns an earlier gate.

MANDATORY HANDOFF
Before evaluating GREEN:
1. read current `BOOT.md`, `AGENTS.md`, `docs/contracts/testing-and-validation-contract.md`, `docs/codex-model-selection.md`, `known-issues.md` entry `V7MT`, and this correction's P1 prompt;
2. identify the exact reachable runner-owned P1 implementation commit for `c1-test-fix/P1` and the correction base immediately before it;
3. require top-level package exactly `2.0.0`, no npm lockfile, and no uncommitted executable/test changes before closeout validation begins;
4. inspect P1's complete diff, current `package.json`, `scripts/run-tests.mjs`, `scripts/run-php-tests.mjs`, the focused command-contract unit test, ordinary browser directory, separate PHP-browser directory, and moved PHP customer browser test;
5. confirm no substantive browser/PHP test was deleted, weakened, skipped, or converted to mocked evidence merely to make local validation green;
6. confirm the current testing contract classifies PHP CLI/runtime and PHP-backed customer browser execution as VPS-required for normal Codex prompts and assigns that evidence by default to the later full-system/project release qualification gate;
7. do **not** look for `.codex-runs/c1-test-fix-vps-evidence.txt`, do not require a VPS receipt, and do not run or retry the VPS-only commands locally.

GREEN CORRECTION GOAL
The correction is GREEN at this ordinary correction-closeout gate when the accepted P1 tree proves all of the following:
- `npm test` is the ordinary portable deterministic unit/integration/collection regression aggregate and does not invoke PHP;
- `npm run check` composes portable static checks plus `npm test` and succeeds on the normal local Windows environment without PHP;
- `npm run test:php` remains a separately invokable substantive PHP CLI/runtime suite and retains fail-closed prerequisite behavior by source/retained contract;
- `npm run test:browser` owns ordinary non-PHP browser tests only;
- `npm run test:browser:php` owns the PHP-backed customer/server browser test separately;
- ordinary and PHP-backed browser selections are substantive and disjoint, with the pre-correction PHP browser behavior preserved in source;
- no specialized suite was silently skipped, converted to pass-on-missing-prerequisite, or deleted;
- no automatic retry path was added for deterministic prerequisite failures;
- command containment has meaningful automated regression protection;
- current-gate local evidence passes on the accepted P1 tree;
- PHP runtime and PHP-backed browser runtime proof are explicitly recorded as `DEFER → final full-system/project release qualification`, not passed, waived, or required at this closeout;
- package remains `2.0.0`, no lockfile exists, and no product/roadmap/database/Gemini behavior changed.

PASS 1 — CONTRACT / EVIDENCE REVIEW
Review the actual P1 diff and evidence rather than trusting its final report.

A. PORTABLE AGGREGATES
Verify from current executable scripts:
- `npm test` selects the intended unit/integration/collection suites and contains no `test:php` invocation;
- `npm run check` remains format-check + lint + typecheck + portable `npm test` (or an equivalent contract-compliant portable composition);
- neither portable aggregate invokes PHP, PHP-backed browser, live Source, live Gemini, deployment, or another specialized environment suite;
- the correction did not create a broad always-run `check:vps`/`check:all` replacement that defeats change-aware selection.

B. PHP SPECIALIZED SUITE — SOURCE/CONTRACT PRESERVATION
Verify without claiming runtime execution:
- `test:php` still invokes `scripts/run-php-tests.mjs` or an equivalent fail-closed PHP runner;
- the runner still exits nonzero when the PHP CLI cannot start rather than silently skipping;
- PHP syntax checks and `integrations/php/tests/run.php` execution remain present;
- P1 did not change PHP product behavior merely to make local validation portable;
- runtime PASS for this specialized suite remains unobserved by this closeout and is deferred to final qualification.

C. BROWSER OWNERSHIP
Verify:
- the PHP customer example test was structurally moved rather than deleted/rewritten to evade the ordinary glob;
- its real PHP server, scenario matrix, no-upstream-request assertions, publisher-link behavior, XSS/safety checks, and Playwright observation remain materially intact in source;
- `test:browser` selects ordinary browser tests without selecting the PHP-backed file;
- `test:browser:php` selects the PHP-backed file and would fail zero-match if it disappears under the shared test runner;
- both selections remain bounded through `scripts/run-tests.mjs`;
- runtime PASS for `test:browser:php` is not claimed here and remains deferred to final qualification.

D. FOCUSED REGRESSION
Inspect the new command-contract unit test. It must protect durable ownership/containment rather than merely assert one giant literal package-script string.

Require proof that:
- portable `test` does not transitively invoke PHP;
- `check` does not explicitly re-add VPS-only evidence;
- PHP runner command remains present;
- ordinary and PHP-backed browser selections are disjoint and nonempty;
- the PHP customer browser test is owned only by the specialized browser command.

E. SHARED RUNNER PRESERVATION
If `scripts/run-tests.mjs` changed, require a concrete necessity and focused regression for every altered behavior. The planned solution needs no generic negative-glob/exclusion feature. Verify zero-match failure, deterministic de-duplication/order, global setup, concurrency parsing, and child-status propagation remain intact.

F. QUALIFICATION-GATE DISPOSITION
Verify the validation manifest is interpreted correctly:
- PHP runtime and PHP-backed browser runtime are `VPS-REQUIRED` but remain `DEFER` at this ordinary correction closeout;
- their later owning gate is the explicitly designated full-system/project release qualification gate for the final integrated candidate;
- absence of a VPS receipt is not RED and is not a reason to block correction GREEN;
- the closeout does not imply that deferred PHP/PHP-browser runtime behavior was observed.

PASS 2 — ERROR / EDGE-CASE ADVERSARIAL REVIEW
Independently challenge the final topology. At minimum disposition these hypotheses:
- `npm test` still calls PHP indirectly through another script alias;
- `check` explicitly adds `test:php` after `npm test`;
- the PHP browser file remains under an ordinary browser glob despite the new command;
- the PHP browser file is moved but its relative repository-root calculation is now wrong due directory depth;
- `test:browser:php` points at a typo/empty glob and the focused command-contract test fails to detect it;
- the ordinary browser glob accidentally excludes one or more pre-existing non-PHP files;
- a pre-existing non-PHP browser test was moved or deleted accidentally;
- P1 weakened `run-php-tests.mjs` to return success when PHP is absent;
- P1 added hidden retry/skip/environment-sniffing logic to make local checks green;
- command-contract tests only inspect text loosely enough that PHP could leak back into portable aggregates unnoticed;
- command-contract tests are so overfit to one exact script string that harmless future composition changes become unnecessarily brittle;
- local `npm run check` passes only because unit/integration/collection membership was reduced;
- closeout mistakenly requires a per-correction VPS receipt despite the current Qualification-gate policy;
- closeout mistakenly reports deferred PHP/PHP-browser runtime behavior as passing;
- package version, roadmap state, or product files changed despite the correction being non-versioned test infrastructure.

Classify every plausible concern as:
- protected by source + meaningful current-gate executed evidence;
- deferred runtime claim owned by final qualification;
- correction defect requiring remediation and fresh local evidence;
- Terra High structural-refactor handoff required;
- replanning/contract work required.

Do not declare GREEN with unresolved local coverage-loss, fail-open prerequisite logic, zero-match ownership, command-containment, or scope-drift concerns. Do not treat correctly deferred VPS runtime proof as an unresolved closeout defect.

PASS 3 — CODE QUALITY / STRUCTURAL REVIEW
Inspect P1 for maintainability and unnecessary machinery.

Look specifically for:
- adding exclusion/negative-glob complexity to `run-tests.mjs` when moving one isolated file suffices;
- duplicating the shared test runner for the PHP browser suite;
- adding environment sniffing that silently changes what named commands mean;
- broad `check:vps`/`check:all` aggregates that undermine prompt-specific necessity selection;
- hard-coded Windows/VPS branching inside generic tests where command ownership alone is enough;
- duplicated PHP prerequisite logic outside `run-php-tests.mjs`/the PHP-backed test's actual launch boundary;
- brittle command-contract tests tied to irrelevant formatting rather than semantic containment;
- stale directory/test naming that obscures which environment owns the suite.

The intended final structure should be obvious to a future maintainer from `package.json` and test directories without a new framework.

TERRA HIGH REFACTOR HANDOFF
If Pass 3 finds a meaningful behavior-preserving structural refactor is required, do not implement it. Stop and output exactly one self-contained `Terra High` remediation prompt that:
- identifies the observed structural defect and evidence;
- defines the smallest behavior-preserving refactor boundary and consumers to trace;
- preserves portable/specialized command semantics, fail-closed prerequisites, substantive coverage, package `2.0.0`, and unrelated product behavior;
- forbids product/contract/roadmap expansion and speculative abstractions;
- requires focused tests plus every invalidated local `RUN` item;
- preserves VPS PHP/PHP-browser evidence as deferred to final full-system/project release qualification rather than turning remediation into a VPS gate;
- does not declare the correction GREEN.

If the issue instead requires a product/contract decision or genuinely different architecture, stop for normal replanning rather than disguising it as a refactor.

VALIDATION MANIFEST
Closeout execution environment: normal Windows Codex environment.

RUN locally — final accepted P1 tree:
1. `npm run check`;
2. `npm run test:browser`;
3. `npm run codex:phase:validate -- c1-test-fix`;
4. `git diff --check <correction-base>...<P1-commit>` or equivalent committed-range validation;
5. working-tree `git diff --check` after writing only the final durable validation artifact.

Containment:
- do not separately rerun `npm test`, `test:unit`, `test:integration`, `test:collection`, or the focused unit test after successful `npm run check` solely to duplicate contained evidence.

DEFER — later owning gate is final full-system/project release qualification:
- `npm run test:php` → `VPS-REQUIRED`;
- `npm run test:browser:php` → `VPS-REQUIRED`.

These DEFER items do not block this correction closeout. Do not execute them locally and do not require an intermediate exact-tree VPS receipt.

N/A at this correction closeout:
- database/recovery suites;
- security suite;
- live Sources;
- live Gemini/provider;
- reference deployment.

DURABLE VALIDATION ARTIFACT
Only after all three review passes are resolved and every current-gate local requirement is genuinely green, create:

`docs/validation/c1-test-fix-validation.md`

Record concisely:
- correction purpose and exact correction base/P1 commit;
- package `2.0.0` and no-lockfile state;
- files/topology changed;
- local environment versions and exact local commands/results;
- command containment and no-local-coverage-loss conclusions;
- source/contract confirmation that `test:php` and `test:browser:php` remain substantive separately invokable fail-closed suites;
- explicit `DEFER → final full-system/project release qualification` disposition for PHP runtime and PHP-backed browser runtime, with no claim that those suites passed at this closeout;
- Pass 1/2/3 findings/dispositions;
- limitations, if any;
- explicit statement that the artifact records current-gate evidence and does not itself resolve `known-issues.md`, activate roadmap state, or constitute final project qualification.

After adding the artifact, run working-tree `git diff --check`. Do not rerun the portable matrices solely because the documentation-only artifact was added.

FINAL RESPONSE
Return one of:

GREEN — HUMAN REVIEW REQUIRED
- exact P1 commit accepted for this correction;
- local portable/browser evidence summary;
- three-pass review disposition;
- validation artifact path;
- explicit note that `test:php` and `test:browser:php` remain deferred to final full-system/project release qualification and were not run/claimed here;
- unchanged `2.0.0` / no lockfile / no product scope;
- note that V7MT documentation status remains a later `/docs-review` → approval → `/docs-apply` action after human acceptance if still applicable.

RED / REMEDIATION REQUIRED — HUMAN REVIEW REQUIRED
- concrete current-gate defect/risk;
- whether bounded remediation, Terra High refactor handoff, or replanning is required;
- which local evidence becomes invalid and must be rerun;
- keep VPS evidence deferred unless an explicit owner decision changes its owning gate.

Never claim tests, PHP/browser runtime behavior, live/provider behavior, or VPS state that was not actually observed.
