TASK: Phase 17 / P7 — Phase 17 closeout validation

ROADMAP PHASE
Phase 17 — Article and duplicate moderation.

MODEL / REASONING / USAGE
- Recommended configuration: `Sol Light`.
- Model-family basis: repository policy deliberately uses Sol's stronger independent code-reading/review capability at roadmap closeout while keeping reasoning light unless a concrete defect proves deeper analysis is needed.
- Reasoning basis: Light reasoning is sufficient for the explicit three-pass procedure: contract/evidence verification, independent adversarial edge-case review, and separate structural/code-quality review with bounded fixes or escalation when needed.
- Complexity / quality floor: `High`.
- Estimated usage: `High`.
- Lower-cost alternative considered: `Terra High`; not selected because current repository policy intentionally establishes `Sol Light` as the normal independent closeout floor rather than optimizing closeout down to the implementation workhorse.
- Escalation trigger: any pass exposes material architectural ambiguity, difficult cross-subsystem root cause, unresolved topology/transaction/concurrency ownership, security-boundary uncertainty, or another defect that cannot be confidently repaired inside already-governed Phase 17 behavior.
- Escalation target: `Terra Ultra` when the dominant need is deeper reasoning over explicit PostgreSQL/state interactions; use a higher-effort Sol configuration when the blocker requires stronger independent cross-system inference or contract reconciliation. Meaningful behavior-preserving structural refactors use the separate `Terra High` handoff below.
- Estimate confidence: `High`.
- Efficiency rationale: the three independent passes, bounded-fix rule, explicit escalation conditions, and Terra High refactor handoff provide strong final review without pre-paying for deep reasoning on a well-specified phase.
- If this exact configuration is unavailable at execution time, re-read current `BOOT.md`, `docs/codex-model-selection.md`, and `scripts/codex-phase-core.mjs`; do not invent a configuration label.

VERSIONING
- This is Phase 17 prompt 7 and its assigned project version is `0.17.7`.
- On first execution, require P6 to have left top-level `package.json` at `0.17.6`, then advance only the top-level package version to `0.17.7` before establishing the final Phase 17 candidate tree.
- On a rerun of this same P7, retain `0.17.7`; do not increment again.
- Any other package version is sequence drift: stop and report it.
- `package.json` remains the sole current-version authority.
- Do not create `package-lock.json` or `npm-shrinkwrap.json`.
- A later successful conversational `/closeout` separately establishes the Phase 18 baseline `0.18.0`; that transition is not part of P7.

MANUAL CLOSEOUT BOUNDARY
This is the sole final manual closeout prompt for the Phase 17 task stack. `npm run codex:phase -- p17` must stop after P6 and must not execute P7 automatically.

P7 validates the exact integrated Phase 17 implementation, repairs only concrete bounded defects already inside the governed Phase 17 scope, performs the required independent adversarial and structural passes, and writes durable evidence only for the final accepted candidate tree.

Do not pull Phase 18 configurable HTML collection, Phase 19 deployment/operations work, Phase 20 launch validation, Phase 21 refactoring, or unrelated feature ideas into Phase 17.

MANDATORY HANDOFF
Before any P7 write/version action:
1. read current `BOOT.md`, `docs/codex-model-selection.md`, the complete Phase 17 roadmap entry, and P1–P6 task files;
2. read `docs/contracts/domain-and-data-contract.md`, `docs/contracts/source-and-collection-contract.md`, `docs/contracts/article-lifecycle-and-deduplication.md`, `docs/contracts/public-feed-and-admin-contract.md`, `docs/contracts/testing-and-validation-contract.md`, and relevant admin/security/transaction guidance;
3. inspect the actual final P1–P6 source/tests rather than assuming prompt prose describes what Codex implemented;
4. inspect the final migration chain and all Phase 17 moderation persistence, Article moderation/provenance, duplicate topology/manual authority, public-feed effective-state, admin API, and browser UI code;
5. trace important unchanged producers/consumers: Worker Article persistence/Relevance Category reconciliation, Phase 16 duplicate detector/grouping, Source/Category administration, canonical `/api/feed`, public browser presentation, admin perimeter/request-integrity middleware, and database test harness;
6. inspect runner-owned P1–P6 commit history and establish the Phase 17 implementation base/range for final diff checks; prompt-writing/documentation commits are not implementation evidence;
7. confirm Level 7 live-Source evidence and Level 8 Cloudflare/direct-origin deployment observation are not Phase 17 requirements solely because moderation was added;
8. stop and report sequence/planning drift if P1–P6 are incomplete, package baseline is unexpected, or implementation materially differs from the governed Phase 17 boundary.

DURABLE VALIDATION ARTIFACT
Create/update only after evidence is gathered on the final candidate tree:

`docs/validation/phase-17-article-and-duplicate-moderation.md`

If a Terra High refactor handoff is emitted, do not finalize or mark this artifact GREEN until remediation is applied and P7 is rerun from the beginning against the resulting tree.

The artifact must identify at minimum:
- validation date/time and relevant environment;
- exact final Phase 17 source SHA/tree accepted after every bounded fix and completed remediation;
- package version `0.17.7`;
- implementation range/base used for inspection/diff validation;
- Node/npm/PostgreSQL/Playwright Chromium versions where observed and relevant;
- every validation command/procedure actually run and its result/counts where useful;
- evidence levels actually achieved;
- moderation persistence/effective-value evidence;
- Article search/provenance/history evidence;
- duplicate manual-authority/topology/concurrency evidence;
- canonical public-feed effective state/discovery evidence;
- admin request-integrity/API/browser evidence;
- Pass 2 hypotheses/findings/fixes/tests/disposition;
- Pass 3 structural findings and whether a Terra High remediation handoff occurred;
- limitations, unavailable prerequisites, retries, skipped/zero-test conditions;
- explicit statement that local browser evidence does not prove Cloudflare Access deployment protection and that ordinary Level 7/Level 8 evidence is not required by this phase;
- Phase 17 exit-gate conclusion as GREEN or blocked/red;
- intended next phase if GREEN: Phase 18 — Configurable HTML collection;
- statement that conversational `/closeout` is still required for the separate `0.18.0` baseline transition.

PASS 1 — CONTRACT / EVIDENCE
Validate the final tree against the actual Phase 17 contracts and roadmap exit gate.

A. MODERATION PERSISTENCE / EFFECTIVE STATE — LEVEL 4
Prove against disposable PostgreSQL:
- migration from zero creates the final Phase 17 persistence and retains all Phase 16 constraints;
- Source-derived `display_title` remains separate from active display override;
- active override survives later Source Article update and clear reveals the latest Source title;
- automatic `article_categories` continue advancing under an active manual override;
- active manual Category set, active empty set, and cleared/no-override state remain distinct;
- clear exposes the latest automatic Category set;
- manual-separation canonical pairs and Primary authority metadata satisfy database constraints;
- `audit_events` are bounded and append-only through ordinary administration;
- no Article/observation/provenance deletion/cascade is introduced.

B. STORED ARTICLE ADMINISTRATION / PROVENANCE — LEVEL 2 + LEVEL 3 + LEVEL 4
Prove:
- bounded deterministic search/filter/pagination works over stored Articles rather than public eligibility;
- visible, hidden, archived, ungrouped, Primary, non-Primary, and review-participating rows remain discoverable as governed;
- malformed/repeated/out-of-bound criteria fail closed;
- Article detail distinguishes Source-derived vs effective headline and automatic vs effective Categories;
- endpoint/run/observation/Relevance/Category provenance is inspectable and read-only;
- hide/restore is reversible and does not mutate duplicate role, Category state, or provenance;
- display/Category set/clear commands are transactional;
- successful material mutations append coherent bounded history while failed/rolled-back/no-op commands do not claim success;
- Worker/Web race behavior leaves coherent manual and Source-derived state.

C. DUPLICATE MODERATION / MANUAL AUTHORITY — LEVEL 4
Prove:
- manual dismiss persists and unchanged evidence remains dismissed;
- manual split leaves selected Articles ungrouped, dissolves invalid singleton residual groups, creates separation authority, and updates directly affected existing candidate disposition coherently;
- materially unchanged evidence cannot reconstruct a manual split;
- materially changed evidence may become reviewable but cannot automatically defeat active separation;
- separation protects indirect group merges, not only the original evidence pair;
- automatic same-group/add-member/group-merge operations retain one valid manual Primary when present;
- two groups with different manual Primaries do not auto-merge;
- explicit manual merge resolves separation conflicts intentionally and requires explicit Primary when multiple manual choices conflict;
- choose-Primary updates the one canonical group Primary and survives later automatic reevaluation;
- split removing a manual Primary leaves a valid deterministic automatic Primary when a residual group survives;
- concurrent manual/automatic topology operations converge under the shared PostgreSQL topology gate without overlapping memberships, invalid Primary, or lost manual authority;
- injected failure including audit failure rolls back the governed duplicate mutation;
- all Articles/observations/Category/Relevance provenance remain retained.

D. PUBLIC FEED / DISCOVERY — LEVEL 3 + LEVEL 4 + LEVEL 6 REGRESSION
Prove through the one canonical feed path:
- visible ungrouped and visible Primary Articles remain eligible;
- visible non-Primary remains suppressed;
- hidden Primary still does not resurrect a visible non-Primary;
- manual Primary changes affect which member can represent the group;
- active display override controls headline while stored `original_url` remains the publisher destination;
- keyword search composes with effective headline without losing earlier governed searchable fields;
- Category filter uses manual memberships only while override marker is active, including active-empty behavior;
- clearing returns to latest automatic Category behavior;
- Source/Category/q AND behavior, chronological ordering, keyset cursors, page bounds, root `/`, and `/api/feed` remain unchanged otherwise;
- public browser loading/empty/error/discovery/external-link/responsive behavior remains green in real Chromium.

E. ADMIN API / BROWSER — LEVEL 3 + LEVEL 6
Prove:
- admin-disabled moderation routes/page workspace are unavailable as governed;
- central request-integrity header is required on every unsafe moderation route;
- strict query/body/resource validation yields bounded 400/404/409 semantics without SQL/stack leakage;
- Article list/detail/provenance/history workflow is usable;
- hide/restore, display override set/clear, Category set/empty/clear work through browser/API;
- duplicate queue/evidence/dismiss/merge/split/choose-Primary work through browser/API;
- manual Primary conflict requires explicit operator resolution rather than silent arbitration;
- stale/conflicting browser actions recover without duplicate requests or permanently disabled controls;
- keyboard/focus/error behavior and representative desktop/mobile layouts are usable;
- existing Publication/Source/Editorial administration remains functional after Phase 17 additions.

F. EARLIER-PHASE REGRESSION
Confirm the integrated final matrix protects the affected earlier boundaries, especially:
- Source-scoped Article identity/idempotency/observation rollback;
- Relevance/Category automatic reconciliation and retained reasons;
- Phase 16 duplicate detector/grouping/effect accounting;
- public discovery/keyset behavior;
- Source/endpoint/Publication/Editorial admin request-integrity and relationships;
- scheduler/Worker/collection behavior implicated by shared Article/duplicate helpers;
- public and admin browser regressions.

STATIC / ARCHITECTURE INSPECTION
Confirm:
- shared engine remains topic independent;
- singleton Publication remains configuration, not a relational tenant key;
- Source-derived Article identity/provenance remains distinct from moderation state;
- `original_url` remains read-only publisher destination;
- `article_categories` remains automatic state and manual Categories are layered separately;
- `duplicate_groups.primary_article_id` remains the sole Primary ID authority;
- automatic and manual topology mutation use one cross-process PostgreSQL ownership boundary;
- no generic Article PATCH/editor, native admin identity, semantic duplicate engine, public moderation, or related-story clustering was introduced;
- admin controllers do not own database/topology business logic;
- one canonical public eligibility/feed path remains;
- no Phase 18+ scope was pulled forward.

PASS 2 — ERROR / EDGE-CASE ADVERSARIAL REVIEW
After Pass 1 is green on a candidate tree, perform a separate fresh review derived from actual source rather than merely checking existing test names.

At minimum challenge:
- Source title changing multiple times under an active display override, then clear;
- automatic Categories changing to nonempty/empty/different sets beneath active manual nonempty/empty override, then clear;
- missing/deleted Category reference during an operator set request;
- repeated/no-op hide/restore/override commands and audit cardinality;
- stale Article list cursor after mutations and tie-heavy stored-Article ordering;
- large/invalid/repeated admin query/body fields and bounded error output;
- Article moderation racing Worker identity/material update on the same Article;
- split racing automatic grouping on the same/crossing pair;
- indirect regroup where the blocking separation does not equal the current evidence pair;
- changed evidence after manual split;
- automatic merge between two manual-Primary groups;
- manual merge with zero/one/multiple manual Primaries and invalid explicit Primary;
- split of current manual Primary, split of all-but-one member, and repeated split;
- hidden/archived Articles as Primary/non-Primary through hide/restore/topology actions;
- candidate disposition after split and later detector reevaluation;
- audit failure or unexpected DB error mid-mutation;
- stale browser action after another actor changed candidate/group state;
- effective Category/title joins interacting with q/source/category/cursor and non-Primary suppression;
- accidental provenance deletion or editable identity fields;
- admin error details leaking SQL/internal state;
- performance regressions from unbounded/N+1 admin or public reads at intended MVP scale.

Classify each plausible finding as:
1. already protected by implementation plus meaningful executed evidence;
2. governed behavior clear but coverage weak — add/strengthen a bounded regression;
3. concrete bounded Phase 17 defect — repair and add focused regression coverage where practical;
4. meaningful behavior-preserving refactor — stop and use the Terra High handoff below;
5. material ambiguity/hard defect — stop for model escalation/replanning/correction.

Do not invent policy during closeout. After any bounded source/test change, rerun focused evidence and every broader suite invalidated before continuing.

PASS 3 — CODE-QUALITY / STRUCTURAL REVIEW
After Pass 1 and Pass 2 are green on the current candidate tree, independently review the complete Phase 17 implementation diff plus important touched consumers.

Inspect for:
- duplicate effective-title/Category calculations that can drift between admin/public paths;
- two competing duplicate topology mutation/lock implementations;
- a second Primary authority or manual role duplication;
- unnecessarily generic moderation persistence/API abstractions;
- SQL/business logic leaking into Express controllers or browser JavaScript;
- unbounded/N+1 provenance/history/review reads;
- duplicated admin request/error/mutation handling;
- frontend Article state tangled through unrelated Publication/Source/Editorial logic;
- dead/compatibility-only code inconsistent with pre-production simplification policy;
- weak rollback/concurrency tests or implementation-detail assertions that miss the actual invariant;
- error swallowing, raw JSON dumps, secrets/SQL/stack leakage;
- unnecessary dependencies/frameworks;
- documentation/task comments that misstate Source-vs-effective, automatic-vs-manual Category, identity-vs-duplicate, or visibility-vs-Primary semantics.

Small local cleanup or one bounded defect may be fixed with appropriate rerun evidence. Do not perform a meaningful structural refactor under Sol Light.

TERRA HIGH REFACTOR HANDOFF
If Pass 2 or Pass 3 finds a meaningful behavior-preserving structural refactor, stop before implementing it and output exactly one self-contained transient remediation prompt recommending `Terra High`.

That remediation prompt must:
- identify the observed structural problem and concrete evidence;
- define the smallest behavior-preserving refactor boundary and important producers/consumers;
- preserve Phase 17 contracts, Source-scoped identity, automatic/manual state separation, duplicate topology/manual authority, provenance, security/request integrity, public/admin behavior, and failure isolation;
- forbid unrelated cleanup, new behavior, contract/ADR changes, roadmap expansion, Phase 18+ features, and speculative abstractions;
- preserve package version `0.17.7` and consume no roadmap prompt number;
- require focused tests plus every broader regression/evidence level invalidated by the refactor;
- require a concise implementation report but must not declare Phase 17 GREEN or finalize the validation artifact;
- instruct Terra High to stop if the work actually requires a product/contract/ADR change, materially different architecture decision, or reasoning beyond its reliable boundary.

After remediation is applied and implementation-level validation is green, rerun P7 from the beginning. Only that rerun may establish the accepted final tree and GREEN artifact.

FINAL VALIDATION — SMALLEST NON-OVERLAPPING MATRIX
On the unchanged final candidate tree, run:
- clean dependency installation using the repository's current npm/no-lockfile policy, recording the exact command/result;
- `npm run check`
- `npm run test:db`
- `npm run test:browser`
- `npm run codex:phase:validate -- p17`
- committed/final Phase 17 implementation-range `git diff --check` or equivalent that actually checks the accepted change range.

Do not separately rerun `npm test`, `npm run test:unit`, `npm run test:integration`, or `npm run test:collection` after successful `npm run check` on the same unchanged final tree solely to duplicate contained evidence.

Any explicitly required suite that is unavailable, silently skips required tests, or matches zero tests blocks GREEN. Mocks do not substitute for PostgreSQL concurrency/transaction claims or real Chromium browser proof.

No ordinary Level 7 live-Source run and no Level 8 Cloudflare/direct-origin deployment procedure is required solely for Phase 17 moderation. Do not claim either unless separately and actually observed.

GREEN EXIT REQUIREMENTS
Declare Phase 17 GREEN only when all are true:
- important automatic Article/duplicate decisions are inspectable and reversible through the governed admin surface;
- Source updates do not clobber active display override or manual effective Categories;
- clearing overrides reveals latest Source-derived/automatic state;
- manual split and manual Primary authority cannot be silently undone by materially unchanged automatic duplicate processing;
- manual merge/split/choose-Primary preserve all topology invariants and provenance under concurrency/rollback;
- moderation never erases Article/observation/Relevance/Category provenance;
- public feed consumes effective display/Category state without changing `original_url`, eligibility, suppression, ordering, or cursor guarantees;
- request-integrity/API/browser moderation workflows pass at the required evidence levels;
- Pass 2 completed with every finding resolved, covered, handed off, or explicitly non-blocking;
- Pass 3 completed with every meaningful structural finding resolved through bounded fix or completed Terra High remediation/rerun, with no unresolved material refactor need;
- all required final-tree regressions/evidence are green;
- durable artifact truthfully records both independent review passes, fixes/tests, any remediation, limitations, and exact accepted tree;
- no later-phase scope was pulled forward;
- final range/diff validation is clean.

If GREEN, report accepted final SHA/tree, validation artifact path, evidence levels/results, Pass 2 outcome, Pass 3 outcome, any Terra High handoff/remediation outcome, bounded fixes/tests added, and that the next conversational action is `/closeout` for the separate `0.18.0` Phase 18 baseline transition.

Do not advance `package.json` to `0.18.0` or rewrite roadmap/root current-phase summaries to Phase 18 inside P7.