TASK: Phase 19 / P7 — Phase 19 closeout validation

ROADMAP PHASE
Phase 19 — Reliability, observability, and production operations.

MODEL / REASONING / USAGE
- Recommended configuration: `Sol Light`.
- Model-family basis: repository policy deliberately uses Sol's stronger independent code-reading/review capability at roadmap closeout, and Phase 19 additionally requires integrated local, PostgreSQL, recovery, security, browser, upgrade, and deployment evidence.
- Reasoning basis: Light reasoning is the owner-approved closeout baseline; Stage 1 uses the required three distinct Codex review passes plus explicit escalation/refactor handoff, while Stage 2 deliberately transfers real VPS/reference-deployment execution to the human operator.
- Complexity / quality floor: `Critical`.
- Estimated usage: `Very High`.
- Lower-cost alternative considered: `Terra High`; not selected because repository closeout policy intentionally uses Sol Light for independent final review and the owner also approved Sol Light for this Phase 19 closeout.
- Escalation trigger: any Stage 1 pass exposes material architectural ambiguity, difficult recovery/schema/transaction root cause, uncertain Cloudflare/origin trust boundary, unresolved cross-process capacity ownership, or another defect that cannot be confidently repaired inside already-governed Phase 19 behavior.
- Escalation target: `Terra Ultra` when the dominant need is deeper reasoning over explicit state/recovery interactions; use `Sol High` or `Sol Ultra` when the blocker requires stronger independent cross-system/security inference. Meaningful behavior-preserving structural refactors use the separate Terra High remediation handoff below.
- Estimate confidence: `High`.
- Efficiency rationale: Stage 1 uses the smallest non-overlapping final validation matrix and independent closeout review; Stage 2 then collects only the real deployment evidence that cannot honestly be produced by local automation or repository inspection.
- Plan delta: `Owner-adjusted` after `/prompt-write`: P7 is now a hard two-stage handoff, with Codex Stage 1 certification separated from operator-controlled VPS/reference-deployment validation.

VERSIONING
- This is Phase 19 prompt 7 and its assigned project version is `0.19.7`.
- On first execution require P6 to have left top-level `package.json` at `0.19.6`, then advance only the top-level version to `0.19.7` before establishing the final Phase 19 candidate tree.
- On a rerun of this same P7 retain `0.19.7`; do not increment again.
- Any other package version is sequence drift: stop and report it.
- Do not create `package-lock.json` or `npm-shrinkwrap.json`.
- A later successful conversational `/closeout` separately establishes the Phase 20 baseline `0.20.0`; that transition is not part of P7 and Phase 20 acceptance, not P7, establishes the first supported production schema/data baseline.

TWO-STAGE MANUAL CLOSEOUT BOUNDARY
This is the sole final manual closeout prompt for the Phase 19 stack. `npm run codex:phase -- p19` must stop after P6 and must not execute P7 automatically.

P7 is deliberately split into two stages:

- **Stage 1 — Codex closeout certification:** Codex validates the exact integrated Phase 19 candidate, repairs only concrete bounded defects already inside governed Phase 19 scope, performs the required three independent review passes, runs the final deterministic/local/PostgreSQL/security/recovery/browser/upgrade evidence, and creates the durable Phase 19 validation artifact with an explicit Stage 1 result and Stage 2 operator handoff.
- **Stage 2 — Operator-controlled VPS/reference-deployment validation:** the human operator deploys and tests the exact Stage 1 candidate through copy/paste commands and external browser/client observations. Codex must not impersonate the operator, fabricate VPS results, automate an Access identity, or claim Level 8 evidence before the operator actually observes it.

Stage 1 GREEN means only that the exact candidate is ready for operator deployment certification. It is **not** Phase 19 GREEN. Phase 19 becomes GREEN only after every required Stage 2 observation is recorded against the same executable candidate and the durable artifact is finalized accordingly.

If Stage 2 exposes any implementation defect that requires a source, migration, package, runtime-configuration contract, or test change, the candidate is invalidated. Return to Stage 1, apply/review the bounded repair through the proper workflow, rerun all invalidated evidence, establish a new exact candidate, and restart Stage 2 from the beginning. Do not combine Stage 1 evidence from one executable tree with Stage 2 evidence from another.

STAGE 1 — MANDATORY HANDOFF
Before any P7 write/version action:
1. read current `BOOT.md`, `docs/codex-model-selection.md`, complete Phase 19 roadmap entry, P1-P6 task files, testing contract, operations/reliability document, architecture, production-data/schema ADR, and Cloudflare Access ADR;
2. inspect actual final P1-P6 source/tests/docs rather than assuming task prose matches implementation;
3. inspect runner-owned P1-P6 commit history and establish the Phase 19 implementation base/range for final diff review;
4. trace P1 operational snapshot through P2 admin consumer, P3 tuned capacity/timing/health policy, P4 security suite, P5 backup/restore/retention, and P6 deployment/schema/reference validation tooling;
5. trace important preserved consumers: scheduler/jobs/recovery, endpoint locking/capacity, collection/network safety, RSS/HTML parsing, normalization/Relevance/identity/provenance, duplicate/moderation, public feed, and admin resource integrity;
6. confirm package/task sequence and `npm run codex:phase:validate -- p19` grammar;
7. inspect the operator-facing deployment/runbook/reference-validator interfaces that Stage 2 will use and verify they are specific enough for copy/paste operation without inventing deployment capabilities;
8. stop if P1-P6 are incomplete, material repository drift invalidates the plan, or Stage 2 cannot be performed safely using the implemented deployment/recovery interfaces.

DURABLE VALIDATION ARTIFACT — TWO-STAGE LIFECYCLE
Stage 1 creates:

`docs/validation/phase-19-reliability-observability-production-operations.md`

Create the artifact only after the Stage 1 candidate is internally coherent and all required Stage 1 evidence is green. At Stage 1 completion its status must be exactly and prominently represented as:

`STAGE 1 GREEN — OPERATOR DEPLOYMENT VALIDATION REQUIRED`

That status is intentionally not Phase 19 GREEN.

The Stage 1 artifact must identify at minimum:
- Stage 1 validation date/time and environment;
- exact Phase 19 executable candidate SHA/tree and package version `0.19.7`;
- implementation base/range;
- Node/npm/PostgreSQL/browser/tool versions actually observed during Stage 1;
- exact deterministic, database, security, recovery, browser, and representative upgrade commands/procedures actually run;
- evidence levels actually achieved in Stage 1;
- operational snapshot/dashboard evidence;
- measured/tuned capacity/timing/health evidence and final selected values;
- security/abuse regression conclusions and bounded fixes if any;
- backup creation, restore, semantic data-preservation, post-restore recovery, retention behavior, and observed duration evidence from the controlled Stage 1 recovery procedure;
- schema preflight/representative pre-launch upgrade/rollback-restore preservation evidence;
- deployment/runbook/reference-validator readiness evidence;
- Pass 2 adversarial hypotheses/findings/fixes/disposition;
- Pass 3 structural findings and whether a Terra High remediation handoff occurred;
- limitations, retries, unavailable evidence, skipped/zero-selected conditions;
- explicit statement that no Level 8 Cloudflare/direct-origin result is claimed by Stage 1;
- an ordered Stage 2 operator checklist with blank result/evidence fields for each required real-deployment observation;
- explicit statement that Phase 19 validates the procedure intended to govern production but Phase 20 customer-launch acceptance establishes the first supported production baseline;
- intended next phase only if Stage 2 later becomes GREEN: Phase 20 — Customer launch validation;
- statement that conversational `/closeout` remains required after final Phase 19 GREEN for the separate `0.20.0` baseline transition.

After Stage 2 is completed, the existing artifact may be finalized with the operator-observed evidence. If the executable candidate did not change, finalization is an evidence-only continuation of P7 and does not require rerunning Stage 1 merely because the artifact text changes. If executable source, migrations, package version, runtime behavior, or governed deployment configuration changes, Stage 1 must be rerun as described above.

STAGE 1 — CODEX CLOSEOUT CERTIFICATION

PASS 1 — CONTRACT / EVIDENCE
Validate the integrated final tree against the Phase 19 roadmap and governing contracts, excluding the real reference-deployment observations explicitly reserved for Stage 2.

A. OPERATIONAL SNAPSHOT / ALERTS
Prove:
- one canonical server-side operational snapshot owns health/queue/alert semantics;
- endpoint health reuses canonical health derivation and does not confuse lifecycle/operational state with health;
- ready/future/running jobs and oldest-ready delay are truthful;
- alert/status derivation is deterministic, bounded, topic independent, and non-authoritative;
- snapshot reads do not mutate jobs/endpoints/runs;
- database failure cannot become fabricated green state;
- no unbounded/N+1 query behavior exists at intended MVP scale;
- protected admin API exposes only bounded non-secret operational facts.

B. ADMIN OPERATIONS DASHBOARD — LEVEL 6
Observe in Chromium:
- Operations workspace loads and navigates correctly;
- healthy/empty, delayed/degraded/unhealthy, queue-delay, and backend-error/recovery states render truthfully;
- hostile Source/endpoint/error text remains inert;
- refresh does not create stale overwrite/duplicate-request behavior;
- keyboard/focus/live-region/mobile behavior is usable;
- links navigate to existing Source/endpoint administration rather than duplicating authority;
- Publication/Sources/Editorial/Articles workspaces remain functional;
- browser does not implement health/queue/alert calculations.

C. RUNTIME / CAPACITY TUNING — LEVEL 4
Confirm:
- measurement procedure is repeatable and records the evidence used for final values;
- existing global/Source/host cross-process authority remains coherent;
- unrelated Source/host work progresses under localized saturation;
- blocked capacity defers safely without retry/run consumption;
- endpoint lock remains independent;
- DB-pool headroom is sufficient for selected global capacity;
- Worker lease/renewal/recovery timing relationships are valid;
- delayed-health behavior avoids normal-jitter false positives;
- values were not changed merely for ceremony; unchanged values are justified by evidence;
- no second semaphore/rate-limit/lease architecture exists.

D. SECURITY / ABUSE
Run and inspect the specialized security evidence. Confirm adversarial coverage for:
- Source/endpoint approval and state gating;
- scheme/domain/port/DNS/address SSRF policy including special-use and mapped-address cases;
- redirect revalidation and validated-address transport binding;
- fetch/response/decompression/parser bounds;
- RSS/HTML media-type isolation;
- static HTML no-subresource/no-Article-page/no-browser-fetch behavior;
- preview no-network/no-persistence behavior;
- inert public/admin content;
- admin request integrity, body/JSON bounds, and real resource relationships;
- CSP/error/secret leakage;
- Web/Worker structured failure diagnostics.

Do not count or simulate real Cloudflare Access/direct-origin proof here. That belongs only to Stage 2.

E. BACKUP / RESTORE / RETENTION — LEVEL 4
Using real disposable PostgreSQL/native tooling, prove:
- backup command fails clearly if prerequisites are absent and never logs credentials;
- failed/interrupted backup is not represented as valid completed backup;
- restore defaults to a safe explicit fresh target rather than silently replacing current DB;
- representative Publication/Source/endpoint/Category/Relevance/Article/observation/run/job/duplicate/moderation/history state survives semantically;
- restored repositories/services can read expected state;
- queued/expired/in-flight job/run recovery follows existing semantics without unsafe duplicate replay;
- corrupt/incompatible restore fails closed;
- retention pruning only deletes managed eligible backup artifacts and invalid policy deletes nothing;
- actual backup/restore duration evidence is recorded where measurable.

F. SCHEMA UPGRADE / ROLLBACK / DEPLOYMENT PROCEDURE
Using controlled/disposable state, prove:
- schema status/preflight distinguishes uninitialized/current/pending/incompatible safely;
- representative earlier pre-launch DB state upgrades through actual migration files to current;
- seeded governed data/relationships survive;
- application reads/writes work after upgrade;
- pre-upgrade backup permits safe rollback/restore when in-place schema reversal is unsupported;
- the test/evidence explicitly does not promote arbitrary old pre-production DBs to supported production inputs;
- deployment procedure identifies exact source/version, backup, preflight, migration, Web/Worker startup/readiness, smoke checks, operational verification, and rollback decision points;
- required incident runbooks point to real commands/controls and do not invent capabilities;
- the reference-deployment validator classifies public/admin/direct-origin outcomes safely without claiming real Cloudflare evidence from local/fake HTTP tests.

STAGE 1 FINAL NON-OVERLAPPING COMMAND MATRIX
Re-read current package scripts before execution and use the smallest matrix that covers all Stage 1 evidence exactly once.

Expected shape if scripts remain as planned:
- `npm install --ignore-scripts`
- `npm run codex:phase:validate -- p19`
- `npm run check`
- `npm run test:db`
- `npm run test:security`
- `npm run test:recovery`
- `npm run test:browser`
- relevant P6 upgrade/reference-validator command if not contained above
- `git diff --check <phase-19-base>...HEAD`

Do not separately rerun `test:unit`, `test:integration`, or `test:collection` after `npm run check` merely to duplicate contained evidence. If P4/P5/P6 script names differ on the final tree, map to the actual equivalent and record the commands used.

Any required suite selecting zero tests or silently skipping a required prerequisite is a failure, not green evidence.

PASS 2 — ERROR / EDGE-CASE ADVERSARIAL REVIEW
After Pass 1 is green on a candidate tree, independently derive realistic failure hypotheses from the actual implementation rather than existing test names.

At minimum challenge:
- operational snapshot during empty DB, all-paused Sources, queued-only backlog, expired-running jobs, DB interruption, and clock-boundary cases;
- alert severity/order with simultaneous health and queue problems;
- stale/out-of-order dashboard refresh response;
- capacity saturation plus scheduler/recovery activity and DB-pool exhaustion pressure;
- Worker shutdown while capacity/job/lease work is active;
- security suite blind spots created by shared helper assumptions;
- backup destination collision, partial file, disk/path error, symlink/foreign file, tool absence, wrong DB target, corrupt archive, restore failure, and cleanup failure;
- secrets embedded in intentionally hostile connection/path/tool stderr sentinels;
- restore containing queued/running/expired jobs and interrupted Collection runs in combinations not covered by normal tests;
- migration checksum mismatch, missing repository migration, partial migration failure, concurrent migration attempt, and application rollback against an incompatible upgraded schema;
- deployment failure after backup but before migration, during migration, after migration before Worker start, and after Web start before operational verification;
- Cloudflare Access lockout where public feed remains healthy;
- origin exposure by alternate port/hostname/IP or deployment misconfiguration;
- redirect behavior during unauthenticated admin probe that could be mistaken for application success;
- authorized admin session succeeding only because direct origin was used rather than the Access path;
- monitoring ownership missing when an alert is intentionally induced;
- restore/RPO/RTO claims that Stage 2 could not substantiate with the actual deployment procedure;
- artifact accidentally containing secret-bearing URLs/tokens/cookies;
- Phase 19 wording accidentally declaring the Phase 20 production baseline early.

Classify each finding:
1. protected by implementation plus meaningful observed evidence;
2. clear governed behavior but weak coverage — add bounded regression/evidence;
3. concrete bounded Phase 19 defect — repair and rerun invalidated evidence;
4. meaningful behavior-preserving refactor — stop and emit Terra High remediation prompt;
5. material ambiguity/hard defect — stop for model escalation/replanning/correction.

PASS 3 — CODE-QUALITY / STRUCTURAL REVIEW
After Pass 1 and Pass 2 are green on the current candidate tree, independently inspect the Phase 19 diff and important producers/consumers for:
- duplicated operational SQL or health/alert derivation across backend/browser;
- observability code becoming workflow authority;
- P3 measurement harness entangled with production runtime;
- capacity/timing constants copied into multiple modules;
- duplicated security tests that add runtime without new coverage;
- backup child-process/security logic scattered across scripts;
- proprietary backup abstractions over native PostgreSQL tooling without need;
- restore safety checks duplicated/inconsistent across test/operator code;
- migration preflight reimplementing migration-ledger semantics;
- deployment validator containing provider-specific assumptions not governed by the repo;
- runbooks referencing nonexistent commands or stale paths;
- unbounded logs/errors/metrics or secret-bearing diagnostics;
- dead compatibility code inconsistent with pre-production simplification policy;
- unnecessary dependencies/infrastructure frameworks;
- redundant final test commands or unnecessarily slow setup inconsistent with the test-efficiency contract;
- documentation drift about Phase 20 baseline establishment.

Small local cleanups/obvious bounded defects may be fixed inside Stage 1 with invalidated evidence rerun.

TERRA HIGH REFACTOR REMEDIATION HANDOFF
If Pass 2 or Pass 3 finds a meaningful behavior-preserving structural refactor, stop before implementing it and output exactly one self-contained transient `Terra High` remediation prompt as required by current `BOOT.md`/model-selection policy.

That remediation prompt must:
- identify the observed structural problem/evidence;
- define the smallest behavior-preserving refactor boundary and producers/consumers;
- preserve package version `0.19.7` and consume no new phase prompt number;
- preserve security, data integrity, idempotency, provenance, recovery, admin/public behavior, and Phase 20 baseline semantics;
- forbid unrelated cleanup/new features/ADR changes;
- require focused tests plus all invalidated broader evidence;
- instruct Terra High to stop if the work actually requires product/contract/architecture redesign.

After remediation is applied, rerun Stage 1 from the beginning. Only a fresh Stage 1 GREEN candidate may be handed to the operator.

STAGE 1 EXIT GATE AND HANDOFF
Stage 1 may report `STAGE 1 GREEN — OPERATOR DEPLOYMENT VALIDATION REQUIRED` only when:
- required deterministic/database/security/recovery/browser/representative-upgrade evidence is green on the exact Stage 1 candidate;
- restore is actually tested in controlled real PostgreSQL, not merely documented;
- Source failures/queue delay are observably represented by the implemented operational boundary and deterministically tested;
- security coverage includes SSRF, unsafe content, secret leakage, fetch limits, application-side admin perimeter assumptions, and request integrity;
- deployment/rollback and required failure runbooks are internally usable and point to real controls;
- representative pre-launch schema upgrade/data preservation and rollback/restore evidence proves the procedure intended for production;
- Pass 2 and Pass 3 have no unresolved findings;
- no evidence claim exceeds what Stage 1 actually observed;
- the durable artifact exists with the exact Stage 1 candidate identity and an ordered Stage 2 checklist.

At that point Codex must stop implementation work and hand control to the human operator. Do not attempt Stage 2 automatically.

The Stage 1 final report must include:
- exact Stage 1 candidate SHA/tree and package version `0.19.7`;
- deterministic/database/security/recovery/browser/upgrade command results;
- backup/restore and representative schema-upgrade/rollback conclusions;
- final tuned runtime policy;
- Pass 2 and Pass 3 findings/disposition and whether remediation occurred;
- durable validation artifact path;
- explicit `STAGE 1 GREEN — OPERATOR DEPLOYMENT VALIDATION REQUIRED` status;
- explicit statement that no Level 8 Cloudflare/direct-origin evidence has yet been claimed;
- concise operator handoff telling the user to begin Stage 2 against this exact candidate.

STAGE 2 — OPERATOR-CONTROLLED VPS / REFERENCE-DEPLOYMENT VALIDATION
Stage 2 is human-operated. The operator performs the deployment-sensitive actions manually through copy/paste commands on the project VPS and performs required Cloudflare/origin observations from an external browser/client where the boundary cannot be proven from inside the VPS.

Codex must not:
- SSH into or operate the VPS on the user's behalf unless the user separately provides an authorized tool surface that makes the action explicitly operator-controlled;
- run a giant unattended production-validation script that continues after failures;
- automate or fake an authorized Cloudflare Access identity;
- treat localhost/origin requests made from inside the VPS as proof that an Internet client cannot bypass Cloudflare;
- infer Level 8 success from config files, screenshots, local mocks, or Stage 1 validator tests.

The preferred operator workflow is one numbered copy/paste block at a time. Each block must state the expected safe result and stop on a material mismatch before the next deployment action is supplied.

STAGE 2 CANDIDATE INTEGRITY
Before deployment testing:
1. confirm the VPS checkout/deployment corresponds to the exact Stage 1 executable candidate and `0.19.7`;
2. confirm required environment/secrets exist without printing their values;
3. confirm PostgreSQL/native backup tooling and required operator commands are present;
4. record the current deployed source/version/schema/process state before modification;
5. confirm the Stage 1 artifact identifies the same executable candidate.

A docs-only artifact-finalization commit may be newer than the executable candidate only when executable source, migrations, package version, runtime configuration semantics, and deployment behavior are unchanged and that relationship is recorded explicitly. Any executable change invalidates Stage 1.

STAGE 2 ORDERED DEPLOYMENT / EXIT-GATE PROCEDURE
The operator must observe and record, in this order unless the actual P6 runbook establishes a safer equivalent order:

1. **Pre-deployment state and prerequisites**
   - exact candidate SHA/version;
   - Web/Worker/PostgreSQL/process-supervision arrangement;
   - schema status;
   - backup location/tool availability;
   - required environment present without secret disclosure.

2. **Real pre-deployment backup**
   - create a real backup through the P5 operator path before risky schema/application changes;
   - confirm the backup is complete/recognized and record bounded metadata/duration;
   - never print credentials.

3. **Real schema preflight**
   - run the P6 schema-status/preflight against the reference database;
   - stop on `incompatible`, checksum/history mismatch, missing migration, or ambiguous state.

4. **Safe deployment/migration sequencing**
   - quiesce Worker/Web only as required by the implemented runbook;
   - apply migrations only through the existing migration engine;
   - verify schema becomes current;
   - start/restart Web and Worker in the documented safe order.

5. **Public Web/API observation**
   - canonical public root is reachable with expected bounded behavior;
   - basic public API/readiness behavior works as applicable;
   - public feed remains independent from admin Access availability.

6. **Worker/scheduler/PostgreSQL observation**
   - Worker is actually running;
   - scheduler/consumer/recovery diagnostics show operational progress or healthy idle behavior;
   - PostgreSQL connectivity/current schema is confirmed without exposing credentials.

7. **Operational dashboard observation**
   - deployed Operations reflects the real endpoint/job state;
   - Source failures/queue delay are visible when real durable state warrants them;
   - intentionally paused/disabled/archived resources are not falsely treated as unhealthy;
   - unexpected backlog/failure state is investigated rather than ignored.

8. **Real backup/restore demonstration**
   - create/use a real reference-deployment backup according to the deployment procedure;
   - demonstrate restore into a non-production/safe fresh target;
   - verify restored schema/application readability and representative semantic state;
   - do not replace the active reference database merely to satisfy the test.

9. **Deployment/rollback/runbook usability**
   - validate the schema/deployment/rollback decision points to the extent practical without destroying the active deployment;
   - confirm required incident runbooks point to real commands/controls for failing Source triage, parser/profile pause, duplicate false-positive correction, stuck/expired job recovery, backup/restore, schema rollback, Cloudflare lockout, unsafe Source response, and legal/editorial takedown.

10. **Unauthenticated Cloudflare Access observation — external client required**
    - from an external browser/client without an authenticated Access session, request the admin surface;
    - observe Cloudflare challenge/denial;
    - confirm the application admin UI is not returned directly.

11. **Authorized operator Cloudflare Access observation — external browser required**
    - authenticate through the intended Cloudflare Access path;
    - confirm the authorized operator can reach the admin surface and Operations workspace;
    - do not record Access cookies/tokens.

12. **Direct-origin bypass observation — external client required**
    - identify the actual origin-protection mechanism: Cloudflare Tunnel, firewall restriction, authenticated origin connectivity, or documented equivalent;
    - perform the bounded authorized direct-origin/IP/alternate-host test appropriate to that mechanism from outside the VPS;
    - confirm the application admin surface cannot be successfully reached while bypassing Cloudflare Access;
    - a localhost request from the VPS is not sufficient proof.

13. **Operational ownership / recovery policy**
    - record monitoring/alert owner;
    - record recovery/incident owner;
    - record actual backup schedule and retention;
    - record chosen RPO/RTO targets informed by the observed backup/restore procedure;
    - record relevant Cloudflare/admin access-log, application change-history, infrastructure/IP-log, and bounded Raw-item/source-metadata retention/access ownership required before launch.

14. **Final post-deployment smoke**
    - re-confirm exact deployed candidate/version and current schema;
    - re-confirm Web readiness, root `/`, `/api/feed`, Worker/scheduler, Operations, protected admin path, and current backup availability;
    - record any limitation/retry truthfully.

STAGE 2 REFERENCE-DEPLOYMENT SECURITY
- Never record Access cookies/tokens, database URLs/passwords, origin secrets, private keys, or full secret-bearing URLs in terminal transcripts or the artifact.
- It is acceptable to redact sensitive origin identifiers while preserving enough mechanism/result detail to make the evidence auditable.
- Direct-origin testing must be bounded and authorized against this project's own deployment.
- If any required Stage 2 item cannot be observed, Phase 19 is not GREEN. Do not downgrade the requirement or infer success.

STAGE 2 EVIDENCE RETURN / ARTIFACT FINALIZATION
After the operator completes Stage 2, return the observed command outputs/results to the reviewing assistant/Codex for evidence transcription and final artifact review. This continuation may update only the durable validation artifact and other explicitly required evidence text unless a defect was found.

If no executable candidate change occurred, finalize the existing artifact with:
- Stage 2 date/time/environment;
- exact deployed candidate/version relationship;
- VPS process/deployment arrangement;
- real backup and safe restore observations/durations;
- real schema/deployment/rollback observations;
- public Web/API, Worker/scheduler, PostgreSQL, and Operations observations;
- Cloudflare unauthenticated result;
- authorized-operator result;
- direct-origin bypass result;
- actual origin-protection mechanism;
- monitoring/recovery owner, backup schedule/retention, chosen RPO/RTO, and required log/data retention/access ownership;
- limitations/retries;
- final Phase 19 exit-gate decision.

Artifact finalization must not reinterpret or embellish operator output. If the operator did not observe it, the artifact must not claim it.

PHASE 19 FINAL EXIT GATE
P7 may declare final Phase 19 GREEN only when all are true across Stage 1 + Stage 2:
- restore is actually tested, not merely documented;
- Source failures/queue delay are observable;
- security coverage includes SSRF, unsafe content, secret leakage, fetch limits, admin perimeter/origin assumptions, and request integrity;
- deployment/rollback and required failure runbooks are usable and validated where practical;
- representative pre-launch schema upgrade/data preservation and rollback/restore evidence is sufficient to establish the procedure intended to govern production;
- actual Level 8 reference-deployment evidence confirms unauthenticated admin challenge/denial, authorized operator access, failed direct-origin bypass, and identifies actual origin protection;
- public Web/API, Worker/scheduler, PostgreSQL, operational dashboard, and real backup/restore behavior have been observed on the reference deployment as required;
- monitoring/recovery ownership, backup retention, RPO/RTO, and required retention/access ownership are recorded;
- required deterministic/database/security/recovery/browser suites are green on the exact Stage 1 executable candidate used for Stage 2;
- Pass 2 and Pass 3 have no unresolved findings;
- no evidence claim exceeds what was actually observed;
- Phase 20 customer-launch acceptance remains explicitly identified as the first supported production schema/data baseline.

PRESERVE / FORBID
Do not:
- declare Phase 20 complete or establish the production baseline;
- fake or infer Cloudflare/direct-origin evidence;
- weaken security/recovery requirements to make closeout green;
- let Stage 2 silently modify the executable candidate while retaining Stage 1 evidence;
- add unrelated feature work or Phase 21 refactors;
- introduce native admin identity;
- record secrets in validation evidence;
- rewrite historical validation artifacts.

FINAL REPORT
If Stage 1 is GREEN but Stage 2 has not yet been performed, report only the Stage 1 handoff status and do not claim Phase 19 GREEN.

If final Stage 2 evidence is GREEN, report:
- exact accepted executable candidate SHA and package version `0.19.7`;
- Stage 1 deterministic/database/security/recovery/browser/upgrade command results;
- Stage 2 actual Level 8 deployment results for all required items;
- backup/restore and schema-upgrade/rollback conclusions;
- final tuned runtime policy;
- monitoring/recovery/retention/RPO/RTO ownership decisions recorded;
- Pass 2 and Pass 3 findings/disposition and whether remediation occurred;
- durable validation artifact path;
- explicit statement that Phase 20 customer-launch acceptance remains the production-baseline establishment point;
- readiness for separate conversational `/closeout` to create `0.20.0`.

If any required Stage 1 or Stage 2 evidence is missing or red, report BLOCKED/RED precisely and do not write a misleading final GREEN artifact.