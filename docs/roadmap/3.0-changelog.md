# 3.0 Roadmap Changelog

This file is the lightweight accepted-history companion to `docs/roadmap/3.0-roadmap.md`.

It records material roadmap work and decisions after they are accepted. It is intentionally brief and non-authoritative: contracts define behavior, the roadmap defines scope and sequencing, and validation artifacts record observed proof. A changelog entry must never be treated as evidence that tests, runtime behavior, provider behavior, or deployment behavior occurred.

Entries should normally contain the date, package version when relevant, and one to three bullets. Record accepted roadmap activation/version transitions, meaningful implementation milestones, phase closeouts, and material owner-approved roadmap or contract changes. Do not add entries merely because work is planned, and do not duplicate detailed validation evidence here.

## 2026-08-28 — 3.0 roadmap activated

**Package:** `2.1.0`

- Owner accepted the recovered GREEN `c1-n6wd` and post-2.0 runner compatibility corrections; N6WD is resolved and the runner's `p2-<phase>` / `2.<phase>.<prompt>` family is cleared for use.
- Performed the separately authorized package-version-only roadmap activation from `2.0.0` to `2.1.0`; no Phase 1 Gemini implementation was included in the activation commit.
- Phase 1 Gemini Profile digest foundation is now the active implementation phase, with first prompt version `2.1.1` and task-folder family `p2-1`.

## 2026-08-28 — Portable validation correction accepted

**Package:** `2.0.0`

- Accepted correction `c1-test-fix`: ordinary `npm test` / `npm run check` are now portable Node validation, while PHP runtime and PHP-backed customer browser proof remain separately addressable specialized suites.
- Added focused command-topology regression coverage and preserved fail-closed PHP prerequisite behavior without adding remote execution, environment sniffing, automatic retries, or broad always-run VPS aggregates.
- Durable evidence is recorded in `docs/validation/c1-test-fix-validation.md`; PHP/PHP-browser runtime proof remains deferred to final full-system/project release qualification.

## 2026-08-28 — VPS validation assigned to final qualification gates

**Package:** `2.0.0`

- Clarified that VPS-required evidence is deferred through ordinary implementation prompts and ordinary phase/correction closeouts rather than becoming a default per-prompt GREEN requirement.
- Only an explicitly designated VPS/full-system/project release qualification gate promotes deferred VPS evidence to `RUN`; that proof applies to the final integrated candidate for the major development line.
- Exact-tree cross-environment matching therefore belongs to the qualification candidate, not to every intermediate prompt commit, while explicitly invoked specialized suites remain fail-closed.

## 2026-08-28 — Change-aware and environment-aware validation locked

**Package:** `2.0.0`

- Locked validation planning around a Test Necessity Matrix plus Test Environment Matrix so prompt validation is selected by actual blast radius and execution prerequisites rather than by an always-run superset.
- Locked `RUN` / `DEFER` / `N/A` planning semantics and no automatic retry for deterministic prerequisite/environment failures while preserving fail-closed behavior for explicitly invoked specialized suites.
- Planned unchanged-version correction stack `c1-test-fix` to make the ordinary local regression path portable and separate VPS-required PHP/PHP-browser evidence without forcing that evidence into ordinary prompts or closeouts.

## 2026-08-28 — Gemini Phase 1 grounding and summary bounds locked

**Package:** `2.0.0`

- Locked the initial Gemini Phase 1 provider profile around the Gemini Developer API/Interactions API, `gemini-3.7-flash`, structured stateless output, governed URL Context over exact Profile Article `originalUrl` values, and no general Google Search grounding.
- Locked N6WD at a 4,000-character normalized persisted `Article.summary` maximum with complete-word truncation plus `...`, including the supported-production forward data transformation requirement.
- Preserved bounded summary fallback for paywalled/unavailable Article-page retrieval and kept provider/retrieved content downstream, untrusted, topic independent, and non-critical to ordinary Article delivery.

## 2026-08-28 — Roadmap changelog established

**Package:** `2.0.0`

- Added a brief accepted-history companion for the 3.0 roadmap.
- Future accepted 3.0 work and material roadmap decisions should be summarized here through the normal `/docs-review` → approval → `/docs-apply` workflow.
