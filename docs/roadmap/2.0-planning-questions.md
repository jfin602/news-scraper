# 2.0 Planning Questions

**Status:** Working roadmap-planning worksheet.  
**Authority:** Non-normative. This file records unresolved 2.0 design questions and the answers reached during roadmap planning. Governing contracts/ADRs must be updated separately through the normal docs workflow before implementation resumes.

As decisions are reached, replace each `Open` entry with the agreed answer rather than keeping a parallel answer log.

## 1. Distribution Profile selectors

**Status:** Answered

Sources and Distribution Profiles are peer top-level resources within one News Scraper instance.

A Source is configured, approved, collected, normalized, and monitored once per instance. It owns its endpoints and collection behavior. Distribution Profiles do not own, duplicate, or recollect Sources. The same approved Source may be associated with multiple Profiles without duplicating Source configuration, endpoint configuration, collection jobs, Article identity, or provenance.

A Distribution Profile has explicit associations to one or more approved Sources. The Profile↔Source association is a first-class relationship because it may carry distribution-specific configuration for that Source in the context of that Profile. An active Profile must ultimately have at least one usable approved Source association, while draft configuration may exist before that requirement is satisfied.

Filtering is deliberately split into two stages with different purposes:

1. **Source collection filtering** is an optional protective ingestion gate. It answers whether an item should enter News Scraper at all. It is expected to be unused in many ordinary deployments and must not be treated as the primary mechanism for sorting or tailoring customer feeds.
2. **Distribution Profile filtering** is the normal customer-facing selection mechanism. It operates after governed collection/normalization and canonical outward eligibility, and decides which already-eligible Articles from each associated Source appear in that Profile.

Profile filtering must never alter Source approval, endpoint collection, stored Article identity/provenance, moderation state, duplicate decisions, or canonical outward eligibility. A Profile may only narrow the already-governed corpus.

The exact bounded selector/operator vocabulary for Profile distribution filtering remains to be designed later, but its ownership and placement are fixed by this answer: Source-global collection safety remains Source-owned; customer/feed tailoring belongs to the Distribution Profile and, where Source-specific behavior is needed, to the Profile↔Source relationship.

## 2. Canonical outward eligibility vs reference-frontend visibility

**Status:** Answered

Canonical outward Article eligibility is independent of bundled reference-frontend exposure.

The canonical eligibility boundary continues to decide which Articles are ordinarily eligible for outward use based on governed Source trust/lifecycle, Article visibility/moderation, duplicate suppression, ordering, destination semantics, and other canonical rules. Distribution Profiles operate after that boundary and may only narrow it.

The Publication/reference-frontend public exposure state governs only the legacy/reference public surfaces. Disabling the bundled public frontend must not disable Distribution Profiles, the 2.0 machine distribution API, RSS/Atom profile output, or PHP/WordPress/custom integrations.

The existing `GET /` and `GET /api/feed` remain supported reference/legacy surfaces associated with the bundled public feed and its public exposure semantics. They are not reinterpreted as the permanent 2.0 integration API.

The 2.0 distribution system therefore uses a separate Profile-governed machine interface whose availability is controlled by Profile lifecycle/state and machine authorization rather than by the reference frontend's public exposure setting.

Conceptually:

```text
canonical outward eligibility
→ Distribution Profile selection
→ 2.0 machine/RSS/adapter distribution
```

is independent from:

```text
canonical outward eligibility
→ reference-frontend public exposure gate
→ GET / and legacy/reference GET /api/feed
```

## 3. Permanent 2.0 machine API

**Status:** Answered

The permanent 2.0 machine-facing distribution interface is a separate, explicitly versioned, read-only, Profile-addressed JSON API.

The initial route shape is:

```text
GET /api/v1/distribution/{profile_key}
GET /api/v1/distribution/{profile_key}?cursor=...
```

The immutable Profile `config_key` is explicit in the resource path. Machine credentials authenticate and authorize access but do not implicitly choose the Profile. This keeps Profile identity visible, allows a credential model that may later authorize more than one Profile, and makes requests/debugging unambiguous.

The API exposes only the result of the governed Distribution Profile/read-model authority. Consumers may not reconstruct Profile selection through arbitrary Source, Category, keyword, or equivalent discovery query parameters. Profile configuration belongs to the control plane.

Continuation uses opaque keyset/cursor pagination rather than SQL offset or page-number semantics. Cursor validity remains bound to the governed request/profile criteria required for deterministic continuation.

The response uses a stable versioned JSON envelope containing API/profile metadata, an Article item collection, and an opaque next cursor or null. Exact guaranteed fields are decided under Question 4.

Normal machine JSON distribution requires machine authentication. The exact credential mechanism and credential-to-Profile mapping are decided under Questions 5 and 6. The distribution API is read-only and machine credentials must not grant administrator mutation capability.

Expected response-state semantics are:

- active Profile + authorized credential + valid request: `200`;
- active Profile with no matching Articles: `200` with an empty item collection;
- malformed request or invalid cursor: `400`;
- missing/invalid machine authentication: `401`;
- authenticated credential without authorization for the requested Profile: `403`;
- nonexistent Profile: `404`;
- disabled Profile: an explicit machine-readable disabled-Profile error rather than disguising it as nonexistent;
- bounded dependency/service failure: `503`.

The exact HTTP status/code used for the disabled-Profile error may be finalized with the API/error contract, but the response must distinguish disabled state from missing Profile state for authorized clients.

Compatibility is version-bound. Within `/api/v1`, required fields and documented semantics may not be removed or incompatibly reinterpreted. Additive optional fields may be introduced when they preserve existing consumers. Breaking external contract changes require a new major API path such as `/api/v2` rather than silently changing v1 semantics.

The existing `GET /api/feed` remains a supported reference/legacy public-feed surface and is not redefined as this permanent integration API.

## 4. Guaranteed distribution fields

**Status:** Answered

The v1 distribution API uses a stable response shape. Guaranteed schema presence is distinct from guaranteed non-null source data: optional metadata fields remain present and return `null` when unavailable, while collections remain present and return `[]` when empty. This lets adapters rely on one durable schema without fabricating missing publisher metadata.

The envelope guarantees:

- `apiVersion`;
- `generatedAt`;
- `profile` containing immutable `configKey` and human-facing `displayName`;
- `publication` containing `name`;
- `items`;
- `nextCursor`, containing an opaque continuation cursor or `null`.

Each Article item guarantees:

- `articleId` as stable Article identity across Profiles;
- `headline` as the effective outward/moderated headline;
- `originalUrl` as the canonical publisher destination;
- `effectiveFeedDate` as the canonical outward ordering/display date;
- `feedDateSource` identifying how the effective date was derived;
- nullable `publishedAt` for the publisher-supplied publication timestamp when available;
- nullable `author`;
- nullable `summary`;
- nullable `imageUrl`;
- `source` containing stable Source `configKey` and human-facing `displayName`;
- `categories` as an array of effective outward Category objects containing `configKey` and `displayName`.

`summary` and `imageUrl` are part of the guaranteed v1 schema even when current Source collection cannot populate them. They return `null` until data is available. This allows collection/enrichment capabilities to improve later without requiring an API-breaking schema change.

Both `publishedAt` and `effectiveFeedDate` are exposed because they have different semantics. `publishedAt` may be null, while `effectiveFeedDate` remains the canonical date used for outward ordering. `feedDateSource` makes the fallback provenance explicit.

Category output reflects the effective outward moderated state and does not expose internal automatic/manual evaluation mechanics.

The machine distribution contract deliberately excludes control-plane and persistence internals such as endpoint configuration, Source approval/lifecycle internals, canonical identity URLs, duplicate-group/Primary mechanics, moderation history, Relevance evaluation internals, collection runs/observations, and incidental database timestamps.

Presentation-specific branding is not required in the initial machine contract beyond Publication name because downstream integrations own presentation. Additional optional outward metadata may be added compatibly within v1 when it does not change existing required semantics.

## 5. Machine authentication

**Status:** Answered

The 2.0 machine distribution API uses dedicated high-entropy bearer API credentials sent only in the HTTP `Authorization: Bearer ...` header. Machine credentials are separate from human administrator authentication and authorize only machine distribution access.

Tokens are generated by News Scraper rather than chosen by users. The token format may include a recognizable distribution-token prefix and a non-secret lookup identifier plus a cryptographically random secret, but the secret itself must provide strong entropy (approximately 256 bits or equivalent). Plaintext credentials are shown only when created and are never stored in PostgreSQL after issuance.

Persisted credential state contains only the non-secret identity/lookup material, a secure digest/verifier for the secret, a human-readable label, lifecycle metadata, and audit-oriented timestamps such as creation, last use, optional expiry, and revocation. Secret verification must avoid timing leaks. Credentials must never be accepted through query strings and must not be intentionally written to application logs. Production machine distribution requires HTTPS.

The initial capability family is deliberately narrow: `distribution:read`. Possession of a machine credential must never imply administrator authority or mutation capability. Authorization to particular Distribution Profiles is layered on top of this read capability and is defined under Question 6.

Credential expiration is supported but not mandatory. Long-lived server-to-server integrations may operate without forced short expirations, while customers may opt into expiry when appropriate.

Rotation is non-destructive: customers may create a replacement credential, configure and verify the consumer using it, then revoke the old credential. Multiple simultaneously valid credentials are therefore supported where authorization permits. Revocation takes effect for subsequent requests and the revoked credential record is retained for audit/history rather than deleted.

Invalid, revoked, expired, or otherwise unusable credentials return an authentication failure without leaking internal credential state. After authentication succeeds, Profile authorization failures are distinguished as authorization errors.

Rate limiting is primarily per authenticated machine credential so one integration cannot consume every other consumer's allowance. Repeated invalid-authentication attempts also receive separate network/IP-oriented abuse protection. Rate limits must support ordinary cron synchronization and cursor pagination, and `429` responses should be machine-readable and include `Retry-After`. Billing-style quotas are not required for the initial 2.0 contract; rate limiting is an operational and abuse-protection mechanism.

## 6. Credential-to-Profile mapping

**Status:** Open

How do machine credentials map to Distribution Profiles? One credential per Profile, one credential allowed multiple Profiles, or both?

## 7. Adapter synchronization model

**Status:** Open

How should PHP/WordPress adapters synchronize? Full bounded snapshots, incremental/delta synchronization, polling intervals, retries, and atomic replacement need to be decided.

## 8. Last-known-good cache contract

**Status:** Open

What is the local last-known-good cache contract? Define storage representation, freshness/TTL, stale-on-error behavior, locking, corruption handling, and replacement rules.

## 9. Generic PHP integration surface

**Status:** Open

What should the generic PHP integration expose? Raw normalized data access, synchronization/cache helpers, fallback rendering, extension hooks, or a combination?

## 10. WordPress/PHP sharing boundary

**Status:** Open

How much implementation should WordPress share with the generic PHP package? Determine the common client/synchronization/cache boundary and WordPress-specific responsibilities.

## 11. RSS/Atom distribution access

**Status:** Open

How should RSS/Atom distribution be exposed? Decide public vs private/authenticated access, Profile-specific endpoints, credentials/tokens if any, and supported metadata.

## 12. SEO and link policy

**Status:** Open

What is the supported SEO/link policy? Define direct publisher links, `rel` attributes, reciprocal-link behavior, canonical/sitemap ownership, and the limits of any SEO/backlink claims.

## 13. Exclude-self semantics

**Status:** Open

Does the Platform need an explicit `exclude self` Distribution Profile semantic, or should this always be represented through ordinary Source exclusion/profile configuration?

## 14. 2.0 self-hostable deployment definition

**Status:** Open

What does self-hostable mean for the 2.0 release? Decide required packaging/deployment form, supported environment assumptions, configuration/secrets, PostgreSQL expectations, upgrades, backup/restore, and operational boundaries.

## 15. Self-hosted administrator authentication

**Status:** Open

What secure administrator perimeter replaces or complements Cloudflare Access for self-hosted installations? Native application authentication, reverse-proxy/SSO contract, or both?

## 16. Distribution telemetry

**Status:** Open

What telemetry is required for distribution? Consider profile/API requests, authentication failures, adapter synchronization failures, stale caches, adapter versions, response errors, and whether referral/click analytics belong in 2.0.

## 17. Backward compatibility

**Status:** Open

What backward compatibility is promised through 2.0? Define treatment of existing `/`, `/api/feed`, `1.0.1` behavior, the new external API, adapter compatibility, and production schema/data upgrades.

## 18. 2.0.0 completion gate

**Status:** Open

What specifically must be production-ready for `2.0.0`? Define required adapters, deployment modes, control-plane/profile capabilities, security/reliability proof, and which capabilities may remain post-2.0.
