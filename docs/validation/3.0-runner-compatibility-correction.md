# 3.0 runner compatibility correction validation

**Correction:** `c1-runner-fix`, Correction 1 / P1

**Disposition:** **RED — HUMAN REVIEW REQUIRED**

## Purpose and execution shape

This owner-approved correction adds the explicit post-2.0 roadmap family to the Codex phase runner while preserving the historical pre-1.0, post-1.0, and correction-stack semantics. The correction stack intentionally contains one prompt only: its final closeout. The owner approved the combined implementation/closeout execution shape; no implementation or validation changes were self-committed.

## Reviewed state

- Pre-correction HEAD: `1fcd8d38b94ca1d99632ec20bf07364c28563938`.
- Package version remains `2.0.0`.
- No `package-lock.json` or `npm-shrinkwrap.json` exists.
- Final candidate is an uncommitted human-review working tree; no final commit SHA is claimed.
- No Gemini, product, database, API, PHP runtime, or `2.1.0` activation work occurred. A bounded PHP-package *test* expectation was repaired so its version assertions derive from the authoritative current `package.json` rather than a retired literal `1.7.0` value.

Changed files at prompt completion:

- `scripts/codex-phase-core.mjs`
- `scripts/codex-phase-core.d.mts`
- `scripts/validate-codex-phase.mjs`
- `test/unit/codex-phase-prompt-grammar.test.ts`
- `test/unit/codex-phase-runner.test.ts`
- `test/unit/php-integration-package.test.ts`
- this validation artifact

Current router/status documentation was intentionally left unchanged because the required full validation did not reach GREEN.

## Implemented grammar and preservation

| Family | Folder grammar | Prompt target formula |
| --- | --- | --- |
| Historical pre-1.0 | `p<number>` | `0.<phase>.<prompt>` |
| Post-1.0 | `p1-<phase>` | `1.<phase>.<prompt>` |
| Post-2.0 | `p2-<phase>` | `2.<phase>.<prompt>` |

`p2` remains historical Phase 2; only `p2-<phase>` selects the new explicit family. `roadmapVersionFor()` remains the centralized target and Git-resume version authority. Corrections retain their independent `c<phase>-<lower-kebab-slug>` grammar, exact fixed-version policy, commit-subject convention, and closeout behavior.

The runtime/type union now includes `post-2.0` and major `2`. Validator/dashboard/handoff labels use an explicit three-family label helper rather than binary post-1.0/historical branching.

## Evidence actually run

- `node scripts/run-tests.mjs --test-concurrency=1 test/unit/codex-phase-prompt-grammar.test.ts test/unit/codex-phase-runner.test.ts` — PASS, 83 tests.
- `node scripts/run-tests.mjs --test-concurrency=1 test/unit/php-integration-package.test.ts test/unit/codex-phase-prompt-grammar.test.ts test/unit/codex-phase-runner.test.ts` — PASS, 90 tests.
- `npm run codex:phase:validate -- c1-runner-fix` — PASS. The one-prompt correction stack parses as a valid correction closeout at unchanged `2.0.0`.
- `npm run check` — RED. Formatting, lint, and TypeScript passed. The JavaScript unit/integration/collection command passed 528 tests after the bounded package-test repair. The required subsequent `npm run test:php` could not run because the PHP CLI executable was unavailable in this environment.
- `git diff --check` — PASS after all final candidate files, including this artifact, were present.

The failed full-check result is not treated as a green exception: the validation contract requires explicitly required suites to fail when prerequisites are absent.

## Three-pass closeout review

### Pass 1 — contract and evidence

- The runner has an explicit third `post-2.0` family with major `2`, parsed from canonical `p2-<phase>` folders.
- Grammar tests prove historical `p2`, post-1.0 `p1-1`, post-2.0 `p2-0`/`p2-1`, cross-family target rejection, collision rejection, one-based prompt numbering, and unchanged correction grammar.
- Runner tests prove baseline `2.1.0`, committed-P1 `2.1.1` reconstruction, exact semantic-version subjects, unrelated-newer-commit tolerance, duplicate/gap/package mismatch rejection, truthful post-2.0 display/validator output, and one-prompt correction `--closeout` review-required behavior without self-commit.
- Package `2.0.0` and no-lockfile invariants are observed.
- Result: implementation/focused evidence is green; full required evidence is RED because PHP regression prerequisites are unavailable.

### Pass 2 — adversarial edge cases

- Historical `p2` versus `p2-1` collision: protected by distinct canonical regexes and grammar coverage.
- Cross-family target acceptance, leading zeroes, uppercase folders, missing phase, doubled hyphen, and alternate forms: protected by fail-closed grammar tests.
- Target validation/resume divergence: both use `roadmapVersionFor()`; synthetic 2.x resume tests cover the baseline and P1 prefix.
- Declaration/runtime drift: explicit closed runtime and declaration unions include the same family/major set.
- Display mislabeling: validator/dashboard/handoff use tested three-family labels.
- Duplicate/gapped history and correction-major leakage: existing checks remain in place and are exercised for the new family or retained correction tests.
- Zero-implementation one-prompt correction closeout: explicit runner coverage verifies `--closeout`, unchanged HEAD, no closeout commit message, and review-required state.
- Closeout safety boundaries: retained runner tests cover HEAD drift, lockfiles, invalid version, and incoherent diffs.
- Current docs claiming clearance before evidence: avoided; current router/status documentation remains unchanged due the RED full check.

### Pass 3 — code quality and structure

- Folder parsing remains solely in `buildPlan()`.
- Semantic roadmap version derivation remains solely in `roadmapVersionFor()`.
- No arbitrary-major parser, aliases, configuration-driven registration, or speculative future family was added.
- The label helper replaces the two brittle display branches without duplicating version logic.
- No changes to `scripts/codex-phase.mjs`, launcher mapping, commit ownership, dirty-tree checks, or closeout semantics were required.
- The PHP package test repair is a bounded test-only fix: it now asserts against the sole current-version authority and prevents the already-observed stale-literal failure on future version transitions.

## Remaining limitation and required next action

Install or make the PHP CLI available, then rerun `npm run check`, `npm run codex:phase:validate -- c1-runner-fix`, and `git diff --check` on the exact candidate. Only if those results are green may current router/status docs say the runner blocker is cleared, followed by human review/acceptance and the separately authorized version-only `2.0.0` to `2.1.0` roadmap activation. Phase 1 Gemini implementation has not started.
