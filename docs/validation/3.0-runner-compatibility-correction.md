# 3.0 runner compatibility correction validation

**Correction:** `c1-runner-fix`, Correction 1 / P1  
**Disposition:** **GREEN — HUMAN REVIEW REQUIRED**  
**Package:** `2.0.0`  
**Pre-correction HEAD:** `1fcd8d38b94ca1d99632ec20bf07364c28563938`  
**Accepted correction commit:** `ba7d1eb6d5496aada0d9ba96f500b8568ca1c98a`

## Purpose and execution shape

This owner-approved correction adds the explicit post-2.0 roadmap family to the Codex phase runner while preserving historical pre-1.0, post-1.0, and correction-stack semantics.

The correction stack intentionally used a combined implementation/closeout shape. It did not activate the 3.0 roadmap or change package version.

## Implemented grammar and preservation

| Family | Folder grammar | Prompt target formula |
| --- | --- | --- |
| Historical pre-1.0 | `p<number>` | `0.<phase>.<prompt>` |
| Post-1.0 | `p1-<phase>` | `1.<phase>.<prompt>` |
| Post-2.0 | `p2-<phase>` | `2.<phase>.<prompt>` |

`p2` remains historical Phase 2; only `p2-<phase>` selects the explicit post-2.0 family. `roadmapVersionFor()` remains the centralized target/resume version authority. Correction stacks retain their independent `c<phase>-<lower-kebab-slug>` grammar, unchanged-version policy, commit-subject convention, and closeout behavior.

The runtime/type union includes `post-2.0` and major `2`. Validator/dashboard/handoff labels use an explicit three-family label helper rather than binary historical/post-1.0 branching.

## Evidence history and recovery of the former RED disposition

The original correction closeout recorded:

- focused parser/runner suite — **PASS, 83 tests**;
- focused PHP-package + parser/runner suite — **PASS, 90 tests**;
- `npm run codex:phase:validate -- c1-runner-fix` — **PASS**;
- `npm run check` — formatting, lint, TypeScript, and **528 JavaScript tests PASS**, followed only by an unavailable PHP CLI prerequisite;
- `git diff --check` — **PASS**.

The original closeout therefore reported RED solely because the then-current ordinary aggregate invoked `npm run test:php` and the local Windows environment did not provide PHP CLI.

Correction `c1-test-fix` later made `npm test` / `npm run check` portable and recorded `npm run check` **PASS with 530 Node tests and no PHP invocation** at commit `c72d8238ca2179d18eea4491a9113c1fde07e7fb`.

Repository comparison from runner correction commit `ba7d1eb6d5496aada0d9ba96f500b8568ca1c98a` to the later accepted validation-policy state shows no changes to:

- `scripts/codex-phase-core.mjs`;
- `scripts/codex-phase-core.d.mts`;
- `scripts/validate-codex-phase.mjs`;
- `test/unit/codex-phase-prompt-grammar.test.ts`;
- `test/unit/codex-phase-runner.test.ts`;
- `test/unit/php-integration-package.test.ts`.

Later product/correction work therefore did not alter the runner implementation or its focused proof. The later portable `npm run check` result closes the former aggregate-only blocker without repeating unchanged runner evidence.

No new runner tests were executed while updating this artifact. Existing exact-tree/focused evidence is being reused under the project’s coverage-set-based validation policy.

## Three-pass closeout review

### Pass 1 — contract and evidence

The original closeout established:

- explicit third `post-2.0` roadmap family with major `2` parsed from canonical `p2-<phase>` folders;
- historical `p2`, post-1.0 `p1-1`, and post-2.0 `p2-0` / `p2-1` grammar coverage;
- cross-family target rejection, collision rejection, one-based numbering, and unchanged correction grammar;
- baseline `2.1.0` and committed-P1 `2.1.1` resume reconstruction;
- exact semantic-version subjects;
- unrelated-newer-commit tolerance;
- duplicate/gap/package-mismatch rejection;
- truthful post-2.0 display/validator output;
- one-prompt correction `--closeout` review-required behavior without self-commit;
- package `2.0.0` and no-lockfile invariants.

Under the current Qualification-gate policy, PHP runtime evidence is not an ordinary runner-correction closeout requirement merely because the old aggregate happened to invoke it. The runner correction does not change PHP runtime behavior.

Result: **PASS**.

### Pass 2 — adversarial edge cases

Protected cases from the original closeout include:

- historical `p2` versus `p2-1` collision;
- cross-family target acceptance/rejection;
- leading zeroes, uppercase folders, missing phase, doubled hyphen, and alternate forms;
- target-validation/resume divergence;
- declaration/runtime family drift;
- display mislabeling;
- duplicate/gapped Git history;
- correction-major leakage;
- zero-implementation one-prompt correction closeout behavior;
- closeout HEAD drift, lockfile, invalid-version, and incoherent-diff safety boundaries.

No later runner-source drift invalidated those protections.

Result: **PASS**.

### Pass 3 — code quality and structure

The accepted correction preserves narrow ownership:

- folder parsing remains in `buildPlan()`;
- semantic roadmap version derivation remains in `roadmapVersionFor()`;
- no arbitrary-major parser, aliases, configuration-driven registration, or speculative future family was added;
- the label helper removes brittle binary display branching without duplicating version logic;
- launcher mapping, commit ownership, dirty-tree checks, and closeout semantics were not unnecessarily redesigned;
- the bounded PHP-package test repair derives expected package version from authoritative `package.json` rather than a retired hard-coded `1.7.0` literal.

No structural remediation is required.

Result: **PASS**.

## Deferred qualification evidence

PHP runtime and PHP-backed browser evidence remain governed by the project’s later full-system/project release qualification gate when applicable to the final integrated release candidate. They are not required to certify this runner grammar correction and are not claimed as executed by this artifact update.

## Closeout

`c1-runner-fix` is **GREEN** for its governed correction scope.

The phase runner supports the intended `p2-<phase>` / `2.<phase>.<prompt>` family while preserving historical grammar and correction behavior. The former RED status was caused only by the obsolete PHP-coupled aggregate validation topology, which has since been corrected independently.

This artifact update does not itself perform or authorize the version-only `2.0.0` → `2.1.0` roadmap activation. That transition remains a separate owner-controlled step.